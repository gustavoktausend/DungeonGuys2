---
phase: 02-migra-o-para-a-vps
plan: 05
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - playwright.config.ts
  - tests/pwa/tsconfig.json
  - tests/pwa/helpers.ts
  - tests/pwa/fixtures/old-build/
  - tests/pwa/fixtures/README.md
  - tests/pwa/install.spec.ts
  - tests/pwa/offline.spec.ts
  - tests/build-base.test.ts
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "Existe um runner capaz de servir um `dist/` construído numa origem controlada e de derrubar esse servidor no meio do teste"
    - "A instalação antiga do critério 2 está congelada no repositório, com o service worker de antes da reescrita"
    - "A fixture não pode nascer sendo o build novo: um teste reprova se ela for regenerada"
    - "As duas specs de instalação limpa e de offline existem e falham por afirmarem o comportamento alvo, não por erro de configuração"
  artifacts:
    - path: "playwright.config.ts"
      provides: "runner de PWA com um único browser, por service worker ser Chromium-only no Playwright"
      contains: "chromium"
    - path: "tests/pwa/helpers.ts"
      provides: "servidor estático derrubável, leitura do Cache Storage e espera de ativação"
      min_lines: 60
    - path: "tests/pwa/fixtures/old-build/sw.js"
      provides: "a instalação antiga do critério 2, congelada antes da reescrita"
      contains: "skipWaiting"
    - path: "tests/pwa/install.spec.ts"
      provides: "instalação limpa: precache cobre o dist inteiro e o cache é único"
    - path: "tests/pwa/offline.spec.ts"
      provides: "o jogo abre com o servidor derrubado, sem nunca ter sido jogado"
  key_links:
    - from: "tests/pwa/offline.spec.ts"
      to: "tests/pwa/helpers.ts"
      via: "fechar o http.Server da fixture antes do reload"
      pattern: "close\\(\\)"
    - from: "tests/build-base.test.ts"
      to: "tests/pwa/fixtures/old-build/sw.js"
      via: "guarda de que a fixture continua sendo o worker antigo"
      pattern: "skipWaiting"
---

<objective>
Montar o runner de PWA, **congelar a instalação antiga** e escrever as duas specs que
descrevem o comportamento alvo — que ficam vermelhas até o plano 02-06 reescrever o service
worker.

**Fatia (MVP):** este plano entrega o instrumento de medida antes da mudança. Ao fim dele,
"instalou e joga offline" deixa de ser opinião e passa a ser um comando que hoje diz
**não** — e é essa transição de não para sim que o plano seguinte tem de produzir.

Purpose: a ordem aqui não é gosto, é a reconciliação obrigatória número 3 da fase.
`tests/pwa/fixtures/old-build/` precisa ser o `dist/` construído **depois** da mudança de
`base` (02-02, para que o escopo do service worker antigo seja `/` e a atualização seja
in-place) e **antes** da reescrita do `sw.js` (02-06). Se a fixture nascer depois, ela é o
build novo, e o teste de atualização do critério 2 passa por vacuidade. O `depends_on` deste
plano e o do 02-06 são o que amarra isso.

Output: `@playwright/test@1.62.1`, `playwright.config.ts`, `tests/pwa/helpers.ts`, a fixture
congelada com guarda, e `install.spec.ts` + `offline.spec.ts` em RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-migra-o-para-a-vps/02-CONTEXT.md
@.planning/phases/02-migra-o-para-a-vps/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `@playwright/test`, `playwright.config.ts` e os helpers</name>
  <files>package.json, package-lock.json, tsconfig.json, playwright.config.ts, tests/pwa/tsconfig.json, tests/pwa/helpers.ts</files>
  <read_first>
    - `package.json` — `devDependencies` (nota: `playwright` já está travado em `1.62.1` exato) e o bloco `scripts`, para manter a forma de uma linha por script
    - `vitest.browser.config.ts` — o precedente de config separada por runner, com o comentário explicando por que a **forma** foi escolhida
    - `vitest.config.ts` — `include: ['tests/**/*.test.ts']`, que é por que specs `.spec.ts` **não** precisam de exclusão
    - `tsconfig.json` — `types: ["vite/client"]` (`:14`) e `include` (`:25`)
    - `packages/protocol/tsconfig.json` — o precedente de `tsconfig` de subdiretório com `lib`/`types` próprios e o comentário que explica a razão
    - `tests/helpers.ts` — o precedente de módulo `.ts` sem `.test.` no nome, com comentário explicando por que a fixture é o que é
    - `.planning/phases/02-migra-o-para-a-vps/02-VALIDATION.md` — § "Notas de projeto do teste de PWA", os seis itens (Chromium-only, `setOffline` não confiável sozinho, filtrar `requestfailed` por origem, `http://localhost` é contexto seguro, nunca `serviceWorkers: 'block'`, cuidado com o typecheck)
    - `.github/workflows/ci.yml` — linhas 50-60, o cache de browser por versão exata do lock, que o job `pwa` do plano 02-09 vai reaproveitar
  </read_first>
  <action>
    Instalar `@playwright/test` em versão **exata**: `npm i -D -E @playwright/test@1.62.1`.
    A versão tem de casar com o `playwright` 1.62.1 já travado no lock — a chave de cache do
    `ci.yml` é derivada dessa versão exata, e um par divergente invalidaria o cache de
    binários dos dois portões.

    `playwright.config.ts`:
    - `testDir: 'tests/pwa'`, `testMatch: '**/*.spec.ts'`
    - `projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]` — **um**
      browser. Comentário explicando por quê, no tom do comentário de `vitest.browser.config.ts`:
      o Playwright só suporta service worker em Chromium, então rodar Firefox e WebKit aqui
      daria confiança falsa. Isso amplia a lacuna aceita de D2-11 e está registrado em
      `02-VALIDATION.md`.
    - `fullyParallel: false` e `workers: 1` — as specs registram service workers na mesma
      origem e o Cache Storage é por origem; paralelismo aqui produz interferência entre
      testes, não velocidade.
    - `reporter: 'list'`, `use: { serviceWorkers: 'allow' }` — nunca `'block'`, que
      inutilizaria a suíte inteira.
    - **Sem `webServer` global.** Cada spec cria e destrói o seu servidor pelo helper, porque
      `offline.spec.ts` precisa **derrubar** o servidor no meio do teste; um `webServer`
      gerenciado pelo Playwright não permite isso.
    - Script na raiz: `"test:pwa": "playwright test"` (uma linha, invocado por `npm run`,
      como `tools/README.md` §2 exige).

    Typecheck, sem afrouxar a disciplina do cliente:
    - Criar `tests/pwa/tsconfig.json` estendendo o da raiz, com `types: ["node"]` e
      `lib: ["ES2023", "DOM"]`, `include: ["."]`. Comentário na mesma forma do de
      `packages/protocol/tsconfig.json`: este e o de `apps/server` são os únicos lugares do
      repositório que pedem tipos de Node, e a razão é que o helper abre um `http.Server`.
    - Acrescentar `"exclude": ["tests/pwa"]` ao `tsconfig.json` da raiz, com comentário
      dizendo que o `types: ["vite/client"]` da raiz **não muda** (`tools/README.md` §4) e que
      esses arquivos são cobertos pelo `tsconfig` próprio.
    - Script na raiz: `"typecheck:pwa": "tsc -p tests/pwa/tsconfig.json --noEmit"`, simétrico
      a `typecheck:sim` e `typecheck:protocol`.

    `tests/pwa/helpers.ts`, exportando:
    - `serveDir(dir: string)` → `Promise<{ origin: string; setRoot(dir: string): void; close(): Promise<void> }>`.
      Um `http.Server` de `node:http` numa porta efêmera em `127.0.0.1`, servindo o diretório
      atual com `Content-Type` correto para `.html`, `.js`, `.css`, `.json`, `.webmanifest`,
      `.png`, `.woff2`, `.md` e `.txt`, e `Cache-Control: no-cache` para os caminhos de
      shell (`/`, `/index.html`, `/sw.js`, `/manifest.json`). `/` serve `index.html`;
      qualquer caminho inexistente responde **404**, nunca `index.html` — o servidor de teste
      tem de espelhar a decisão do `Caddyfile` de não usar `try_files`, ou os testes provariam
      um comportamento que a produção não tem.
      `setRoot` troca a raiz servida **sem fechar o servidor**, o que é o que permite encenar
      "build antigo → build novo" na mesma origem e na mesma porta (usado pelo plano 02-09).
    - `route(path, handler)` — registro de rotas dinâmicas, para uma spec poder servir
      `/api/health` e para poder forçar um 502 num caminho nomeado (usado pelo plano 02-09).
    - `waitForActivated(page)` — espera o `navigator.serviceWorker.ready` e o estado
      `activated` do worker.
    - `readCacheEntries(page)` → `Record<string, string[]>`, com os nomes de cache e, para cada
      um, os **pathnames** das entradas (não as URLs completas, para o teste não depender da
      porta efêmera).
    - `collectSameOriginFailures(page, origin)` — acumula eventos `requestfailed` cuja URL
      comece pela origem própria, e ignora o resto.
    - Comentário de cabeçalho explicando por que `close()` existe: `context.setOffline()` é
      emulação por CDP e há relato aberto de que não alcança requisições feitas pelo service
      worker (`microsoft/playwright#2311`). O teste de offline derruba o servidor **de
      verdade** e chama `setOffline(true)` por cima; assim ele prova offline mesmo se a
      emulação falhar.
    - Comentários em inglês; mensagens e nomes de teste em português.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run typecheck:pwa</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').devDependencies['@playwright/test']"` imprime `1.62.1` (sem `^` nem `~`)
    - `node -p "require('./package.json').devDependencies['playwright']"` continua imprimindo `1.62.1`
    - `grep -c "name: 'chromium'" playwright.config.ts` retorna 1 e `grep -c "firefox\|webkit" playwright.config.ts` retorna 0
    - `grep -c "serviceWorkers: 'block'" playwright.config.ts` retorna 0
    - `grep -c 'webServer' playwright.config.ts` retorna 0
    - `node -p "require('./package.json').scripts['test:pwa']"` imprime `playwright test`
    - `npx tsc --noEmit` sai 0 (a raiz continua com `types: ["vite/client"]`) e `npm run typecheck:pwa` sai 0
    - `npm test` continua verde (o `include` do Vitest é `*.test.ts` e não colide com `*.spec.ts`)
  </acceptance_criteria>
  <done>Existe um runner capaz de servir um build numa origem controlada e de derrubá-la no meio do teste, sem afrouxar o typecheck do cliente.</done>
</task>

<task type="auto">
  <name>Task 2: Congelar `tests/pwa/fixtures/old-build/` antes de o `sw.js` mudar</name>
  <files>tests/pwa/fixtures/old-build/, tests/pwa/fixtures/README.md, tests/build-base.test.ts</files>
  <read_first>
    - `public/sw.js` — inteiro, **na forma atual**: `CACHE = 'dungeonguys2-v1'` (`:20`), `PRECACHE` escrito à mão (`:22-30`), `skipWaiting()` no install (`:36`), `caches.keys()` + delete-tudo e `clients.claim()` no activate (`:42-44`), network-first sem checar `res.ok` (`:65-73`). É exatamente por ter esses cinco defeitos que ele é a fixture certa
    - `.gitignore` — `dist` é ignorado; `tests/pwa/fixtures/` **não** é, e é por isso que a fixture mora aqui em vez de depender de histórico de deploy
    - `tools/assets/fixtures/` — o precedente de fixture versionada que existe para provar aceitação **e** recusa
    - `tests/build-base.test.ts` — criado no plano 02-02, que ganha a guarda aqui
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` — linhas 763-776 (P-1, incluindo o "bônus": o `sw.js` atual é o fixture perfeito)
  </read_first>
  <action>
    Rodar `npm run build` **agora**, com o estado do repositório neste ponto: `base: '/'` e as
    fontes próprias já valendo (plano 02-02), e o `public/sw.js` ainda sendo o antigo. Copiar
    o `dist/` inteiro resultante para `tests/pwa/fixtures/old-build/` e commitá-lo.

    Essa combinação é a única que serve, e o motivo tem de ficar escrito:
    - Se a fixture fosse construída **antes** do plano 02-02, o escopo do service worker
      antigo seria `/DungeonGuys2/` e a atualização testada não seria in-place — o teste
      provaria outra coisa.
    - Se fosse construída **depois** do plano 02-06, ela seria o build novo e o teste de
      atualização do critério 2 passaria por vacuidade.

    Criar `tests/pwa/fixtures/README.md` (curto, 5 a 10 linhas) dizendo exatamente isso, e
    fechando com a regra: **este diretório nunca é regenerado por um build posterior.** Se
    algum dia ele precisar mudar, é commit próprio com justificativa.

    Acrescentar a `tests/build-base.test.ts` um `describe('fixture da instalação antiga')` com
    globs raw sobre `'../tests/pwa/fixtures/old-build/sw.js'` e
    `'../tests/pwa/fixtures/old-build/index.html'` e quatro asserções, cada uma com o motivo
    em comentário:
    - o `sw.js` da fixture contém `skipWaiting` e `dungeonguys2-v1` — prova que é o worker
      **antigo**;
    - o `sw.js` da fixture **não** contém `__PRECACHE__` nem `dg2-` — prova que não foi
      sobrescrito pelo template novo;
    - o `index.html` da fixture contém `href="/manifest.json"` — prova que foi construído
      **depois** da mudança de `base`, ou seja, com escopo `/`, que é o que torna o teste de
      atualização in-place;
    - o `index.html` da fixture **não** contém `/DungeonGuys2/`.
  </action>
  <verify>
    <automated>npx vitest run tests/build-base.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/pwa/fixtures/old-build/sw.js` e `test -f tests/pwa/fixtures/old-build/index.html` saem 0
    - `cmp tests/pwa/fixtures/old-build/sw.js public/sw.js` sai 0 (a fixture é byte a byte o worker atual)
    - `grep -c 'skipWaiting' tests/pwa/fixtures/old-build/sw.js` retorna 1 ou mais
    - `grep -c '__PRECACHE__' tests/pwa/fixtures/old-build/sw.js` retorna 0
    - `grep -c 'href="/manifest.json"' tests/pwa/fixtures/old-build/index.html` retorna 1
    - `git status --porcelain tests/pwa/fixtures/` mostra os arquivos como adicionados (a fixture está versionada, não ignorada)
    - `npx vitest run tests/build-base.test.ts` sai 0
  </acceptance_criteria>
  <done>A instalação antiga do critério 2 existe no repositório, e um teste reprova se ela virar o build novo.</done>
</task>

<task type="auto">
  <name>Task 3: `install.spec.ts` e `offline.spec.ts` — as duas specs em RED</name>
  <files>tests/pwa/install.spec.ts, tests/pwa/offline.spec.ts</files>
  <read_first>
    - `tests/pwa/helpers.ts` — criado na Task 1; as cinco exportações e o contrato de `serveDir`
    - `.planning/phases/02-migra-o-para-a-vps/02-VALIDATION.md` — § "Success Criteria → Sinal Observável → Onde É Medido", linhas do critério 2 (a) e (c); e § "Notas de projeto do teste de PWA"
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` — linhas 1113-1195 (o template alvo do `sw.js`: `CACHE = 'dg2-<hash>'`, precache derivado, sem `skipWaiting`, sem `clients.claim`)
    - `public/manifest.json` — `start_url` e `scope` em `"."`, que resolvem para `/` quando servido de `/manifest.json` (DM-6)
    - `index.html` — os ids `#start-screen` e `#btn-start`, que a spec de offline usa como sinal de "o jogo abriu e responde"
  </read_first>
  <action>
    Escrever as duas specs contra o comportamento **alvo**. Elas ficam vermelhas até o plano
    02-06, e isso é o ponto: a transição de vermelho para verde é a prova de que a reescrita
    do service worker fez o que devia. Não escrever nenhuma spec contra o comportamento atual.

    `tests/pwa/install.spec.ts` — instalação limpa (INFRA-02):
    - `serveDir('dist')` e navegar para `origin`.
    - `waitForActivated(page)`.
    - Assertar `navigator.serviceWorker.controller === null` no **primeiro** carregamento — o
      worker novo não chama `clients.claim()`, então a página que o instalou não é controlada
      por ele (D2-09). Depois de um `page.reload()`, assertar `controller !== null`.
    - Assertar que `caches.keys()` tem comprimento **1** e que o nome casa com
      `/^dg2-[0-9a-f]{16}$/`.
    - Assertar que o conjunto de pathnames guardados nesse cache é **exatamente** a lista de
      arquivos de `dist/` menos `sw.js`. A lista de referência vem de uma varredura recursiva
      real do diretório, feita em Node dentro da spec — não de uma lista escrita à mão, que é
      o defeito que o cabeçalho do `public/sw.js` atual documenta.
    - Assertar instalabilidade: ler `/manifest.json` e conferir que `scope` e `start_url`
      resolvem para `/`, e que o escopo do registro do service worker (`registration.scope`)
      resolve para a mesma coisa. Se os dois não baterem, o Chrome recusa a instalação como
      "fora do escopo".
    - Fechar o servidor no `afterEach`.

    `tests/pwa/offline.spec.ts` — abre sem rede, sem nunca ter jogado (INFRA-02):
    - `serveDir('dist')`, navegar, `waitForActivated`, `page.reload()` para o worker assumir o
      controle. **Não** clicar em START antes: o critério é "100% jogável offline sem nunca
      ter sido jogado".
    - Instalar o coletor `collectSameOriginFailures(page, origin)`.
    - **Derrubar o servidor de verdade** (`await server.close()`) e, por cima,
      `context.setOffline(true)`. Comentário explicando a redundância deliberada.
    - `page.reload()` e assertar: `#start-screen` tem a classe `active` e está visível; o
      texto do título aparece; `#btn-start` está visível; clicar em `#btn-start` faz
      `#start-screen` perder a classe `active` (o jogo começou de verdade, não só pintou).
    - Assertar `failures.length === 0` — **sem exceção de origem de terceiro**, porque D2-20
      trouxe as fontes para casa e não existe mais requisição cross-origin no caminho de
      carregamento. Comentário registrando que essa asserção só é possível por causa de D2-20.
  </action>
  <verify>
    <automated>npx playwright test tests/pwa/install.spec.ts tests/pwa/offline.spec.ts; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/pwa/install.spec.ts tests/pwa/offline.spec.ts` sai **1** — as specs estão em RED, como esperado neste ponto
    - O relatório de falha nomeia asserções de comportamento (nome de cache `dg2-…`, conjunto de precache, `controller`), **não** erro de import, `ReferenceError`, timeout de navegação nem falha ao subir o servidor da fixture
    - `npm run typecheck:pwa` sai 0
    - `npx playwright test --list` lista as duas specs no projeto `chromium` e em nenhum outro
    - Nenhuma spec usa `serviceWorkers: 'block'`, e `offline.spec.ts` chama `close()` do servidor **antes** de `setOffline(true)`
    - `npm test`, `npm run lint` e `npx tsc --noEmit` continuam saindo 0 (as specs estão fora do runner do Vitest e fora do `tsconfig` da raiz)
  </acceptance_criteria>
  <done>"Instalou e joga offline" virou um comando. Hoje ele diz não, com o motivo certo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache Storage do navegador ↔ origem inteira | qualquer service worker da origem enxerga o armazenamento de todos os apps dela; é o vetor que INFRA-03 existe para fechar |
| suíte de teste → npm | um pacote novo entra no grafo de dependências do repositório |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-CACHE | Information Disclosure | Cache Storage ← `/api/*` | mitigate | Este plano constrói a **medida**: `readCacheEntries` e a rota dinâmica do helper são o que torna `api-isolation.spec.ts` (plano 02-09) possível. A mitigação em si — allowlist derivado do build — é do plano 02-06 |
| T-2-VACUOUS | Tampering | `tests/pwa/fixtures/old-build/` | mitigate | Uma fixture regenerada a partir do build novo faria o teste de atualização passar sem testar nada. Quatro asserções em `tests/build-base.test.ts` travam a fixture: contém `skipWaiting` e `dungeonguys2-v1`, não contém `__PRECACHE__` nem `dg2-`, e o `index.html` já tem `href="/manifest.json"` |
| T-2-FALSEGREEN | Tampering | `offline.spec.ts` | mitigate | `context.setOffline()` é emulação por CDP e há relato de que não alcança requisições do service worker. O servidor da fixture é **fechado de verdade** antes do reload, e o `setOffline` é redundância por cima — sem isso o teste poderia ficar verde com o servidor ainda respondendo |
| T-2-SC | Tampering | `@playwright/test@1.62.1` | mitigate | O pacote consta da Package Legitimacy Audit da pesquisa com veredito **[OK]** (58,4M downloads/semana, `github.com/microsoft/playwright`, sem `postinstall`); a varredura foi `7 packages / 7 OK`, sem `[ASSUMED]`, `[SUS]` ou `[SLOP]`. Instalado com versão exata (`-E`) para casar com o `playwright` já travado |
</threat_model>

<verification>
- `npm run typecheck:pwa` e `npx tsc --noEmit` verdes
- `npm test` e `npm run lint` verdes
- `npx playwright test` sai 1, com falhas de asserção de comportamento nomeadas
- `cmp tests/pwa/fixtures/old-build/sw.js public/sw.js` sai 0
</verification>

<success_criteria>
O instrumento de medida do critério 2 existe e está calibrado: um runner de um browser só
(com a lacuna nomeada), uma instalação antiga congelada com guarda anti-vacuidade, e duas
specs que hoje reprovam por afirmarem o comportamento que o plano seguinte tem de entregar.
</success_criteria>

<output>
Create `.planning/phases/02-migra-o-para-a-vps/02-05-SUMMARY.md` when done
</output>

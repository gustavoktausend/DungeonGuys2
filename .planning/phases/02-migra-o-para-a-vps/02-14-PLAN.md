---
phase: 02-migra-o-para-a-vps
plan: 14
type: execute
wave: 10
depends_on: [02-13]
files_modified:
  - ops/Dockerfile.web
  - ops/Dockerfile.api
  - ops/docker-compose.yml
  - ops/litestream.yml
  - ops/probe/docker-compose.yml
  - ops/deploy.sh
  - ops/rollback.sh
  - ops/deploy-forced.sh
  - ops/prune-releases.sh
  - ops/dg2.service
  - ops/litestream.service
  - ops/cert-check.sh
  - ops/cert-check.service
  - ops/cert-check.timer
  - ops/README.md
  - tests/ops-config.test.ts
autonomous: true
requirements: [INFRA-04]

must_haves:
  truths:
    - "A composição declara dois serviços, nenhuma porta publicada no host, nenhuma rede própria e nenhum passo de build"
    - "Toda imagem é referenciada por sha de commit, nunca por tag móvel — sem isso não existe 'imagem anterior' para reverter"
    - "O limite de memória do cgroup e o teto do heap do V8 andam juntos, e o segundo é menor que o primeiro"
    - "O contêiner tem mais tempo para desligar do que o watchdog do processo pede, com folga para a sincronização final do Litestream"
    - "O Litestream é PID 1 e envolve o Node, e o Node não é PID 1"
    - "Nenhum arquivo que ninguém executa continua em `ops/`, e nenhuma asserção sobre eles ficou órfã"
    - "O piso anti-vacuidade dos globs de `ops/` corresponde ao conteúdo real do diretório"
    - "A réplica do Litestream vive num volume PERSISTENTE, não numa camada de contêiner — senão o primeiro redeploy apaga o backup em silêncio (D2-33)"
    - "Nenhuma variável de bucket (`LITESTREAM_BUCKET`, `LITESTREAM_ENDPOINT`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`) aparece na composição nem no runbook — D2-33 as revogou"
    - "O runbook é executável por um operador que nunca viu esta caixa, e diz `sudo` em todo comando de Docker"
  artifacts:
    - path: "ops/docker-compose.yml"
      provides: "a composição que o Coolify lê do repositório: dois serviços, DOIS volumes persistentes (banco e réplica, D2-33), limites"
      contains: "pull_policy"
      min_lines: 60
    - path: "ops/Dockerfile.api"
      provides: "o servidor, o Litestream que o envolve, e nada mais"
      contains: "ENTRYPOINT"
      min_lines: 30
    - path: "ops/Dockerfile.web"
      provides: "o Caddy com a política HTTP e o dist/ que passou pelo portão cross-engine"
      contains: "COPY"
      min_lines: 12
    - path: "ops/README.md"
      provides: "o runbook da caixa containerizada"
      contains: "sudo docker"
      min_lines: 300
  key_links:
    - from: "ops/docker-compose.yml"
      to: "ops/litestream.yml"
      via: "o caminho do banco declarado no serviço `api` é o mesmo caminho que o Litestream replica"
      pattern: "/var/lib/dg2/dg2.db"
    - from: "ops/docker-compose.yml"
      to: "apps/server/src/shutdown.ts"
      via: "o stop_grace_period do serviço `api` é maior que SHUTDOWN_GRACE_MS, importado e não copiado"
      pattern: "stop_grace_period"
    - from: "ops/Dockerfile.web"
      to: "ops/Caddyfile"
      via: "a imagem copia o Caddyfile final que o plano 02-13 escreveu"
      pattern: "Caddyfile"
---

<objective>
Trocar `ops/` de uma máquina vazia com systemd por uma composição de dois contêineres — e
apagar, no mesmo commit em que suas asserções morrem, os nove arquivos que a containerização
aposentou.

**Fatia (MVP, operacional):** ao fim deste plano existe no repositório tudo o que o Coolify
precisa ler para subir o jogo de verdade: duas imagens descritas, uma composição com limites e
volume, e um runbook que um operador consegue seguir. O plano 02-15 constrói essas imagens no
integrador; o 02-12 as promove na caixa.

Purpose: nove dos catorze arquivos de `ops/` descrevem um layout de disco, um supervisor e um
caminho de deploy que **deixaram de existir** (D2-30, mais `litestream.service` por D2-28). Um
arquivo que ninguém executa e continua no repositório é uma armadilha para quem ler o runbook
daqui a seis meses — e o git guarda a história sem precisar do arquivo vivo. Junto com eles
morrem **34 dos 74 casos** de `tests/ops-config.test.ts`, e o piso anti-vacuidade `>= 13` dos
dois blocos que varrem os globs **quebraria em silêncio** quando a contagem caísse, deixando o
bloco de D2-15 vermelho por um motivo que não é o dele.

**Custo real da retirada, medido e barato:** nenhuma dessas units jamais foi instalada em
lugar nenhum. Não há `node`, `rsync`, `litestream` nem `turnserver` na caixa; não há
`/srv/dg2/`; não há `/etc/dg2/env`. Os arquivos morrem no git sem deixar resíduo — que é a
única razão pela qual D2-30 é barata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-migra-o-para-a-vps/02-CONTEXT.md
@.planning/phases/02-migra-o-para-a-vps/02-10-SUMMARY.md
@.planning/phases/02-migra-o-para-a-vps/02-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: As duas imagens, a composição, e as asserções que as prendem</name>
  <files>ops/Dockerfile.web, ops/Dockerfile.api, ops/docker-compose.yml, ops/litestream.yml, ops/probe/docker-compose.yml, tests/ops-config.test.ts</files>
  <read_first>
    - `ops/Caddyfile` — reescrito no plano 02-13; é o arquivo que `ops/Dockerfile.web` copia, e o `{$DG2_UPSTREAM}` dele decide o nome de serviço que a composição precisa ter
    - `ops/litestream.yml` — o arquivo que esta tarefa altera na prosa; leia o cabeçalho inteiro, especialmente o parágrafo do `replica` singular (P-8) e o do `/etc/dg2/env`
    - `apps/server/src/shutdown.ts` — a constante `SHUTDOWN_GRACE_MS` e a sequência de drain; é o número contra o qual o `stop_grace_period` é comparado, e ele é **importado** no teste em vez de copiado
    - `apps/server/src/env.ts` — a tabela `DEFAULTS` depois do plano 02-13: quais chaves existem, quais têm padrão e quais são recusadas em branco. A composição só declara o que precisa divergir do padrão
    - `package.json` da raiz e `apps/server/package.json` — os nomes de workspace e a lista de dependências que o `npm ci` da imagem instala
    - `tests/ops-config.test.ts` — linhas 20-100 (helpers e pisos), 473-573 (o bloco `ops/dg2.service`, cujas propriedades renascem aqui em forma nova), 691-724 (`ops/litestream.yml`) e 1094-1200 (as constantes `NOT_A_TLD`, `NON_ROUTABLE_V4`, `MEMBER_ACCESS`, e o bloco D2-15 que esta tarefa tem de atravessar)
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` § "Code Examples" — `ops/docker-compose.yml` e `ops/Dockerfile.api` na íntegra, com os comentários que explicam cada linha
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` § DM-12, § DM-13, § C-4, § C-6, § C-7 e § "Anti-padrões a evitar" — os prebuilds no tarball do npm, o repasse de sinal do `-exec`, o prazo de parada, a tag móvel, o build implícito e as quatro coisas que não se declaram
  </read_first>
  <action>
    **LEIA ISTO PRIMEIRO — D2-33 (2026-09-10) revoga o bucket, e este plano foi escrito antes
    dela.** Onde o texto abaixo disser `s3`, bucket, ou as quatro variáveis
    `LITESTREAM_BUCKET` / `LITESTREAM_ENDPOINT` / `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`,
    **a decisão nova manda.** Leia `02-CONTEXT.md` § "Emenda de execução — 2026-09-10" inteira
    antes de escrever uma linha. O que muda, item por item:

    1. **O destino do Litestream é `file`, não `s3`.** O `ops/litestream.yml` declara um `replica`
       do tipo `file` com um `path`. O processo, o `replicate -exec`, o repasse de sinal medido em
       DM-13 e o `stop_grace_period: 30s` **continuam idênticos** — D2-28 foi emendada no destino,
       não no mecanismo.
    2. **As quatro variáveis do bucket não existem.** Elas saem do `ops/docker-compose.yml` e do
       painel. Nasce no lugar **uma** variável com o caminho da réplica. O resto do parágrafo das
       referências `${...}` (origem, par de relay) vale igual.
    3. **São DOIS volumes persistentes, não um.** O do banco, que o plano já declara, **e o da
       réplica.** Se o caminho da réplica cair numa camada de contêiner, o primeiro redeploy
       apaga o backup **sem erro nenhum** — é a armadilha que D2-33 registra explicitamente, e
       ela é silenciosa, o que a torna pior que uma falha. Declare os dois, monte os dois só no
       serviço `api`, e escreva o motivo no cabeçalho da composição.
    4. **A justificativa do `ca-certificates` muda, o pacote fica.** O texto abaixo diz que ele
       entra "porque o Litestream fala HTTPS com o bucket" — isso deixa de ser verdade. Mantenha
       o pacote (o Node precisa dele para TLS de saída) e **corrija a razão escrita**, em vez de
       deixar um comentário que justifica uma dependência por um motivo que não existe mais.
    5. **O threat model encolhe de um lado e cresce do outro.** Sai a credencial de bucket
       atravessando o painel; entra o fato de que **a réplica vive no mesmo disco do banco**, de
       modo que uma falha de disco leva os dois. Não invente mitigação para isso — é escolha
       registrada de D2-33, e o que se exige é que esteja escrita, não resolvida.
    6. **O ensaio de restauração (D2-03, exercitado no 02-12) restaura do caminho local**, não do
       bucket. Se algum texto de tarefa ou de `docs/` que você escrever mencionar a origem da
       restauração, é o caminho da réplica.

    Todo o resto do plano — Dockerfiles, compose, limites, `USER`, healthcheck, os nove arquivos
    aposentados, os testes e o piso — vale exatamente como está escrito.

    Comentários de código em inglês. Todo arquivo novo de `ops/` nasce com cabeçalho em prosa —
    não é estilo, é requisito: `read()` exige mais de 200 bytes por arquivo, e um
    `Dockerfile.web` de três linhas fica abaixo do piso. A alternativa (uma exceção de piso por
    arquivo) é pior, e foi exatamente por essa porta que o achado WR-14 entrou.

    **`ops/Dockerfile.web`.** Base `caddy:2.11.4-alpine`, tag de patch exata e nunca `latest`.
    Copia `ops/Caddyfile` para o caminho padrão de configuração da imagem — o `CMD` padrão dela
    já o lê, então nenhum `command` é necessário — e copia `dist/` para a raiz estática que o
    `root *` do Caddyfile aponta. **Nada aqui constrói nada**: o `dist/` vem do artefato que o
    job `test` do CI subiu, e é isso que faz o que é publicado ser byte a byte o que passou pelo
    portão cross-engine (D2-05, preservada por D2-23). Escreva esse parágrafo no cabeçalho, e
    escreva também por que este arquivo **não** declara `USER`: o Caddy escreve no diretório de
    dados dele ao subir, a imagem alpine não traz usuário não-privilegiado com esse diretório
    preparado, e o contêiner não publica porta, não monta volume de dados e não abre banco — o
    risco que `USER` compra no `api` não existe aqui. Deixe registrado como escolha, não como
    esquecimento.

    **`ops/Dockerfile.api`.** Base `node:24.20.0-trixie-slim`, tag de patch exata — Node 24 é o
    LTS ativo e casa com o Debian 13 da caixa; Node 26 é *Current* e fica fora por doutrina.
    Uma camada de `apt-get` instalando exatamente `sqlite3`, `ca-certificates` e `wget`, sem
    recomendados, e limpando a lista de pacotes na mesma camada: `sqlite3` porque
    `tools/ops/restore-verify.mjs` o invoca em somente-leitura, `ca-certificates` porque o
    Litestream fala HTTPS com o bucket, e `wget` porque é o comando do `healthcheck` da
    composição — **não presuma que a base já o traz**; instalar explicitamente custa nada e
    remove uma suposição.
    O binário do Litestream vem do release oficial, **fora do grafo npm**, com sha256 fixado —
    a mesma doutrina que o `ops/litestream.service` aposentado já escrevia. Os valores foram
    medidos — **procedência: os três vêm do campo `digest` que a API de releases do GitHub
    publica para os assets da tag `v0.5.17` de `benbjohnson/litestream`, reconferido em
    2026-09-09 durante a verificação de plano** — e são estes, use-os literalmente: versão
    `0.5.17`; nome do arquivo
    `litestream-0.5.17-linux-x86_64.tar.gz`; sha256
    `cfb371176d164437ae869f8351cfde49bd1804ae71c61923f75c9cba9c9c006d`; e o binário está na
    **raiz do tarball**, com o nome `litestream`, de modo que extrair só ele para
    `/usr/local/bin` funciona. **Atenção: o exemplo da pesquisa usa um nome de arquivo que não
    existe** (`litestream-v0.5.17-linux-amd64.tar.gz`); copiá-lo produz um 404 no build. Baixe
    com `ADD` para um caminho temporário, verifique com `sha256sum -c` numa camada `RUN`
    própria, extraia e apague o tarball.
    Ordem das camadas, e a ordem é o ponto: os manifestos (`package.json` e `package-lock.json`
    da raiz, mais o `package.json` de `apps/server`) são copiados **antes** do bundle, e só
    então roda `npm ci --omit=dev` restrito ao workspace do servidor. Assim a camada de
    `node_modules` é compartilhada entre builds e cada deploy custa alguns MB de camada nova em
    vez de dezenas — que é o que torna a retenção de 5 imagens de D2-24 barata. **Sem
    compilador**: `better-sqlite3@13.0.3` traz os binários pré-compilados dentro do tarball do
    npm, inclusive `linux-x64`, então nada de `python3`, `make` ou `g++`, e o passo manual de
    `node_modules` na caixa que o runbook antigo documentava **deixa de existir**.
    Depois: o bundle do servidor vindo de `dist-server/`, o `ops/litestream.yml` para o caminho
    de configuração do Litestream, e `tools/ops/restore-verify.mjs`.
    Crie o diretório do banco na imagem e **dê-o ao usuário não-root antes de trocar de
    usuário**. Isto não é higiene, é o que impede um defeito de primeiro deploy: um volume
    nomeado vazio herda dono e modo do diretório que existe na imagem, e um diretório
    `root:root` faria o processo abrir o banco sem permissão de escrita — com a migração
    falhando e o contêiner entrando em laço de reinício. Escreva o motivo no comentário.
    `USER` para o usuário não-root que a base já traz. E `ENTRYPOINT` sendo o Litestream em
    modo de réplica com `-exec` envolvendo o Node: medido no código-fonte, ele repassa o
    **sinal exato** ao filho e **espera o filho sair** antes da sincronização final, então o
    desligamento gracioso do plano 02-08 sobrevive sem `tini`, sem `trap` de shell e sem
    supervisor. Escreva isso no cabeçalho com a medição citada, porque a alternativa óbvia (um
    entrypoint de shell com `trap`) é a armadilha clássica de PID 1.

    **`ops/docker-compose.yml`.** A composição que o Coolify lê do repositório. Serviços `web` e
    `api`, ambos referenciando imagens do registro por **`${DG2_IMAGE_TAG}`** — nunca `latest`,
    nunca `main`: uma tag móvel destrói a reversão de D2-24, porque não existe "imagem
    anterior" quando o nome anterior aponta para o conteúdo novo. Ambos com `pull_policy:
    missing`, que é a linha que faz a reversão não usar rede — o Docker só busca no registro o
    que não estiver em disco, e a tag anterior está em disco. Ambos com `restart:
    unless-stopped`.
    `web` declara `depends_on` do `api` e `expose` da porta interna, que é de onde o Coolify
    tira a porta para o label do Traefik, mais `mem_limit` e `cpus` modestos — ele serve arquivo
    estático e repassa.
    `api` declara: o volume nomeado montado no diretório do banco; `stop_grace_period` de
    **30s**, porque o Node drena em até `SHUTDOWN_GRACE_MS` e **só depois** o Litestream faz a
    sincronização final, e o padrão de 10s do Docker cortaria a segunda etapa — o que se perde
    são as últimas escritas de um ledger de moeda, exatamente o que D2-17 existe para impedir;
    as variáveis de ambiente; `mem_limit` e `cpus`; e um `healthcheck` que pede a rota de saúde
    pelo loopback do próprio contêiner.
    As variáveis do `api`: o caminho do banco e a porta interna com os valores literais que a
    imagem espera; `DG2_BIND` com o valor de contêiner, com o comentário explicando que
    `127.0.0.1` seria o loopback **deste** contêiner e o Caddy vive em outro (DM-9), e que o
    padrão do código continua sendo o loopback — esta linha é a exceção declarada em diff,
    exatamente onde ela é revisável; `DG2_RELEASE` recebendo `${DG2_IMAGE_TAG}`, o que faz a
    comparação "o deploy pegou?" ser uma comparação de string contra a rota de saúde, sem
    infraestrutura nova; e as referências `${...}` de origem, do par de relay e das quatro do
    bucket — que o Coolify expõe como campos editáveis no painel (D2-29). O arquivo em git diz
    **quais** chaves existem; o painel diz **o que** elas valem, e é assim que D2-29 se resolve
    sem quebrar D2-15.
    `NODE_OPTIONS` com o teto do heap do V8, e ele anda **junto** com `mem_limit`, sempre: o V8
    dimensiona o old space pela memória da **máquina** — quase 8 GiB aqui — e não pelo limite do
    cgroup, então limitar um sem o outro troca coleta de lixo por OOM-kill. É a mesma armadilha
    P-10 que o `dg2.service` tratava, com outra roupa.
    **Quatro coisas que este arquivo não declara, e cada uma com o comentário do motivo:** nada
    de `networks:` (a doc do Coolify avisa que uma rede própria causa queda intermitente de rota
    no Traefik); nada de `ports:` (nada é publicado no host, e é isso que faz a colisão com a
    8080 do Traefik desaparecer por construção **e** o `0.0.0.0` do `api` continuar sendo
    controle de acesso); nada de `build:` (D2-23 — e é o que faz o log do deploy dizer `pull`);
    e nenhum label do Traefik escrito à mão (o Coolify os gera a partir do FQDN atribuído ao
    serviço, e labels próprios brigariam com o gerador na primeira mudança de domínio).
    Um bloco `volumes` no fim declarando o volume nomeado.

    **Uma consequência do compartilhamento da tag, e ela tem de estar escrita no arquivo.** Os
    dois serviços referenciam **a mesma** `${DG2_IMAGE_TAG}`, então todo deploy muda a string do
    campo `image` dos dois, e o `up -d` recria os dois contêineres — inclusive num deploy que só
    mexeu no cliente. A pesquisa supôs o contrário ("um deploy que muda apenas o cliente não
    reinicia o `api`"); com uma tag só, isso não se sustenta, porque o Compose decide recriação
    pelo hash da definição do serviço e a tag faz parte dela. **O item de discrição "se o servidor
    reinicia em todo deploy" fica resolvido na direção oposta, por escolha:** ele reinicia, a
    migração é idempotente e o drain é gracioso, então o custo hoje é alguns segundos. A
    alternativa — **duas** variáveis de tag, uma por serviço — fica nomeada no comentário com a
    condição que a traria de volta: ela passa a valer quando derrubar as WebSockets do signaling
    custar algo, ou seja na fase 3 ou 4, e não antes. Escreva as duas frases, porque a diferença
    entre "decidimos assim" e "não percebemos" é só o comentário.

    **`ops/litestream.yml`.** O `replica` no singular, o prefixo fixo e as referências de
    ambiente ficam **intocados**. Muda a prosa: o parágrafo que manda instalar o arquivo em
    `/etc/litestream.yml` na caixa passa a dizer que ele entra **dentro da imagem** por `COPY`;
    o parágrafo que aponta os valores para `/etc/dg2/env` lido pelo systemd passa a apontar para
    as variáveis do app no painel do Coolify (D2-29); e nasce um parágrafo dizendo que o
    caminho do banco declarado aqui é o **mesmo** que a composição passa ao servidor, e que um
    teste compara os dois — porque duas cópias de um caminho que precisam concordar estão a uma
    edição de não concordar, e o sintoma é um backup replicando um arquivo que ninguém escreve.

    **Apagar `ops/probe/` inteiro.** É o andaime do plano 02-04, que já cumpriu o papel de provar
    A1. Sob D2-32 nada dispara deploy sozinho, então o recurso do Coolify apontando para um
    caminho que deixou de existir é inerte até alguém promover à mão — e é o plano 02-12 que
    repõe o ponteiro para `ops/docker-compose.yml`. Registre isso no SUMMARY para que ninguém
    descubra o ponteiro quebrado por acidente.

    **Asserções novas em `tests/ops-config.test.ts`.** Primeiro, três exceções nomeadas sem as
    quais o bloco D2-15 fica vermelho contra os arquivos corretos — e as três seguem a doutrina
    de exatidão que a constante das faixas reservadas já usa, listando tokens literais em vez de
    afrouxar a regra:
    (a) uma constante nova de **hosts de artefato público**, com exatamente dois tokens — o
    registro de imagens e o host de onde o binário do Litestream é baixado. O comentário explica
    a distinção que D2-15 de fato faz: a regra proíbe endereço porque endereço diz **onde esta
    caixa mora**, e um registro público de artefatos diz de onde o binário vem. O domínio do
    jogo continua recusado, que é a prova de que a exceção não abriu buraco.
    (b) os dois sufixos dos Dockerfiles entram na lista de "não é TLD" — pela mesma razão que
    `conf`, `service` e `timer` já estão lá: são extensões que este subsistema escreve, e a
    asserção de host confunde `Dockerfile.api` com um domínio de dois rótulos.
    (c) uma constante de **valores internos de contêiner**, com exatamente dois tokens — o
    caminho do banco e o nome-de-serviço-mais-porta do upstream —, excusada apenas na asserção
    de "nenhuma chave de env aparece com valor literal". O comentário: um caminho dentro do
    contêiner e um nome de serviço do compose não dizem onde a caixa mora; o segundo sequer
    resolve fora da rede daquela composição. Exatidão de token, como nas faixas reservadas: o
    caminho exato é excusado, um caminho vizinho não.
    Depois, as propriedades que morreram com o `dg2.service` e o `litestream.service`, renascidas
    em forma nova sobre a composição — cada uma com um comentário dizendo de qual asserção morta
    ela é herdeira:
    limita a memória do cgroup **e** o heap do V8, nunca só um, e o segundo é menor que o
    primeiro (herdeira de P-10);
    **nenhum serviço declara `ports:`** — é a asserção certa desta arquitetura para "não publica
    a API fora do loopback", e é dela que depende a segurança do `DG2_BIND` de contêiner;
    o `stop_grace_period` do `api` é **maior** que `SHUTDOWN_GRACE_MS`, importado de
    `apps/server/src/shutdown.ts` e não copiado — a forma de importar continua sendo a certa
    (herdeira de WR-07);
    o `Dockerfile.api` declara `USER` e o valor não é root;
    toda imagem é referenciada por `${DG2_IMAGE_TAG}` e **nenhuma** por `latest` ou `main`
    (herdeira de "arranca pelo symlink que o rollback move", e a defesa de C-6);
    todo serviço declara `pull_policy: missing`;
    a composição **não** declara `networks:` nem `build:`;
    o `ENTRYPOINT` do `Dockerfile.api` é o Litestream com `-exec`, e o Node **não** é o primeiro
    argumento do entrypoint (herdeira de "é irmã e não filha");
    e a asserção de ligação: o caminho do banco que a composição passa ao servidor é **a mesma
    string** que `ops/litestream.yml` replica, comparada entre os dois arquivos e não contra um
    literal escrito no teste.
    A asserção existente de `ops/litestream.yml` que fala de `dg2.service` muda de sujeito para
    o serviço `api` da composição.
    **Não** mexa nos dois pisos `>= 13` nesta tarefa: com três arquivos novos e nenhuma remoção
    ainda, a contagem sobe, e o piso desce na Task 2 — no mesmo commit em que a contagem cai.
  </action>
  <verify>
    <automated>npx vitest run tests/ops-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ops/Dockerfile.web && test -f ops/Dockerfile.api && test -f ops/docker-compose.yml` sai 0
    - `test -d ops/probe` sai diferente de 0
    - `grep -c 'networks:' ops/docker-compose.yml` retorna 0; `grep -c 'ports:' ops/docker-compose.yml` retorna 0; `grep -c 'build:' ops/docker-compose.yml` retorna 0
    - `grep -c 'pull_policy: missing' ops/docker-compose.yml` retorna 2
    - `grep -cE ':latest|:main' ops/docker-compose.yml` retorna 0 e `grep -c '${DG2_IMAGE_TAG}' ops/docker-compose.yml` é 2 ou mais
    - `grep -c 'stop_grace_period: 30s' ops/docker-compose.yml` retorna 1
    - `grep -c 'max-old-space-size' ops/docker-compose.yml` retorna 1 e `grep -c 'mem_limit' ops/docker-compose.yml` retorna 2
    - `grep -c 'cfb371176d164437ae869f8351cfde49bd1804ae71c61923f75c9cba9c9c006d' ops/Dockerfile.api` retorna 1
    - `grep -c 'litestream-0.5.17-linux-x86_64.tar.gz' ops/Dockerfile.api` é 1 ou mais e `grep -c 'linux-amd64' ops/Dockerfile.api` retorna 0
    - `grep -cE '^USER ' ops/Dockerfile.api` retorna 1 e `grep -cE '^USER +root' ops/Dockerfile.api` retorna 0
    - `grep -cE '^ENTRYPOINT.*litestream' ops/Dockerfile.api` retorna 1
    - `grep -c 'wget' ops/Dockerfile.api` é 1 ou mais (instalado explicitamente, não presumido)
    - `grep -ci 'duas variáveis de tag\|uma tag só\|recria os dois' ops/docker-compose.yml` é 1 ou mais (a consequência da tag compartilhada está escrita no arquivo)
    - Cada um dos três arquivos novos tem mais de 200 bytes e, depois de removidas as linhas iniciadas por `#`, mais de 50 bytes
    - `npx vitest run tests/ops-config.test.ts` sai 0 e `npm test` sai 0
    - `git grep -nE '([0-9]{1,3}\.){3}[0-9]{1,3}' -- ops/ | grep -v '127\.0\.0\.1' | grep -v '0\.0\.0\.0' | grep -vE '10\.0\.0\.0|10\.255|100\.64|100\.127|127\.0\.0\.0|127\.255|169\.254|172\.16|172\.31|192\.168|0\.255|255\.255'` não imprime nada
  </acceptance_criteria>
  <done>A composição e as duas imagens existem, cada propriedade que o `dg2.service` garantia tem herdeira asserida sobre a nova arquitetura, e o andaime que provou A1 saiu do repositório.</done>
</task>

<task type="auto">
  <name>Task 2: Os nove arquivos e os 34 casos, no mesmo commit — e o piso que corresponde ao conteúdo</name>
  <files>ops/deploy.sh, ops/rollback.sh, ops/deploy-forced.sh, ops/prune-releases.sh, ops/dg2.service, ops/litestream.service, ops/cert-check.sh, ops/cert-check.service, ops/cert-check.timer, tests/ops-config.test.ts</files>
  <read_first>
    - `tests/ops-config.test.ts` — os blocos que morrem inteiros: `scripts de ops/` (18 casos), `ops/dg2.service` (7), `ops/litestream.service` (3) e `cert-check` (6); mais a constante `SCRIPTS` e os dois pisos `toBeGreaterThanOrEqual(13)`, nas linhas ~1172 e ~1333
    - `ops/dg2.service` — as sete propriedades que ele garantia; a Task 1 já as renasceu sobre a composição, e esta tarefa precisa conferir que nenhuma sumiu antes de apagar o arquivo
    - `ops/litestream.service` — as três propriedades; a de "é irmã e não filha" já renasceu na Task 1 como o `ENTRYPOINT` com `-exec`
    - `.planning/phases/02-migra-o-para-a-vps/02-CONTEXT.md` — D2-30 na íntegra (a lista literal e o motivo) e D2-28 (que é quem mata o `litestream.service`, fora da lista literal)
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` § DM-20 — a contagem por bloco, o piso que quebra em silêncio, e a inversão da asserção da lista `SCRIPTS`
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` § "Runtime State Inventory" — a prova de que nenhuma dessas units foi instalada em lugar nenhum, que é o que torna a remoção barata
  </read_first>
  <action>
    Remover e reescrever **no mesmo commit**. É texto literal de D2-30, e o motivo é operacional:
    uma janela em que a suíte está vermelha por um motivo que não é defeito ensina todo mundo a
    ignorar suíte vermelha, que é o oposto do que estes portões compram.

    **Antes de apagar, conferir a herança.** Percorra as sete propriedades de `ops/dg2.service` e
    as três de `ops/litestream.service` e confirme, uma a uma, que cada uma ou tem herdeira
    asserida sobre a composição (escrita na Task 1) ou morreu **por decisão registrada**. As duas
    que morrem sem herdeira, e o motivo tem de estar no SUMMARY: `StartLimitIntervalSec` /
    `StartLimitBurst`, porque o Docker não tem equivalente — **o systemd chegava a `failed` e
    parava; o Docker tenta para sempre** —, e `StateDirectory`, porque o volume nomeado do
    Compose ocupa o lugar dela. A primeira é uma perda real de P-9: o que fecha a corrente de
    alarme passa a ser o `healthcheck` da composição mais o monitor externo de D2-21, e não mais
    o estado de uma unit. Isso vai para o runbook na Task 3.

    **Apagar os nove arquivos** com `git rm`: os quatro scripts de deploy e poda, a unit do
    servidor, a unit do Litestream, e os três de `cert-check`. Oito estão na lista literal de
    D2-30; o `litestream.service` **não está**, e morre por D2-28 — o Litestream deixou de ser
    unit e virou PID 1 do contêiner. Registre essa extensão explicitamente no SUMMARY, com a
    frase de D2-30 como justificativa, para que não pareça um arquivo esquecido.

    **Remover os 34 casos correspondentes**: os dezoito do bloco de scripts, os sete do
    `dg2.service`, os três do `litestream.service` e os seis de `cert-check`. Os quatro
    `describe` ficam vazios e saem inteiros.

    **Inverter a asserção da lista `SCRIPTS` em vez de apagá-la.** A lista fica vazia, e a
    asserção passa a exigir que **`ops/` não tenha nenhum `.sh`** — é uma propriedade que D2-30
    quer preservar (quem executa agora é o Docker), e ela impede alguém de reintroduzir um script
    de deploy por hábito. Escreva o comentário dizendo isso, porque uma asserção invertida sem
    explicação parece resíduo.

    **Descer os dois pisos anti-vacuidade de 13 para 9**, nas duas ocorrências — o bloco D2-15 e
    o bloco de LF/CRLF. A contagem depois desta tarefa: `ops/` fica com Caddyfile, README.md,
    coturn-dropin.conf, litestream.yml, turnserver.conf e os três arquivos de contêiner, oito ao
    todo; `tools/ops/` fica com o verificador de restauração e, se o plano 02-04 escolheu a
    opção do script, também o comando de deploy — nove ou dez entradas. O piso é `9` para valer
    nos dois casos. Escreva o número no comentário junto com a conta, porque o próximo a mexer
    em `ops/` precisa saber de onde ele veio. **Confirme a conta contra o diretório real antes de
    fixar o número** — se `tools/ops/deploy.mjs` não existir, o total é nove exatamente, e o
    piso tem de continuar passando.

    **Prova por remoção, e ela é obrigatória neste plano.** Depois de descer o piso, apague
    temporariamente um arquivo de `ops/` e confirme que o bloco D2-15 fica **vermelho pelo piso**
    e não verde por vacuidade; restaure e confirme o verde. Registre o resultado no SUMMARY. Sem
    essa prova, o piso é decoração — que é exatamente o defeito que WR-14 encontrou e que DM-20
    avisou que voltaria.
  </action>
  <verify>
    <automated>npx vitest run tests/ops-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls ops/*.sh 2>/dev/null | wc -l` retorna 0
    - `ls ops/*.service ops/*.timer 2>/dev/null | wc -l` retorna 0
    - `ls ops/ | wc -l` retorna 8
    - `grep -c 'toBeGreaterThanOrEqual(13)' tests/ops-config.test.ts` retorna 0 e `grep -c 'toBeGreaterThanOrEqual(9)' tests/ops-config.test.ts` retorna 2
    - `git grep -n 'dg2.service\|litestream.service\|cert-check\|deploy-forced\|prune-releases' -- tests/ops-config.test.ts` não imprime nada
    - `npx vitest run tests/ops-config.test.ts` sai 0, e a contagem de casos do arquivo caiu em ao menos 30 em relação aos 74 anteriores
    - `npm test` sai 0 e `npm run lint` sai 0
    - A prova por remoção está registrada no SUMMARY: com um arquivo de `ops/` removido o bloco D2-15 fica vermelho **pelo piso**, com a mensagem do piso, e volta a verde ao restaurar
  </acceptance_criteria>
  <done>Nenhum arquivo que ninguém executa continua em `ops/`, nenhuma asserção ficou órfã, e o piso anti-vacuidade corresponde ao conteúdo real do diretório — provado por remoção, não por leitura.</done>
</task>

<task type="auto">
  <name>Task 3: O runbook reescrito para a caixa que existe</name>
  <files>ops/README.md, tests/ops-config.test.ts</files>
  <read_first>
    - `ops/README.md` — o arquivo inteiro que esta tarefa reescreve. As seções 2, 3, 4, 5, 6, 7, 9, 10 e 11 descrevem uma máquina que não existe; a 12 fica, corrigida; a 1 e a 8 quase sobrevivem
    - `tests/ops-config.test.ts` — o bloco `describe('ops/README.md')` (11 casos, dos quais 9 mudam) e a constante `ENV_KEYS` logo acima dele
    - `docs/OPERACAO.md` — escrito e preenchido pelo plano 02-04: o inventário da caixa, a regra de ouro sobre alterações no host, as quatro regras de firewall já abertas, a configuração de limpeza lida, e a frase de reconciliação do critério 4. O runbook **não repete** esses valores; ele aponta para lá
    - `ops/docker-compose.yml` e `ops/Dockerfile.api` — escritos na Task 1; o runbook descreve como operá-los
    - `ops/turnserver.conf` — alterado pelo plano 02-04: a faixa de relay declarada é a que a §12 do runbook tem de mandar abrir
    - `tools/README.md` — a convenção de documentação deste repositório: decisões numeradas, cada uma com o motivo, escritas para o leitor futuro que vai querer mudá-las
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` § "O delta exato em `ops/`" (a linha do README), § DM-16, § DM-17, § "Convivência com o vizinho" e § "Vigilância"
  </read_first>
  <action>
    Reescrever `ops/README.md` para a caixa containerizada, mantendo a forma que ele já tem —
    seções numeradas, cada decisão com o motivo, tom de runbook executável por quem nunca viu
    esta máquina. Português, e **nenhum endereço, domínio, IP ou segredo** (D2-15, asserido).

    **O que sai por completo:** o layout de disco de releases por sha com symlink; o passo manual
    de instalar `node_modules` de produção na caixa (some por DM-12 — os prebuilds vêm no tarball
    do npm); a criação dos dois usuários e a chave de deploy com `command=` forçado; o arquivo
    `/etc/dg2/env` como lugar único dos segredos; o drop-in de ambiente do Caddy; o
    procedimento de publicar e reverter por script; a seção da porta 443 como propriedade do
    Caddy; a instalação e ordem das units do systemd do jogo.

    **O que nasce, e é o miolo do runbook novo:**
    o recurso do Coolify — tipo, source, build pack e o ponteiro para `ops/docker-compose.yml`,
    mais a atribuição do domínio ao serviço `web` e a conferência de que o Coolify gerou o par de
    roteadores do Traefik;
    as variáveis do app no painel, listadas por **nome** e sem valores, com a frase em voz alta
    de D2-29: o `static-auth-secret` do relay passa a existir em **dois lugares de naturezas
    diferentes** — um arquivo no host e um painel web —, e trocar num só faz o relay recusar toda
    credencial, com o sintoma "um amigo específico nunca entra", indistinguível de NAT ruim;
    publicar, na forma que o plano 02-04 decidiu, e **reverter apontando `DG2_IMAGE_TAG` para o
    sha anterior e redeployando**, com a frase de que isso não usa rede porque o `pull_policy:
    missing` só busca no registro o que não estiver em disco;
    a **retenção de 5 imagens por serviço**, herdada do `prune-releases.sh` aposentado — não é
    número novo, é continuidade de operação — e, junto, a degradação honesta que D2-24 traz: o
    symlink era garantia estrutural, a imagem local é garantia **probabilística**, dependente de
    uma rotina de limpeza que este projeto não controla e que é configuração **do servidor**,
    compartilhada com o vizinho, que se lê e não se mexe;
    `sudo` em **todo** comando de Docker, porque o usuário de deploy não está no grupo `docker` —
    é decisão razoável do vizinho (estar no grupo `docker` equivale a root) e não se mexe, mas o
    operador tropeça no primeiro comando se o runbook não disser;
    a supervisão, com a diferença que importa escrita em voz alta: **o systemd chegava a `failed`
    e parava; o Docker tenta para sempre.** O que fecha a corrente de alarme passa a ser o
    `healthcheck` da composição mais o monitor externo, e não o estado de uma unit — é P-9 com
    outra roupa, e um crash-loop por migração quebrada agora é invisível se ninguém olhar o
    monitor;
    a convivência com o vizinho: os tetos de memória e CPU existem para que um vazamento no
    signaling da fase 3 não mate nem a API nem a produção do outro projeto — o `mem_limit` é do
    cgroup e o OOM-killer mata dentro do cgroup do jogo —, nada é publicado no host, e nenhuma
    rede própria é declarada;
    o backup e o ensaio de restauração: o Litestream deixou de ser unit e virou PID 1 do
    contêiner (D2-28), e o ensaio de D2-03 roda num **contêiner descartável** montando o volume
    em somente-leitura e invocando `tools/ops/restore-verify.mjs` com o entrypoint sobrescrito —
    "ambiente limpo", que é o texto literal do critério 4, deixa de ser um diretório temporário
    na mesma máquina e passa a ser um contêiner novo. O ensaio **não vira timer recorrente**
    (D2-03);
    a vigilância: a perna local morreu com o `cert-check` (D2-30) porque o certificado passou a
    ser do Traefik, que renova sozinho — e o Let's Encrypt encerrou o aviso por e-mail, então
    **ninguém mais avisa de graça**. Sobra a perna externa de D2-21, e ela precisa fazer as
    **duas** coisas: disponibilidade por keyword na rota de saúde **e alerta de expiração de
    certificado com 30 dias**. Escreva que o alarme de 30 dias de D2-16 **mudou de dono** e que
    trocar de serviço um dia leva o limiar junto;
    e uma seção de riscos herdados, apontando para `docs/OPERACAO.md`: a alcançabilidade do
    caminho de deploy depende de configuração de outro projeto, e uma queda dela é causa possível
    de "o deploy parou de funcionar".

    **§12, coturn — fica, corrigida.** O passo de firewall passa a mandar abrir **quatro**
    regras: as três de sinalização e a **faixa de relay** declarada em `ops/turnserver.conf`.
    Escreva no mesmo parágrafo, e não em outro, que declarar a faixa sem abri-la e abri-la sem
    declará-la produzem o mesmo sintoma — as duas metades andam juntas. Registre que a versão
    instalada é a do distribuidor do Debian, que isso é escolha (o distribuidor mantém as
    correções de segurança) e não acidente, e que `no-cli` continua sendo a mitigação certa
    porque os CVEs históricos do coturn moraram na interface de gestão. O bloco do segredo em
    dois arquivos e o parágrafo de orçamento ficam, com o orçamento corrigido: a caixa não é a
    KVM 2 de 2 GB de D2-19, é uma máquina de ~8 GiB partilhada com outro projeto, e os limites
    continuam obrigatórios pelo motivo original. Se o plano 02-04 já abriu as regras, diga isso
    e aponte para o registro em `docs/OPERACAO.md` — o runbook documenta um ato já praticado, e
    a §12 continua servindo para quem reconstruir a caixa do zero.

    **Asserções em `tests/ops-config.test.ts`.** Atualize `ENV_KEYS`: sai a chave do domínio
    (não existe mais em lugar nenhum — o domínio vive no painel do Coolify como FQDN do recurso)
    e entra a chave da tag de imagem. As nove asserções do bloco do README que perseguem texto
    morto trocam de alvo: em vez do `EnvironmentFile` que o reload não relê, o `node_modules` de
    produção, a instalação das units e o dono root dos arquivos que a chave de deploy não pode
    reescrever, passam a perseguir — o ponteiro do compose no recurso do Coolify; `sudo docker`;
    a retenção de 5; a reversão por `DG2_IMAGE_TAG`; as quatro regras de firewall do coturn
    incluindo a faixa; o alerta de expiração de certificado de 30 dias no monitor externo; a
    frase de que o Docker tenta para sempre onde o systemd chegava a `failed`; e o contêiner
    descartável do ensaio de restauração. **Sobrevivem intactas** as duas que já eram sobre
    propriedades e não sobre a máquina: que o runbook nomeia todas as chaves de configuração do
    app, e que §12 escreve que o segredo mora em dois lugares — esta última continua contando
    ocorrências, e o número mínimo não desce.
    Cuidado com a auto-invalidação: várias dessas asserções perseguem texto que também aparece
    em prosa explicativa. Use a mesma disciplina do arquivo — `read()` quando a propriedade
    **vive** num comentário, `code()` quando ela vive no conteúdo — e, onde contar linhas, conte
    depois de filtrar as linhas iniciadas por `#`.
  </action>
  <verify>
    <automated>npx vitest run tests/ops-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sudo docker' ops/README.md` é 3 ou mais
    - `grep -c 'DG2_IMAGE_TAG' ops/README.md` é 2 ou mais
    - `grep -c 'srv/dg2' ops/README.md` retorna 0 e `grep -c 'etc/dg2/env' ops/README.md` retorna 0
    - `grep -c 'dg2.service\|cert-check\|deploy.sh\|rollback.sh\|prune-releases' ops/README.md` retorna 0
    - `grep -c '49200' ops/README.md` é 1 ou mais e `grep -c '3478' ops/README.md` é 1 ou mais
    - `grep -c '30 dias' ops/README.md` é 1 ou mais
    - `grep -c 'restore-verify' ops/README.md` é 1 ou mais
    - `grep -c 'DG2_TURN_SECRET' ops/README.md` é 2 ou mais (o par que existe em dois lugares continua contado)
    - `ops/README.md` tem 300 linhas ou mais
    - `git grep -nE '([0-9]{1,3}\.){3}[0-9]{1,3}' -- ops/README.md | grep -v '127\.0\.0\.1' | grep -v '0\.0\.0\.0'` não imprime nada
    - `npx vitest run tests/ops-config.test.ts` sai 0, com o bloco `ops/README.md` tendo ao menos 10 casos
    - `npm test` sai 0
  </acceptance_criteria>
  <done>Um operador que nunca viu esta caixa consegue criar o recurso, publicar, reverter, restaurar e subir o relay lendo só este arquivo e o registro de operação — e nada nele descreve uma máquina que não existe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro de imagens → caixa | tudo o que a composição referencia é puxado e executado na máquina que hospeda produção de outro projeto |
| release do Litestream → imagem | um binário de terceiro entra na imagem que abre o banco |
| contêiner do jogo → host e vizinho | dois contêineres partilham kernel, disco e dois vCPU com o infraKring |
| painel do Coolify → contêiner | as variáveis do app, inclusive credenciais do bucket e o segredo do relay, chegam por ali |
| repositório público → internet | `ops/` inteiro é legível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-SC | Tampering | binário do Litestream e imagens base | mitigate | Litestream fixado por **sha256 medido** (`cfb3711…c006d`) e verificado numa camada `RUN` própria antes de extrair — um tarball substituído no release falha o build em vez de entrar na imagem. Imagens base fixadas em **tag de patch exata**, nunca `latest`; a melhoria conhecida (fixar por digest) fica registrada no runbook em vez de descoberta depois. **Nenhum pacote npm, PyPI ou cargo novo é instalado nesta fase** — o `npm ci` da imagem instala exatamente o que o `package-lock.json` já trava |
| T-2-ROLLBACK | Denial of Service | reversão por imagem (D2-24) | mitigate | Tag por sha em toda referência e recusa asserida de `latest`/`main` (C-6); `pull_policy: missing` para que voltar não use rede; retenção de 5 registrada. **Degradação honesta declarada:** a garantia deixou de ser estrutural e passou a ser probabilística, dependente de uma limpeza automática que este projeto não controla — está escrita no runbook em vez de descoberta na noite em que importa |
| T-2-BIND | Elevation of Privilege | `DG2_BIND=0.0.0.0` no serviço `api` | mitigate | **Nenhum serviço declara `ports:`** — asserido. Sem publicação no host, a porta do contêiner não atravessa NAT nem o UFW, e a única origem capaz de alcançá-la é a rede bridge que o Coolify criou. É a propriedade estrutural da qual depende a segurança da linha que o plano 02-13 tornou configurável |
| T-2-DATA | Tampering | volume do SQLite | mitigate | O diretório do banco nasce na imagem com dono não-root, de modo que o volume nomeado herde esse dono; o processo roda como usuário não-root (`USER` asserido); o volume é montado **só** no serviço `api` |
| T-2-BACKUP | Denial of Service | sincronização final do Litestream | mitigate | `stop_grace_period` de 30s, asserido como **maior** que `SHUTDOWN_GRACE_MS` importado de `shutdown.ts`. Com o padrão de 10s o Docker mataria com SIGKILL antes da sincronização final, e o que se perde são as últimas escritas de um ledger de moeda |
| T-2-MEM | Denial of Service | vizinho na mesma caixa | mitigate | `mem_limit` **e** `NODE_OPTIONS` com teto de heap menor, asserido como par — o V8 dimensiona o old space pela memória da máquina e não pelo cgroup. `cpus` limita o consumo sob crash-loop. O OOM-killer age dentro do cgroup do jogo |
| T-2-LOOP | Denial of Service | crash-loop por migração quebrada | accept | **Perda declarada de P-9:** o Docker tenta reiniciar para sempre onde o systemd chegava a `failed` e parava. Não há equivalente a `StartLimitBurst` no Compose, e inventar um supervisor contradiz D2-22. A corrente de alarme passa a ser o `healthcheck` da composição mais o monitor externo de D2-21 — e por isso o monitor deixou de ser conforto e virou parte do critério 1 |
| T-2-SECRET | Information Disclosure | variáveis do app | mitigate | O arquivo em git diz **quais** chaves existem, com referências `${...}`; o painel diz o que elas valem (D2-29). O bloco D2-15 de `tests/ops-config.test.ts` passa a cobrir a composição e os dois Dockerfiles de graça pelo glob, e as três exceções nomeadas são **listas de tokens exatos**, não afrouxamento de regra — o domínio do jogo continua recusado |
| T-2-BUILD | Tampering | integridade do artefato publicado | mitigate | Nenhum serviço declara `build:` e nenhuma imagem roda `npm run build`: as imagens **copiam** os artefatos que passaram pelo portão cross-engine (D2-05/D2-23). Um `Dockerfile` multi-stage que reconstruísse seria mais curto de escrever e anularia a fase 1 inteira |
| T-2-NEIGHBOR | Denial of Service | rede do Traefik | mitigate | Nenhum `networks:` declarado: a doc do Coolify registra que uma rede própria causa queda intermitente de rota, e a rota que cairia é a que o vizinho também usa |
</threat_model>

<verification>
- `npm test` verde, com `tests/ops-config.test.ts` reescrito
- `npm run lint` verde
- `ls ops/` mostra exatamente oito entradas e nenhuma delas é `.sh`, `.service` ou `.timer`
- `git grep -n 'srv/dg2\|etc/dg2/env\|deploy-forced\|prune-releases\|cert-check' -- ops/ tests/ops-config.test.ts` não imprime nada
- Prova por remoção do piso registrada no SUMMARY (bloco D2-15 vermelho **pelo piso**, não verde por vacuidade)
- Prova por remoção de `trusted_proxies` continua valendo do plano 02-13, e o SUMMARY registra que a asserção sobreviveu à reescrita do arquivo de teste
</verification>

<success_criteria>
`ops/` descreve uma composição de dois contêineres que o Coolify consegue ler, com limites,
volume, retenção e desligamento gracioso preservados; os nove arquivos aposentados saíram junto
com suas 34 asserções, sem deixar propriedade órfã nem piso anti-vacuidade descolado do
conteúdo; e o runbook é executável por um operador que nunca viu esta caixa.
</success_criteria>

<output>
Create `.planning/phases/02-migra-o-para-a-vps/02-14-SUMMARY.md` when done
</output>

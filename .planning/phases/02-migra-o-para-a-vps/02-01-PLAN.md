---
phase: 02-migra-o-para-a-vps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - tests/workflows.test.ts
autonomous: false
requirements: [INFRA-01]

user_setup:
  - service: github
    why: "D2-05 põe a publicação no CI. Sem repositório remoto o `ci.yml` nunca roda, e DM-1 mediu que ele nunca rodou (161 commits, `git remote -v` vazio)."
    dashboard_config:
      - task: "Confirmar que GitHub Pages permanece DESABILITADO no repositório novo"
        location: "GitHub → Settings → Pages"

must_haves:
  truths:
    - "O `ci.yml` já rodou num runner do GitHub e terminou verde"
    - "Nenhum workflow do repositório publica no GitHub Pages"
    - "O GitHub Pages do repositório nunca foi habilitado"
    - "Um workflow que reintroduza publicação no Pages faz um teste do repositório ficar vermelho"
  artifacts:
    - path: "tests/workflows.test.ts"
      provides: "prova executável de que existe um único alvo de deploy (INFRA-01)"
      contains: "upload-pages-artifact"
      min_lines: 30
  key_links:
    - from: "tests/workflows.test.ts"
      to: ".github/workflows/"
      via: "import.meta.glob raw eager"
      pattern: "import\\.meta\\.glob.*\\.github/workflows"
---

<objective>
Publicar o repositório no GitHub e ver os oito portões do `ci.yml` verdes num runner —
apagando o `deploy.yml` **antes** do primeiro push, para que o primeiro deploy do
DungeonGuys2 no GitHub Pages nunca aconteça.

**Fatia (MVP):** depois deste plano existe um portão de integração real. Antes dele, "o CI
passa" era uma hipótese testada só na máquina do desenvolvedor, em Windows — e D2-05 faz
todo o resto da fase depender desse portão.

Purpose: DM-1 mediu que `git remote -v` está vazio, que
`api.github.com/repos/gustavoktausend/DungeonGuys2` responde 404 e que
`gustavoktausend.github.io/DungeonGuys2/` responde 404. O `ci.yml` que a fase 1 inteira
construiu nunca rodou fora desta máquina. D2-18 fecha a outra ponta: como nunca houve URL
de onde instalar o PWA, INFRA-01 é satisfeito por **não haver espelho para matar** — e isso
passa a ser provado por teste, não por promessa.

Output: `deploy.yml` apagado, `tests/workflows.test.ts` criado, repositório criado e
empurrado, execução verde do `ci.yml` registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-migra-o-para-a-vps/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apagar o `deploy.yml` e transformar INFRA-01 em teste</name>
  <files>.github/workflows/deploy.yml, tests/workflows.test.ts</files>
  <read_first>
    - `.github/workflows/deploy.yml` — o job inteiro que vai embora (Node 20, `lint`/`test`/`build` duplicados, `upload-pages-artifact@v3`, `deploy-pages@v4`)
    - `.github/workflows/ci.yml` — o workflow que fica, para saber o que a contagem exata do teste deve encontrar
    - `tests/purity.test.ts` — linhas 21-29 (glob raw), 31-40 e 81-83 (contagem exata como guarda anti-vacuidade), 94-104 (coleta de violações em lista)
    - `.planning/phases/02-migra-o-para-a-vps/02-PATTERNS.md` — linhas 229-303 (`tests/build-base.test.ts` e `tests/workflows.test.ts`), em especial a nota de **não** usar `tests/scan.ts` aqui
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` — linhas 331-345 (DM-4: `needs:` não atravessa workflows)
  </read_first>
  <action>
    Apagar `.github/workflows/deploy.yml` por inteiro. Ele é substituído por um job `deploy`
    dentro do `ci.yml` no plano 02-11 (DM-4: `jobs.<id>.needs` só referencia jobs do mesmo
    workflow, então encadear dois arquivos exigiria `workflow_run`, que lê o workflow da
    branch padrão e não garante o que D2-05 quer). Apagar **agora**, antes do primeiro push,
    é o que impede que o primeiro deploy do projeto no GitHub Pages seja acidentalmente
    também o único (D2-18).

    Criar `tests/workflows.test.ts` seguindo a forma de `tests/purity.test.ts`:

    - Ler os workflows com `import.meta.glob<string>('../.github/workflows/*.yml', { query: '?raw', import: 'default', eager: true })` — nunca `node:fs`, porque o `types` do
      `tsconfig.json` da raiz é `["vite/client"]` e `tools/README.md` §4 proíbe mexer nisso.
    - Guarda anti-vacuidade **antes** de qualquer asserção de conteúdo: `expect(Object.keys(FILES).length).toBe(1)` e `expect(FILES['../.github/workflows/ci.yml']).toBeTypeOf('string')`.
      Contagem exata, não mínimo: um glob vazio passaria em silêncio, e um `deploy.yml`
      ressuscitado tem de reprovar por existir, não só por conteúdo.
    - Padrões proibidos, como lista de literais: `upload-pages-artifact`, `deploy-pages`,
      `configure-pages`, `github-pages`. Coletar as violações numa lista `bad: string[]` no
      formato `${caminho}: ${match}` e fechar com `expect(bad).toEqual([])`, para que a falha
      diga **qual** arquivo e **qual** padrão.
    - **Não** remover comentários antes de assertar. `tests/scan.ts` remove comentários de
      TypeScript (`//`, `/* */`), e `#` de YAML não é nenhum dos dois; além disso um
      `deploy-pages` comentado ainda é um workflow que alguém vai descomentar. Aqui a prosa
      não é isenta, e isso deve estar escrito num comentário do arquivo.
    - Nomes em português: `describe('alvo único de deploy (INFRA-01)')`, com `it` do tipo
      `'nenhum workflow publica no GitHub Pages'`.
    - Comentários de código em inglês, como o resto do repositório.
  </action>
  <verify>
    <automated>npx vitest run tests/workflows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -e .github/workflows/deploy.yml` retorna 1 (o arquivo não existe)
    - `grep -rn 'deploy-pages\|upload-pages-artifact\|configure-pages' .github/` não imprime nada
    - `npx vitest run tests/workflows.test.ts` sai 0
    - Prova de recusa (o portão reprova de verdade): `printf '\n# upload-pages-artifact\n' >> .github/workflows/ci.yml && npx vitest run tests/workflows.test.ts; echo $?` imprime `1`; depois `git checkout .github/workflows/ci.yml` restaura o arquivo e o teste volta a sair 0
    - `tests/workflows.test.ts` contém a asserção de contagem exata `toBe(1)` antes de qualquer asserção de conteúdo
    - `npm test` continua verde por inteiro
  </acceptance_criteria>
  <done>O repositório não tem caminho para o GitHub Pages, e isso é reprovável por um comando.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Nome e visibilidade do repositório</name>
  <decision>Sob que nome e com que visibilidade o repositório é criado no GitHub.</decision>
  <context>
    DM-1 mediu: 161 commits locais, nenhum remote, nenhum upstream, e
    `gustavoktausend/DungeonGuys2` não existe. A conta tem 23 repositórios, todos públicos,
    e contém `DungeonGuys` (o jogo original, vivo). O `gh` CLI está instalado nesta máquina
    (2.83.2).

    O que a visibilidade decide de verdade:
    - **Minutos de Actions.** Repositório público tem runners gratuitos ilimitados; privado
      tem cota mensal. O `ci.yml` roda três motores de browser por push, que não é barato.
    - **Quem lê `ops/`.** D2-15 garante que nenhum segredo entra no repositório (domínio,
      host e credenciais vivem em `/etc/dg2/env` e nos secrets do GitHub), então público é
      seguro por construção — mas isso só vale enquanto a regra for respeitada.
    - O que a visibilidade **não** decide mais: a suposição A6 (workflow agendado desabilitado
      após 60 dias de inatividade) deixou de importar, porque D2-21 tirou o monitor externo
      do GitHub Actions e o pôs num serviço de terceiro.

    O nome importa porque vira parte da URL do `git remote` e aparece nos logs do CI. Note
    que o `base` do Vite deixa de ser `/DungeonGuys2/` no plano 02-02, então o nome do
    repositório **não** amarra mais nenhum caminho servido.
  </context>
  <options>
    <option id="publico-dungeonguys2">
      <name>Público, nome `DungeonGuys2`</name>
      <pros>Actions ilimitado; simetria com o `DungeonGuys` original; nenhum segredo em risco por D2-15</pros>
      <cons>`ops/` e o roadmap ficam legíveis por qualquer um antes de o jogo existir publicamente</cons>
    </option>
    <option id="privado-dungeonguys2">
      <name>Privado, nome `DungeonGuys2`</name>
      <pros>Nada visível até o jogo estar pronto</pros>
      <cons>Cota de minutos de Actions passa a ser um limite real com três motores de browser por push</cons>
    </option>
    <option id="outro-nome">
      <name>Outro nome (informar)</name>
      <pros>—</pros>
      <cons>—</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` — linhas 244-275 (DM-1: `git remote -v` vazio, 161 commits, o repositório inexistente no GitHub) e 1575-1591 (a suposição A6, sobre workflows agendados em repositório público)
    - `.planning/phases/02-migra-o-para-a-vps/02-CONTEXT.md` — D2-15, que garante que nenhum segredo entra no repositório e é o que torna a opção pública segura
    - `.github/workflows/ci.yml` — os oito portões que vão rodar num runner pela primeira vez, para dimensionar o custo de minutos de Actions
  </read_first>
  <files>(nenhum arquivo do repositório; a saída é a decisão registrada no SUMMARY)</files>
  <action>Apresentar as três opções acima, com o custo de cada uma, e aguardar a escolha. Registrar no SUMMARY o nome e a visibilidade escolhidos: a Task 3 os consome literalmente em `gh repo create`, e nenhuma outra tarefa da fase pode rodar antes disso.</action>
  <verify>
    <automated>gh auth status</automated>
  </verify>
  <acceptance_criteria>
    - `gh auth status` sai 0 (a conta que vai criar o repositório está autenticada)
    - O nome e a visibilidade escolhidos estão registrados no SUMMARY, em texto literal utilizável por `gh repo create`
  </acceptance_criteria>
  <done>O nome e a visibilidade do repositório estão decididos e escritos, e a conta do `gh` está autenticada.</done>
  <resume-signal>Responda: `publico-dungeonguys2`, `privado-dungeonguys2`, ou o nome e a visibilidade desejados.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Criar o repositório, empurrar, e ver o `ci.yml` verde num runner</name>
  <files>(nenhum arquivo do repositório; a saída é o estado remoto)</files>
  <read_first>
    - `.github/workflows/ci.yml` — os oito portões que vão rodar pela primeira vez num runner
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` — linhas 244-275 (DM-1, com as consequências 1 a 3) e 1407-1435 (Environment Availability: `gh` 2.83.2 disponível)
    - `package.json` — os scripts que os passos do CI chamam
  </read_first>
  <action>
    Com a resposta da Task 2, criar o repositório e empurrar:

    - `gh repo create <nome> --<public|private> --source=. --remote=origin --push`
    - Conferir que o Pages **não** foi habilitado: `gh api repos/{owner}/{repo}/pages` tem de
      responder 404. Se por qualquer motivo estiver habilitado, desabilitar com
      `gh api -X DELETE repos/{owner}/{repo}/pages` antes de seguir.
    - Acompanhar a primeira execução com `gh run watch` até ela terminar, e registrar a URL da
      execução no SUMMARY.

    **Se algum portão falhar no runner, conserte o portão — não o desabilite.** DM-1 já nomeia
    os suspeitos prováveis, e nenhum deles justifica afrouxar o `ci.yml`:
    - `npx playwright install --with-deps chromium firefox webkit` pode precisar de pacotes de
      sistema que só o `ubuntu-latest` conhece; o passo já usa `--with-deps` para isso.
    - `npm ci` num Linux resolve dependências opcionais diferentes das do Windows.
    - `sim:version:verify` roda três builds completos; ele exige reprodutibilidade **dentro de
      uma execução**, então um valor de `SIM_VERSION` diferente do visto localmente **não** é
      falha (A7). Não compare o valor do runner com o local.
    - `npm run assets:validate` roda contra `public/assets`, que hoje não tem manifesto — é
      verde por vacuidade de propósito.

    Não tocar em `packages/sim` nem em `SIM_VERSION` (fora de escopo desta fase).
  </action>
  <verify>
    <automated>gh run list --workflow=ci.yml --limit 1 --json conclusion --jq '.[0].conclusion'</automated>
  </verify>
  <acceptance_criteria>
    - `git remote -v` mostra `origin` apontando para o repositório criado
    - `git rev-parse --abbrev-ref --symbolic-full-name @{u}` responde `origin/main` (sem `fatal:`)
    - `gh run list --workflow=ci.yml --limit 1 --json conclusion --jq '.[0].conclusion'` imprime `success`
    - `gh api repos/{owner}/{repo}/pages` responde 404 (Pages nunca habilitado)
    - Nenhum arquivo de `.github/workflows/` foi editado para fazer o CI passar; se algo mudou, a mudança está descrita no SUMMARY com o motivo
    - A URL da execução verde está registrada no SUMMARY
  </acceptance_criteria>
  <done>Os oito portões da fase 1 deixaram de ser hipótese: existe uma execução verde num runner, e o Pages nunca recebeu nada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| máquina local → GitHub | 161 commits nunca revisados por terceiro atravessam para um servidor público |
| repositório → internet | se público, todo arquivo versionado passa a ser legível por qualquer um |
| workflow → GitHub Pages | um deploy no Pages criaria uma origem **compartilhada** com o jogo original |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-PAGES | Denial of Service | `.github/workflows/deploy.yml` → `gustavoktausend.github.io` | mitigate | Apagar `deploy.yml` **antes** do primeiro push. DM-3 mediu que o `sw.js` vivo do DungeonGuys original faz `caches.keys()` e apaga todo cache da origem que não seja `dungeonguys-v3` — publicar o DungeonGuys2 na mesma origem faria um jogo destruir o cache offline do outro. `tests/workflows.test.ts` mantém isso reprovável |
| T-2-SECRET | Information Disclosure | repositório público | mitigate | D2-15 mantém domínio, host e credenciais fora do repositório (`/etc/dg2/env` e secrets do GitHub). Task 2 põe a escolha de visibilidade na frente do usuário em vez de deixá-la implícita; os planos 02-03 e 02-04 asseram a ausência de literal IPv4 e de valor de segredo nos arquivos de `ops/` |
| T-2-SC | Tampering | `gh` CLI e `actions/*` | accept | Nenhum pacote npm é instalado neste plano. O `gh` é binário oficial do GitHub já presente na máquina; o `ci.yml` usa só `actions/checkout`, `actions/setup-node` e `actions/cache`, todas da própria GitHub |
</threat_model>

<verification>
- `npx vitest run tests/workflows.test.ts` verde, e vermelho quando um padrão de Pages é
  reintroduzido no `ci.yml`
- `npm test` inteiro verde
- Uma execução do `ci.yml` com `conclusion: success` existe no repositório remoto
- `gh api repos/{owner}/{repo}/pages` → 404
</verification>

<success_criteria>
INFRA-01 tem sua metade executável fechada: existe **um** alvo de deploy porque não existe
espelho, e um teste do repositório reprova qualquer tentativa de criar um. O `ci.yml` deixou
de ser um portão hipotético.
</success_criteria>

<output>
Create `.planning/phases/02-migra-o-para-a-vps/02-01-SUMMARY.md` when done
</output>

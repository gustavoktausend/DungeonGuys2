---
phase: 02-migra-o-para-a-vps
plan: 09
type: execute
wave: 5
depends_on: [02-07, 02-08]
files_modified:
  - tests/pwa/helpers.ts
  - tests/pwa/update.spec.ts
  - tests/pwa/api-isolation.spec.ts
  - .github/workflows/ci.yml
  - docs/PARIDADE.md
  - tests/build-base.test.ts
autonomous: true
requirements: [INFRA-02, INFRA-03]

must_haves:
  truths:
    - "Uma atualização a partir de uma instalação antiga funciona, e depois dela sobra exatamente um cache"
    - "Depois de várias chamadas a `/api/`, nenhuma URL de `/api/` existe em nenhum cache"
    - "Uma resposta 502 nunca é gravada no cache, e uma 200 seguinte é — o teste não é verde por vacuidade"
    - "Os quatro testes de PWA rodam no CI a cada push, com o mesmo cache de browser dos outros portões"
    - "A caixa de PWA de `docs/PARIDADE.md` continua ABERTA, com o texto atualizado e o alcance real da lacuna registrado"
  artifacts:
    - path: "tests/pwa/update.spec.ts"
      provides: "criterio 2: atualizacao a partir da instalacao antiga, com um cache no fim"
    - path: "tests/pwa/api-isolation.spec.ts"
      provides: "criterio 3: /api/ fora do cache e nao-ok nunca gravado"
    - path: ".github/workflows/ci.yml"
      provides: "job pwa reaproveitando o cache de browser por versao exata do lock"
      contains: "test:pwa"
  key_links:
    - from: "tests/pwa/update.spec.ts"
      to: "tests/pwa/fixtures/old-build/"
      via: "servir a instalacao antiga e trocar a raiz para o build novo na mesma origem"
      pattern: "old-build"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm run test:pwa depois de build e sw:verify"
      pattern: "test:pwa"
---

<objective>
Fechar o critério 3 e a metade de atualização do critério 2 com as duas specs que faltavam, e
pôr os quatro testes de PWA no CI a cada push.

**Fatia (MVP):** ao fim deste plano, "o PWA está correto" deixa de depender de alguém lembrar
de rodar Playwright. Um push que quebre a isolação de `/api/`, a gravação condicionada a
`res.ok` ou o caminho de atualização reprova no CI, no próprio PR que causou.

Purpose: D2-11 decidiu que a verificação de instalação, atualização e offline é **só**
Playwright no CI, sem checklist manual em aparelho real. A consequência aceita fica
registrada e **não** é fechada por este plano: a caixa de `docs/PARIDADE.md` permanece
aberta, e a pesquisa **ampliou** o alcance medido da lacuna — o Playwright só suporta service
worker em Chromium, então Firefox e WebKit também ficam sem cobertura, mesmo em desktop,
mesmo no CI.

Output: `update.spec.ts`, `api-isolation.spec.ts`, o job `pwa` no `ci.yml`, e o texto de
`docs/PARIDADE.md` corrigido com a caixa ainda desmarcada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-migra-o-para-a-vps/02-CONTEXT.md
@.planning/phases/02-migra-o-para-a-vps/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `update.spec.ts` — do build antigo ao novo, com um cache no fim</name>
  <files>tests/pwa/helpers.ts, tests/pwa/update.spec.ts</files>
  <read_first>
    - `tests/pwa/helpers.ts` — criado no plano 02-05: `serveDir`, `setRoot`, `route`, `waitForActivated`, `readCacheEntries`, `collectSameOriginFailures`
    - `tests/pwa/fixtures/old-build/sw.js` — o worker antigo: `CACHE = 'dungeonguys2-v1'`, `skipWaiting()` no install, `clients.claim()` no activate. Como ele reivindica a página, ele controla imediatamente — e é essa diferença de comportamento que a spec encena
    - `public/sw.js` — o template novo, para saber o que esperar do lado novo: sem `claim`, filtro por prefixo `dg2-` no `activate`, troca só por mensagem
    - `src/main.ts` e `src/ui/screens.ts` — o ciclo de update do plano 02-07: `#btn-update`, `showUpdateOffer`, `SKIP_WAITING`, guarda de `controllerchange`
    - `.planning/phases/02-migra-o-para-a-vps/02-VALIDATION.md` — critério 2 item (b) e critério 3 item (c)
    - `index.html` — os ids `#start-screen`, `#btn-start` e `#btn-update`
  </read_first>
  <action>
    Se `tests/pwa/helpers.ts` ainda não expuser `setRoot(dir)` capaz de trocar a raiz servida
    **sem fechar o servidor**, acrescentar agora — é o que permite encenar "build antigo →
    build novo" na **mesma origem e na mesma porta**, que é a única forma de o teste ser uma
    atualização de service worker em vez de uma instalação nova noutro escopo.

    `tests/pwa/update.spec.ts`:
    - `serveDir('tests/pwa/fixtures/old-build')`; navegar; `waitForActivated`.
    - Assertar o estado de partida: `navigator.serviceWorker.controller` não é nulo (o worker
      antigo chama `claim`, então controla de imediato) e `caches.keys()` contém
      `dungeonguys2-v1`.
    - `setRoot('dist')` — a origem passa a servir o build novo, com o `sw.js` derivado.
    - `page.reload()` e esperar que `registration.waiting` deixe de ser nulo.
    - Assertar que `navigator.serviceWorker.controller.scriptURL` ainda corresponde ao worker
      **antigo** — nada trocou sozinho, porque o novo não chama `clients.claim()` (D2-09).
    - Assertar que `#btn-update` está visível na tela inicial — a oferta do plano 02-07
      apareceu porque `gameStarted` é `false`.
    - Clicar em `#btn-update`; aguardar o evento `controllerchange` e o recarregamento
      subsequente estabilizar.
    - Assertar o estado de chegada: `caches.keys()` tem comprimento **exatamente 1**; o nome
      casa com o prefixo `dg2-` seguido de 16 hexadecimais; `dungeonguys2-v1` **sumiu**; e o
      conjunto de pathnames guardados é o precache do build novo.
    - Assertar que a página está sendo controlada pelo worker novo.
    - Comentário explicando por que este teste é o que fecha o critério 3 na parte de "um
      deploy novo não deixa o cache velho para trás": o nome do cache deriva do build, então o
      `activate` finalmente tem o que limpar — com nome estático ele nunca teria (P-3).
  </action>
  <verify>
    <automated>npx playwright test tests/pwa/update.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/pwa/update.spec.ts` sai 0
    - A spec parte de `tests/pwa/fixtures/old-build` e troca a raiz servida **sem** fechar o servidor (mesma porta, mesma origem)
    - A spec assere `caches.keys()` com comprimento exatamente 1 ao final, e a ausência de `dungeonguys2-v1`
    - A spec clica em `#btn-update` — o caminho de troca passa pela UI de D2-09, não por `postMessage` direto do teste
    - Prova de não-vacuidade: comentar temporariamente a chamada de `showUpdateOffer` em `src/main.ts` faz a spec sair 1 por não encontrar o botão visível; restaurar volta a 0
    - `npm run typecheck:pwa` sai 0
  </acceptance_criteria>
  <done>A atualização a partir de uma instalação antiga é um comando verde, e o cache velho comprovadamente não sobrevive a ela.</done>
</task>

<task type="auto">
  <name>Task 2: `api-isolation.spec.ts` — `/api/` fora do cache, e não-`ok` nunca gravado</name>
  <files>tests/pwa/api-isolation.spec.ts, tests/pwa/helpers.ts</files>
  <read_first>
    - `tests/pwa/helpers.ts` — `route(path, handler)` e `readCacheEntries`, criados no plano 02-05
    - `public/sw.js` — o handler de `fetch`: early-return de `/api/` e `/ws`, o allowlist, e a gravação condicionada a `res.ok`
    - `apps/server/src/app.ts` — a forma exata do corpo de `/api/health`, para a rota simulada devolver o mesmo shape
    - `.planning/phases/02-migra-o-para-a-vps/02-VALIDATION.md` — critério 3 itens (a) e (b)
    - `.planning/phases/02-migra-o-para-a-vps/02-RESEARCH.md` — linhas 777-786 (P-2: trinta segundos de 502 num deploy virando um `index.html` de erro cacheado para sempre)
  </read_first>
  <action>
    `tests/pwa/api-isolation.spec.ts`, com duas metades e uma guarda contra verde vacuoso.

    **Metade 1 — `/api/` nunca entra no Cache Storage.**
    - Registrar no helper uma rota `/api/health` devolvendo, com `Cache-Control: no-store`, o
      mesmo shape de três chaves que `apps/server/src/app.ts` produz.
    - Instalar o service worker do build novo e recarregar para ele controlar a página.
    - De dentro da página, chamar `fetch('/api/health')` cinco vezes, conferindo que as
      respostas chegam 200 (ou seja: a requisição **passa**, ela só não é guardada).
    - Percorrer **todos** os caches de `caches.keys()` e, para cada um, todas as entradas, e
      assertar que nenhum pathname começa por `/api/`. Mesma asserção para o caminho `/ws`.
    - A asserção é sobre **todos** os caches, não só o atual: o requisito é que a resposta não
      seja persistida em lugar nenhum da origem.

    **Metade 2 — resposta não-`ok` nunca é gravada.**
    - Escolher um caminho que **está** no precache e tem nome estável: `/manifest.json`.
    - Apagar a entrada dele do cache de dentro da página (`cache.delete('/manifest.json')`) —
      necessário porque o handler é cache-first e sem isso a rede nem seria consultada.
    - Fazer a rota do helper responder **502** para esse caminho.
    - Chamar `fetch('/manifest.json')` da página; assertar que `cache.match('/manifest.json')`
      continua **indefinido** — a resposta de erro não foi gravada.
    - **Guarda contra verde vacuoso:** voltar a rota para 200, chamar `fetch` de novo, e
      assertar que agora a entrada **reapareceu** no cache. Sem essa segunda metade, um
      service worker que simplesmente não gravasse nada passaria no teste.
    - Comentário registrando por que isso importa: Cache Storage ignora `Cache-Control` por
      desenho, então trinta segundos de 502 durante um deploy virariam um documento de erro
      cacheado para sempre (P-2).
  </action>
  <verify>
    <automated>npx playwright test tests/pwa/api-isolation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/pwa/api-isolation.spec.ts` sai 0
    - A spec varre **todos** os caches de `caches.keys()`, não apenas o cache atual
    - A spec assere que as cinco chamadas a `/api/health` **retornaram 200** (a requisição passa; só não é guardada)
    - A spec contém as duas metades da prova de `res.ok`: com 502 a entrada não reaparece, com 200 ela reaparece
    - Prova de não-vacuidade: remover temporariamente a condição `res.ok` de `public/sw.js`, reconstruir, e a spec sai 1; restaurar e reconstruir volta a 0
    - `npm run typecheck:pwa` sai 0
  </acceptance_criteria>
  <done>INFRA-03 deixou de ser promessa: as duas propriedades são comandos, e nenhum dos dois pode ficar verde sem fazer o que diz.</done>
</task>

<task type="auto">
  <name>Task 3: O job `pwa` no CI, e o texto de `docs/PARIDADE.md` sem marcar a caixa</name>
  <files>.github/workflows/ci.yml, docs/PARIDADE.md, tests/build-base.test.ts</files>
  <read_first>
    - `.github/workflows/ci.yml` — linhas 11-25 (a forma dos passos) e 50-69 (o par `playwright-version` + `actions/cache` por versão exata do lock, e o comentário que explica por que a versão exata é a variável do experimento)
    - `docs/PARIDADE.md` — linhas 1-16 (a legenda dos dois estados e a regra de que só conta o que foi provado) e 232-237 (a caixa de PWA que **permanece aberta**)
    - `.planning/phases/02-migra-o-para-a-vps/02-VALIDATION.md` — § "Lacuna aceita, registrada por escolha (D2-11)", com a instrução explícita ao verificador da fase
    - `package.json` — os scripts `build`, `sw:verify` e `test:pwa`
    - `tests/build-base.test.ts` — o arquivo estrutural do cliente, que ganha a guarda da caixa aqui
  </read_first>
  <action>
    1. `.github/workflows/ci.yml`: acrescentar um job `pwa`, **independente** (sem `needs:`),
       para rodar em paralelo com `test`:
       - `runs-on: ubuntu-latest`; `actions/checkout@v4`; `actions/setup-node@v4` com
         `node-version: '24'` e `cache: npm`; `npm ci`.
       - O **mesmo** par de passos que o job `test` já usa para o cache de browser: o `id:
         playwright-version` lendo a versão exata de `package-lock.json`, e `actions/cache@v4`
         com a chave derivada dela. Reaproveitar, não reinventar — o cuidado já está lá.
       - `npx playwright install --with-deps chromium` — **só** Chromium. Comentário em
         português sem acentos explicando: service worker no Playwright e suportado apenas em
         Chromium, entao instalar os outros dois aqui custaria minutos e nao compraria
         cobertura.
       - `npm run build`, `npm run sw:verify`, `npm run test:pwa`, nessa ordem.
       - Comentário no cabeçalho do job, na convenção do arquivo (por que existe e o que a
         falha significa): este job e a prova executavel de INFRA-02 e INFRA-03; se ele ficar
         vermelho, ou o precache deixou de cobrir o dist, ou uma resposta de /api/ passou a ser
         guardada, ou a atualizacao a partir de uma instalacao antiga parou de funcionar.
       - Registrar, em comentário, que o job `deploy` do plano 02-11 vai declarar
         `needs: [test, pwa]`.
    2. `docs/PARIDADE.md`, linhas 232-237: reescrever o **texto** da linha do PWA e **deixar a
       caixa desmarcada**. O texto atual diz que o `public/sw.js` foi reescrito na Task 20 e
       que instalar num dispositivo é o único teste que vale. O texto novo deve dizer: o
       `sw.js` passou a ser derivado do build por `tools/sw/emit.mjs`, com precache do `dist/`
       inteiro, allowlist e nome de cache por hash; quatro specs de Playwright cobrem
       instalação limpa, atualização, offline e isolação de `/api/` — **em Chromium apenas**,
       porque o Playwright só suporta service worker nesse motor; e portanto **iOS/Safari
       físico, Firefox e WebKit continuam sem cobertura, por decisão registrada (D2-11)**.
       Manter o marcador `*aguardando o humano*` e **não** marcar `[x]`: marcar seria
       contradizer D2-11.
    3. `tests/build-base.test.ts`: acrescentar
       `describe('lacuna aceita de PWA (D2-11)')` lendo `'../docs/PARIDADE.md'` por glob raw,
       com guarda de existência, e asserindo que a linha que menciona
       `PWA instalável e funcional offline` começa com `- [ ]` e **não** com `- [x]`. Comentário
       explicando que a asserção existe para que ninguém marque a caixa em silêncio, e que a
       lacuna é escolha, não pendência da fase.
  </action>
  <verify>
    <automated>npx vitest run tests/build-base.test.ts && npm run build && npm run sw:verify && npm run test:pwa</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test:pwa' .github/workflows/ci.yml` retorna 1 e `grep -c 'sw:verify' .github/workflows/ci.yml` retorna 1
    - `grep -c 'playwright install --with-deps chromium$' .github/workflows/ci.yml` retorna 1 — só Chromium no job novo
    - O job `pwa` reutiliza a chave de cache derivada de `package-lock.json`, idêntica em forma à do job `test`
    - `grep -c 'PWA instalável e funcional offline' docs/PARIDADE.md` retorna 1, e a linha começa com `- [ ]`
    - `grep -c 'Chromium' docs/PARIDADE.md` retorna 1 ou mais — o alcance real da lacuna está registrado
    - `npm run test:pwa` roda as **quatro** specs e sai 0
    - `npx vitest run tests/build-base.test.ts` e `npm test` saem 0
    - `npx vitest run tests/workflows.test.ts` continua saindo 0 (nenhum padrão de Pages voltou)
  </acceptance_criteria>
  <done>Os quatro testes de PWA rodam a cada push, e a lacuna aceita está escrita com o alcance que a pesquisa mediu — sem ninguém marcar a caixa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resposta de `/api/*` → Cache Storage | a partir da fase 6 essas respostas carregam sessão; o que for guardado aqui não é limpo em logout nenhum |
| build antigo instalado → build novo servido | a janela em que duas versões do jogo coexistem no mesmo navegador |
| CI → registro de lacuna aceita | uma caixa marcada por engano transforma uma decisão registrada em cobertura imaginária |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-CACHE | Information Disclosure | Cache Storage ← `/api/*` | mitigate | `api-isolation.spec.ts` varre **todos** os caches da origem depois de cinco chamadas e assere ausência de qualquer pathname sob `/api/` e de `/ws`. O job `pwa` no `ci.yml` faz essa asserção rodar a cada push, e não só quando alguém lembra |
| T-2-STALE | Tampering | `cache.put` condicionado a `res.ok` | mitigate | Metade 2 de `api-isolation.spec.ts`, com a guarda de não-vacuidade: com 502 a entrada não reaparece, com 200 ela reaparece. Um worker que simplesmente não gravasse nada não passaria |
| T-2-STALECACHE | Denial of Service | cache velho sobrevivendo ao deploy | mitigate | `update.spec.ts` assere `caches.keys()` com comprimento exatamente 1 e a ausência de `dungeonguys2-v1` ao fim da atualização. Com nome de cache estático o `activate` nunca teria o que limpar (P-3) |
| T-2-FALSECOVER | Repudiation | `docs/PARIDADE.md` | mitigate | O texto passa a descrever o que **de fato** é coberto (Chromium apenas) e a caixa continua `- [ ]`, com asserção em `tests/build-base.test.ts` reprovando quem a marcar. A lacuna de iOS/Safari, Firefox e WebKit é decisão registrada de D2-11, não pendência da fase — e o verificador deve ler o critério 2 com essa ressalva |
| T-2-SC | Tampering | job `pwa` no `ci.yml` | mitigate | Só ações da própria GitHub (`actions/checkout`, `actions/setup-node`, `actions/cache`); o Playwright é instalado por `npx` a partir da versão exata já travada no lock, reaproveitando o cache por chave derivada dessa versão |
</threat_model>

<verification>
- `npm run test:pwa` verde com as quatro specs
- `npm run build && npm run sw:verify` verde
- `npx vitest run tests/build-base.test.ts tests/workflows.test.ts` verde
- A caixa de PWA em `docs/PARIDADE.md` continua `- [ ]`
- O job `pwa` aparece no `ci.yml` com o mesmo esquema de cache de browser do job `test`
</verification>

<success_criteria>
Os critérios 2 e 3 estão inteiramente cobertos por comando, e a cobertura roda no CI a cada
push. A única parte não coberta é a que D2-11 recusou de propósito, e ela está escrita com o
alcance real medido pela pesquisa.
</success_criteria>

<output>
Create `.planning/phases/02-migra-o-para-a-vps/02-09-SUMMARY.md` when done
</output>

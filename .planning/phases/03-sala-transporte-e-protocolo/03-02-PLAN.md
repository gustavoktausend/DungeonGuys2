---
phase: 03-sala-transporte-e-protocolo
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/protocol/src/snapshotCodec.ts
  - packages/protocol/src/index.ts
  - tests/worlds.ts
  - tests/worlds.test.ts
  - tests/snapshot-codec.test.ts
  - tests/cross-engine.test.ts
autonomous: true
requirements: [SYNC-04]

must_haves:
  truths:
    - "Um World de wave 16 com quatro jogadores, `swarm` e todo inimigo elite vira três mensagens, e a maior fica abaixo de 16 KiB (SYNC-04, D3-19)"
    - "O snapshot nunca carrega `obstacles`, `traps`, `play` nem `config` — a camada estática vem da seed (D3-17)"
    - "Decodificar e recodificar devolve os mesmos bytes; decodificar o que foi codificado devolve o mesmo registro campo a campo, conferido com `Object.is` (D3-18)"
    - "Cada parte decodifica sozinha: perder a parte 0 não invalida as partes 1 e 2 (D3-19)"
    - "O codec produz os mesmos bytes em Chromium, Firefox, WebKit e Node"
  artifacts:
    - path: "tests/worlds.ts"
      provides: "Os três mundos sintéticos de pior caso, construídos pelas fórmulas do sim, compartilhados entre teste e bench (D3-20)"
      exports: ["wave1FourPlayers", "wave16SwarmElite", "wave40Endless"]
    - path: "packages/protocol/src/snapshotCodec.ts"
      provides: "SnapshotRecord, extractSnapshot, encodeSnapshot, decodeSnapshot, decodeSnapshotPart"
      min_lines: 300
    - path: "tests/snapshot-codec.test.ts"
      provides: "Round-trip duplo, idempotência da quantização, -0 por Object.is, teto de 16 KiB, partição forçada"
  key_links:
    - from: "packages/protocol/src/snapshotCodec.ts"
      to: "packages/protocol/src/inputCodec.ts"
      via: "reuso de AIM_STEP para todo ângulo"
      pattern: "AIM_STEP"
    - from: "packages/protocol/src/snapshotCodec.ts"
      to: "packages/protocol/src/enums.ts"
      via: "índices das tabelas congeladas"
      pattern: "SNAPSHOT_PART|ENEMY_TYPE"
    - from: "tests/cross-engine.test.ts"
      to: "packages/protocol/src/snapshotCodec.ts"
      via: "round-trip do codec nos três motores"
      pattern: "encodeSnapshot"
---

<objective>
Escrever o codec binário do snapshot: `World` → registro de inteiros → três mensagens
auto-contidas de `DataView` little-endian, e a volta. Mais os três mundos sintéticos de pior
caso que o teste e o bench vão compartilhar.

Purpose: `JSON.stringify(world)` mede **82 KiB** numa wave 16 com quatro jogadores neste
repositório — cinco vezes o teto de 16 KiB por mensagem de DataChannel. O codec é restrição
medida, não otimização, e o formato é decisão de protocolo: quem o define é esta fase, mesmo
que só a fase 4 o exercite com tráfego real.

Output: `packages/protocol/src/snapshotCodec.ts` e um teste que prova, com `Object.is` e nunca
por hash, que nenhum bit se perde no caminho.
</objective>

## Phase Goal

**As a** jogador que quer jogar com amigos, **I want to** criar uma sala, receber um código
curto e ver meus amigos entrarem e escolherem classe no lobby antes de eu iniciar a run,
**so that** nós quatro comecemos a mesma run no mesmo mundo, sobre um fio cujo formato já
está medido e congelado.

Este plano é a fatia "o fio cabe": depois dele, um mundo de pior caso pode atravessar um
DataChannel real, o que antes dele era impossível por medição.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sala-transporte-e-protocolo/03-CONTEXT.md
@.planning/phases/03-sala-transporte-e-protocolo/03-PATTERNS.md
@.planning/phases/03-sala-transporte-e-protocolo/03-VALIDATION.md
@.planning/phases/03-sala-transporte-e-protocolo/03-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Do plano 03-01, já congelado — use os índices destas tabelas, nunca strings no fio:
`SNAPSHOT_PART` (actors, projectiles, pickups), `PHASE`, `MUTATOR_KEY`, `PLAYER_SLOT`,
`CLASS_KEY`, `ENEMY_TYPE`, `ELITE_TYPE`, `BOSS_STATE`, `ATTACK_KIND`, `CHEST_STATE`, e
`MSG_KIND` (o índice de `'snapshot'` é 6).

De `packages/protocol/src/inputCodec.ts`, já existente e **para reusar, não reimplementar**:
- `AIM_STEP` (exportado) — a quantização de ângulo. `facing` do jogador e `angle` de projétil
  usam esta e nenhuma outra.
- O `toBase64`/`fromBase64` escritos à mão, se o snapshot precisar de texto numa fixture.
- O idioma de normalização de `-0`: `| 0` depois do arredondamento, e `& 0xffff` no ângulo.
- O padrão de "contagem declarada contra bytes recebidos" de `decodeLog` (linhas ~402-428).

De `packages/sim`, importado **só como tipo** (`import type`), porque `packages/protocol`
mantém `dependencies: {}`:
- `World` (`packages/sim/src/types.ts:341`) — os campos reais: `tick`, `phase`, `rng`,
  `nextId`, `players` (Record), `enemies`, `bullets`, `enemyBullets`, `coins`, `potions`,
  `chests`, `wave`, `waveActive`, `waveHasBoss`, `waveMutator` (`MutatorKey | null`), `score`,
  `combo`
- `Player` (`:86`) — `x`, `y`, `hp`, `maxHp`, `stamina`, `sprinting`, `invincible`, `facing`,
  `moving`, `level`, `xp`, `gold`, `cls`, `id`
- `Enemy` (`:113`) — `id`, `type`, `x`, `y`, `hp`, `maxHp`, `dead`, `moving`, `elite`
  (`string | null`), `hitFlash`, `poisonT`, `burnT`, `slowT`, `fusing`, `bossState` (`string`),
  `enraged` (`boolean`)
- `Bullet` — `owner` (id de jogador, **string**), `x`, `y`, `angle`, `type` (`AttackKind`)
- `EnemyBullet` — `x`, `y`, `vx`, `vy`
- `Coin`, `Potion` — `x`, `y`; `Chest` — `x`, `y`, `state`
- `Rng.save()` devolve o cursor como `number` (`packages/sim/src/rng.ts:56`), que é o que
  `saveWorld` já grava
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Os três mundos sintéticos, provados contra as fórmulas do sim</name>
  <files>tests/worlds.ts, tests/worlds.test.ts</files>

  <read_first>
    - `tests/helpers.ts` **inteiro** (o analog exato: `.ts` sem `.test.`, `BASE_CONFIG`, o deep-copy de `players` e o motivo escrito)
    - `packages/sim/src/run.ts` (`startNextWave` — a fórmula de contagem de inimigos e o efeito de `swarm`)
    - `packages/sim/src/enemies.ts` (`makeEnemy`, `makeElite`, e as linhas 125-127 que sorteiam o tipo de elite)
    - `packages/sim/src/defs/enemies.ts` (`ENEMY_DEFS`, `ELITE_TYPES`, `BOSS_WAVES`, `MINIBOSS_WAVES`)
    - `packages/sim/src/serialize.ts` (`saveWorld` e o cabeçalho das linhas 23-39 sobre `-0`)
    - `packages/sim/src/equipment-catalog.ts` (os itens com que os quatro jogadores são equipados)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-RESEARCH.md` § "Measured Baseline" (a tabela de números medidos) e § Discretion #14 (os tetos de projétil e loot)
  </read_first>

  <action>
    Crie `tests/worlds.ts` exportando três construtores determinísticos, sem `Math.random` em
    lugar nenhum: `wave1FourPlayers()`, `wave16SwarmElite()` e `wave40Endless()`. Cada um monta um
    `RunConfig` de quatro jogadores (`p0`..`p3`, quatro classes diferentes, `forge` idêntico e
    explícito), chama `createWorld`, `generateArena` e `createPlayer` para os quatro, e então
    popula o mundo **pelas fórmulas do próprio sim**: a contagem de inimigos por
    `round((4 + wave * 3) * 1.6)` quando o mutador é `swarm`, `makeEnemy` para cada um,
    `makeElite` em todos (pior caso do mutador `elite`), mais o chefe da wave quando
    `BOSS_WAVES`/`MINIBOSS_WAVES` o define.

    Os tetos de projétil e loot ficam como **constantes nomeadas no topo do arquivo**, com o
    comentário dizendo que são o teto declarado de D3-20 e de onde vieram: 80 `bullets`, 24
    `enemyBullets`, 140 `coins`, 12 `potions`, 4 `chests` na wave 16. `wave40Endless` usa
    `wave = 40`, `mode: 'endless'` e os mesmos tetos escalados pela mesma regra, também escrita.

    O cabeçalho do arquivo tem de dizer, em inglês e com ênfase, que **este módulo é a única
    fonte do teto**: o teste do codec e o bench de `tools/bench/snapshot.mjs` importam daqui, e é
    isso que impede o bench e o portão de medirem mundos diferentes. Mesma disciplina de
    `tests/helpers.ts`: cada fixture explica **por que** é o que é, e `players` é copiado em
    profundidade para que um teste que escreva em `forge` não contamine os outros.

    Crie `tests/worlds.test.ts` provando que os construtores são o que dizem ser, em português:
    contagem de inimigos por wave, todos com `elite` não nulo, quatro jogadores com ids
    `p0`..`p3`, `waveMutator === 'swarm'` onde previsto, e a linha de base de JSON medida —
    `saveWorld(wave16SwarmElite()).length` entre 70.000 e 95.000 caracteres, com o comentário
    dizendo que a faixa é larga de propósito (a medição da pesquisa deu 82 KiB, e o que o teste
    protege é a ordem de grandeza que justifica o codec, não um número exato). O mesmo para wave 1
    (faixa 15.000-30.000) e wave 40 (faixa 130.000-190.000).

    Determinismo: dois `wave16SwarmElite()` seguidos produzem o mesmo `hashWorld`. Sem isso o
    bench mede coisas diferentes a cada execução.
  </action>

  <verify>
    <automated>npx vitest run tests/worlds.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `tests/worlds.ts` exporta `wave1FourPlayers`, `wave16SwarmElite` e `wave40Endless`
    - `grep -c "Math.random" tests/worlds.ts` retorna 0
    - `npx vitest run tests/worlds.test.ts` sai com código 0
    - A saída de `npx vitest run tests/worlds.test.ts` inclui um teste cujo nome menciona `hashWorld` (o determinismo dos construtores)
    - `tests/worlds.ts` contém as cinco constantes de teto com nome (bullets, enemyBullets, coins, potions, chests) e cada uma tem um comentário na linha acima ou ao lado
    - `npx tsc --noEmit` sai 0
    - `npm run lint` sai 0
  </acceptance_criteria>

  <done>
    Existe um mundo de pior caso reproduzível que o teste e o bench compartilham, e a medição de
    82 KiB da pesquisa deixou de ser um número num documento e virou uma asserção do repositório.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `snapshotCodec.ts` — extrair, codificar em três partes, decodificar de volta</name>
  <files>packages/protocol/src/snapshotCodec.ts, packages/protocol/src/index.ts, tests/snapshot-codec.test.ts</files>

  <read_first>
    - `packages/protocol/src/inputCodec.ts` **inteiro, as 444 linhas** — a forma é para ser copiada, não parafraseada
    - `tests/input-codec.test.ts` **inteiro** — o cabeçalho que proíbe o matcher de tolerância, o comparador campo a campo com `Object.is`, o corpus semeado com contagem de falhas
    - `packages/sim/src/serialize.ts` linhas 23-39 (o cabeçalho que já prevê este arquivo)
    - `packages/sim/src/types.ts` linhas 86-160 e 341-384 (as formas reais de `Player`, `Enemy` e `World`)
    - `packages/protocol/src/enums.ts` (as tabelas do plano 03-01)
    - `packages/protocol/tsconfig.json` (`lib: ["ES2022"]`, `types: []`)
    - `tests/worlds.ts` (criado na tarefa 1)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-RESEARCH.md` § Discretion #8 (o layout byte a byte) e § Discretion #10 (a divisão de camadas)
  </read_first>

  <behavior>
    - `decodeSnapshot(encodeSnapshot(r))` devolve um registro igual a `r` campo a campo, comparado com `Object.is` e **nunca** por hash
    - `encodeSnapshot(decodeSnapshot(bytes))` devolve os mesmos bytes, comparados como `Uint8Array`
    - `encodeSnapshot` devolve sempre **três** `ArrayBuffer`, um por entrada de `SNAPSHOT_PART`
    - Cada parte decodifica sozinha por `decodeSnapshotPart`, sem as outras duas
    - Cada parte carrega o mesmo `tick` no cabeçalho e o seu próprio índice de parte
    - `baselineTick` é sempre 0 nesta fase, e o decodificador recusa qualquer valor diferente com erro em português nomeando D3-18
    - Em `wave16SwarmElite()`, cada parte tem menos de 16384 bytes
    - Em `wave40Endless()`, cada parte tem menos de 16384 bytes
    - Quantizar duas vezes é igual a quantizar uma: sobre 200.000 amostras de um `Rng` semeado, zero divergências
    - Uma posição levemente negativa (`-0.04`) quantiza para `+0` e não para `-0`: `Object.is(q, 0)` é `true` e `Object.is(q, -0)` é `false`
    - O registro extraído de `wave16SwarmElite()` não contém nenhum campo derivado de `obstacles`, `traps`, `play` ou `config`
  </behavior>

  <action>
    Crie `packages/protocol/src/snapshotCodec.ts` no estilo de `inputCodec.ts`: `DataView`,
    little-endian, constantes de layout nomeadas no topo com o custo em bytes escrito, e um
    cabeçalho que carrega a decisão.

    **O cabeçalho tem de dizer quatro coisas**, cada uma com o "o que aconteceria se fosse do
    outro jeito": (a) o snapshot **nunca** carrega `obstacles`, `traps`, `play` nem `config` —
    a camada estática é derivada da seed em cada cliente, pelo mesmo caminho que o replay usa
    (D3-17, D-11), e são 3,3 KiB constantes que simplesmente não existem no fio; (b) a partição
    por classe de entidade **não é** filtro de relevância e não pode virar interest management,
    que está fora do marco; (c) a parte 0 só passa de 16 KiB com cerca de **1014 inimigos**, o que
    pela fórmula do sim é a **wave ~210** — por isso o mecanismo é exercido por um teste que o
    força, e não pelo bench, e sem esta frase alguém futuro conclui que é código morto e apaga;
    (d) o teto de 16 KiB **não** é compatibilidade de 2017: é head-of-line blocking entre os dois
    canais na mesma associação SCTP, é perda multiplicada num canal com `maxRetransmits: 0`, e é
    o upload doméstico da autoridade que quebra primeiro.

    **Constantes de layout**, exportadas onde outro módulo precisar delas: `SNAPSHOT_HEADER_BYTES
    = 10`, `POS_SCALE = 8` (um `uint16` cobre 0..8191,875 px com 0,125 px de precisão, contra um
    `WORLD` de 2400×1600 — mesmos dois bytes que 1 px e sem tremor de grade),
    `STAMINA_SCALE = 100`, `SNAPSHOT_MAX_BYTES = 16 * 1024`, `PLAYER_RECORD_BYTES = 29`,
    `ENEMY_RECORD_BYTES = 16`. `AIM_STEP` vem de `./inputCodec`, importado, **nunca** redeclarado.

    **Cabeçalho comum de 10 bytes, em toda parte:** `u8 kind` (índice de `'snapshot'` em
    `MSG_KIND`), `u8 part` (índice em `SNAPSHOT_PART`), `u32 tick` LE, `u32 baselineTick` LE
    (0 = completo, sempre 0 nesta fase).

    **Parte 0, `actors`:** `u32 rng` (o cursor de `world.rng.save()`), `u32 nextId`, `u8 phase`
    (índice em `PHASE`), `u16 wave`, `u8 waveFlags` (bit0 `waveActive`, bit1 `waveHasBoss`),
    `u8 waveMutator` (índice em `MUTATOR_KEY`, 0 quando `world.waveMutator` é `null`), `u32 score`,
    `u16 combo`, `u8 playerCount`; por jogador, 29 bytes: `u8 slot` (índice em `PLAYER_SLOT`),
    `u8 cls` (índice em `CLASS_KEY`), `f32 x`, `f32 y`, `u16 facing` (por `AIM_STEP`), `u16 hp`,
    `u16 maxHp`, `u16 stamina` (×`STAMINA_SCALE`), `u16 level`, `u32 gold`, `u32 xp`, `u8 flags`
    (bit0 `moving`, bit1 `sprinting`, bit2 `invincible > 0`, bit3 reservado para "caído" da fase 5,
    escrito como 0); depois `u16 enemyCount` e, por inimigo, 16 bytes: `u32 id`, `u16 x`
    (por `POS_SCALE`), `u16 y`, `u16 hp`, `u16 maxHp`, `u8 type` (índice em `ENEMY_TYPE`),
    `u8 elite` (índice em `ELITE_TYPE`, 0 quando `null`), `u8 bossState` (índice em `BOSS_STATE`),
    `u8 flags` (bit0 `dead`, bit1 `moving`, bit2 `enraged`, bit3 `fusing`, bit4 `hitFlash > 0`,
    bit5 `slowT > 0`, bit6 `burnT > 0`, bit7 `poisonT > 0`).

    **A posição dos quatro jogadores vai em `float32`, não quantizada** — e o motivo vai escrito:
    quantizar a posição que alimenta a reconciliação dá tremor permanente, e mandar `float32` só
    para o destinatário local tornaria a mensagem diferente por peer, obrigando a autoridade a
    codificar três vezes por tick. Dezesseis bytes por snapshot compram "os mesmos bytes para todo
    mundo".

    **`hp` e `maxHp` em `uint16` com clamp em 65535**, e o clamp documentado como limite cosmético
    conhecido: o produto passa de 65535 por volta da wave 70, os dois únicos consumidores
    (`src/render/entities.ts:328` e `src/ui/hud.ts:83`) usam a razão, e a saída boa — exportar uma
    função do sim — está fechada nesta fase porque mexeria em `SIM_VERSION`. Registre como dívida
    para o próximo commit que já mova `SIM_VERSION`.

    **`id` de inimigo em `uint32`, não `uint16`** — `world.nextId` passa de 65535 na wave 164 no
    pior caso, e um id que dá a volta faz dois inimigos aliasarem no delta da fase 4, com sintoma
    de dessincronização silenciosa.

    **Parte 1, `projectiles`:** `u16 bulletCount`; por bullet 8 bytes: `u16 x`, `u16 y`,
    `u16 angle` (por `AIM_STEP`), `u8 type` (índice em `ATTACK_KIND`), `u8 ownerSlot` (índice em
    `PLAYER_SLOT`, resolvido a partir de `Bullet.owner`, que é o id em texto). Depois
    `u16 enemyBulletCount`; por enemyBullet 6 bytes: `u16 x`, `u16 y`, `u16 angle` (derivado de
    `vx`/`vy` **sem** trigonometria dentro do codec — leia o ângulo já existente se houver, ou
    guarde `vx`/`vy` quantizados; a regra C-7 proíbe `Math.atan2` aqui, e a decisão que você tomar
    fica escrita no arquivo).

    **Parte 2, `pickups`:** `u16 coinCount`; por coin 4 bytes (`u16 x`, `u16 y`).
    `u16 potionCount`; por potion 4 bytes. `u16 chestCount`; por chest 5 bytes (`u16 x`, `u16 y`,
    `u8 state` índice em `CHEST_STATE`).

    **Os escalares ficam na parte 0 de propósito**: um snapshot sem jogadores não existe, então a
    parte 0 é a única que nunca é vazia; e as partes 1 e 2 carregam o próprio `tick`, então perder
    a parte 0 não as invalida.

    **A API:** `SnapshotRecord` (a forma inteira legível, um comentário por campo dizendo a
    largura e a conversão, no molde de `InputRecord`), `extractSnapshot(world: World):
    SnapshotRecord`, `encodeSnapshot(record: SnapshotRecord): ArrayBuffer[]` (sempre três),
    `decodeSnapshotPart(bytes: ArrayBuffer): SnapshotPartRecord` e `decodeSnapshot(parts:
    ArrayBuffer[]): SnapshotRecord`. A hidratação de volta a `World` é **fase 4** e não entra
    aqui — reconstruir um `Enemy` completo exige `ENEMY_DEFS` em runtime, o que faria
    `@dg2/protocol` depender do bundle do sim. Escreva isso no cabeçalho.

    A normalização de `-0` é estrutural, não uma linha extra: `Math.round(x * POS_SCALE) | 0` mais
    o clamp em 0 já a resolvem, no idioma exato de `inputCodec.ts:72`. Erros em português.
    Acrescente `export * from './snapshotCodec';` ao barrel.

    Escreva `tests/snapshot-codec.test.ts` no estilo de `tests/input-codec.test.ts`, incluindo o
    cabeçalho que **proíbe o matcher de tolerância** e diz por quê, e um `expectDeepIs` que
    compara campo a campo com `Object.is`. Cobre todos os itens de `<behavior>` acima.
  </action>

  <verify>
    <automated>npx vitest run tests/snapshot-codec.test.ts &amp;&amp; npm run typecheck:protocol &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/snapshot-codec.test.ts` sai com código 0
    - A saída inclui um teste cujo nome contém `16` e afirma o teto por parte (SYNC-04)
    - `grep -c "toBeCloseTo" tests/snapshot-codec.test.ts` retorna 0
    - `grep -c "Object.is" tests/snapshot-codec.test.ts` retorna 5 ou mais
    - `packages/protocol/src/snapshotCodec.ts` contém `POS_SCALE = 8` e `SNAPSHOT_MAX_BYTES`
    - `packages/protocol/src/snapshotCodec.ts` contém `import { AIM_STEP` (ou equivalente `import type`/valor de `./inputCodec`) e **não** contém `const AIM_STEP =`
    - `grep -v '^\s*[/*]' packages/protocol/src/snapshotCodec.ts | grep -cE "obstacles|traps|world\.play|world\.config"` retorna 0
    - `grep -cE "Math\.(sin|cos|atan2|hypot)" packages/protocol/src/snapshotCodec.ts` retorna 0
    - `packages/protocol/src/index.ts` contém `export * from './snapshotCodec';`
    - `npx vitest run tests/purity.test.ts -t 'dependencies'` sai 0 (`packages/protocol` continua `dependencies: {}`)
    - `npm run typecheck:protocol` sai 0
  </acceptance_criteria>

  <done>
    O mesmo mundo que ocupa 82 KiB em JSON atravessa três mensagens somando menos de 3 KiB, e o
    teste prova bit a bit que nada se perdeu.
  </done>
</task>

<task type="auto">
  <name>Task 3: A partição exercida, os limites do decodificador e o round-trip nos três motores</name>
  <files>tests/snapshot-codec.test.ts, tests/cross-engine.test.ts, packages/protocol/src/snapshotCodec.ts</files>

  <read_first>
    - `packages/protocol/src/inputCodec.ts` linhas 400-430 (`decodeLog`: o cálculo de `room` e a recusa antes de alocar — o padrão a copiar, não a reinventar)
    - `tests/input-codec.test.ts` linhas 58-73 (`craftHeader`: construir bytes à mão para testar a recusa sem materializar o payload)
    - `tests/cross-engine.test.ts` **inteiro** (o cabeçalho que diz por que este é o único teste que pode falhar pelo motivo que existe para pegar; `buildWorld` nas linhas 49-66)
    - `vitest.browser.config.ts` (`include: ['tests/cross-engine.test.ts']` — **um arquivo só**; é por isso que o round-trip entra dentro dele)
    - `vitest.config.ts` (que exclui `cross-engine.test.ts` do runner de Node, de propósito)
    - `packages/protocol/src/snapshotCodec.ts` (criado na tarefa 2)
  </read_first>

  <action>
    **(a) Limites do decodificador (T-3-08).** Em `decodeSnapshotPart`, antes de alocar qualquer
    array, cada contagem declarada (`playerCount`, `enemyCount`, `bulletCount`,
    `enemyBulletCount`, `coinCount`, `potionCount`, `chestCount`) ganha o seu `room`, calculado
    como `Math.floor((bytes restantes) / (tamanho mínimo de um registro))`, exatamente como
    `decodeLog` faz. Se a contagem declarada passar do `room`, lance erro em português dizendo os
    dois números. Recuse também: comprimento menor que o cabeçalho; `part` fora de `SNAPSHOT_PART`;
    `kind` diferente do índice de `'snapshot'`; `baselineTick` diferente de 0 (com o texto citando
    D3-18). Os testes constroem os bytes à mão, no molde de `craftHeader`, para provar a recusa sem
    materializar 60.000 inimigos.

    **(b) A partição exercida.** Um teste chamado com "partição" no nome monta um mundo forçado —
    contagens declaradas no próprio teste, não vindas de `tests/worlds.ts` — e prova quatro coisas:
    `encodeSnapshot` devolve exatamente 3 partes; cada uma decodifica sozinha por
    `decodeSnapshotPart` sem as outras; descartar a parte 0 e decodificar só as partes 1 e 2 ainda
    devolve projéteis e loot corretos (é a propriedade "sem remontagem" de D3-19); e a parte 0
    cruza `SNAPSHOT_MAX_BYTES` com cerca de 1014 inimigos — o teste faz a busca e assere que o
    ponto de virada está entre 900 e 1100, com o comentário dizendo que isso pina a frase "só morde
    na wave ~210" do cabeçalho do codec, e que a folga da faixa é porque o número exato depende do
    número de jogadores.

    **(c) Cross-engine.** Acrescente um `describe` a `tests/cross-engine.test.ts` — **não** um
    arquivo novo, porque `vitest.browser.config.ts:10` tem `include` de um arquivo só e mudar o
    `include` seria uma segunda decisão no mesmo commit. O `describe` codifica
    `wave16SwarmElite()`, compara os bytes das três partes contra um ouro versionado gravado do
    Node, e faz o round-trip duplo. Se o ouro precisar de um arquivo, ele vai em
    `tests/snapshots/`, nunca em `tests/golden/` (aquele diretório é reservado a hashes de
    simulação, e o cabeçalho de `tools/golden/rebaseline.mjs` explica por quê).

    Se `tests/worlds.ts` não puder ser importado no browser mode por alguma restrição do runner,
    **não** duplique os construtores: mova a fixture para um JSON em `tests/snapshots/` gerado por
    um teste do Node e leia-o dos dois lados, e escreva o motivo no arquivo.
  </action>

  <verify>
    <automated>npx vitest run tests/snapshot-codec.test.ts &amp;&amp; npm run test:browser &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/snapshot-codec.test.ts -t "partição"` sai 0 e executa pelo menos 1 teste
    - `npx vitest run tests/snapshot-codec.test.ts` sai 0 e a saída inclui testes de recusa nomeando `baselineTick` e `contagem`
    - `npm run test:browser` sai 0 e a saída lista os três projetos (`chromium`, `firefox`, `webkit`)
    - `git diff --name-only` do commit desta tarefa **não** inclui `vitest.browser.config.ts`
    - `tests/cross-engine.test.ts` contém `encodeSnapshot`
    - `packages/protocol/src/snapshotCodec.ts` contém a palavra `room` num comentário ou identificador de limite, e pelo menos 4 `throw new Error(` com mensagem em português
    - `npm test` sai 0
  </acceptance_criteria>

  <done>
    O decodificador recusa um payload mentiroso antes de alocar um byte, a partição deixou de ser
    código sem teste, e os quatro motores concordam byte a byte.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer → peer (DataChannel `unreliable`) | O snapshot chega de uma máquina remota que pode ter sido modificada; toda contagem declarada é entrada não confiável |
| codec → renderizador | Um índice de enum fora de faixa vira acesso a `undefined` no consumidor se não for recusado aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | Denial of Service | `decodeSnapshotPart` | mitigate | Cada contagem declarada é comparada contra o `room` calculado dos bytes recebidos **antes** de qualquer alocação, no padrão literal de `decodeLog` (`inputCodec.ts:402-428`); testes constroem os bytes à mão para provar a recusa |
| T-3-08b | Denial of Service | mensagem grande no canal `unreliable` | mitigate | `SNAPSHOT_MAX_BYTES = 16 * 1024` é constante exportada e asserida por parte na wave 16 e na wave 40; o portão de CI é o plano 03-05 |
| T-3-13 | Tampering | índice de enum fora de faixa | mitigate | `part`, `kind`, `phase`, `cls`, `type`, `elite`, `bossState`, `state` são recusados quando fora do comprimento da tabela congelada, com erro em português |
| T-3-09 | Information Disclosure | o que o snapshot carrega | mitigate | `obstacles`, `traps`, `play` e `config` não têm representação no formato (D3-17); nenhum dado de conta entra no snapshot |
| T-3-12 | Tampering | autoridade emitindo snapshot forjado | accept | Fora do escopo por desenho: a autoridade é fonte da verdade da sessão, não da conta; nada durável passa por ela nesta fase |
| T-3-SC | Tampering | instalação de pacotes | mitigate | **Nenhum pacote é instalado neste plano.** `packages/protocol` continua com `dependencies: {}`, asserido por `tests/purity.test.ts` |
</threat_model>

<verification>
- `npm run lint` sai 0
- `npm test` sai 0
- `npm run test:browser` sai 0 nos três motores
- `npx tsc --noEmit` sai 0
- `npm run typecheck:protocol` sai 0
- `npx vitest run tests/purity.test.ts tests/workspaces.test.ts` sai 0
</verification>

<success_criteria>
- Cada uma das três partes de `wave16SwarmElite()` mede menos de 16384 bytes
- Cada uma das três partes de `wave40Endless()` mede menos de 16384 bytes
- Round-trip duplo verde, com `Object.is` e sem nenhum matcher de tolerância no arquivo
- O ponto em que a parte 0 cruza 16 KiB está medido e pinado entre 900 e 1100 inimigos
- Os quatro motores (Node, Chromium, Firefox, WebKit) produzem os mesmos bytes
- `packages/protocol/package.json` continua com `"dependencies": {}`
</success_criteria>

<output>
Crie `.planning/phases/03-sala-transporte-e-protocolo/03-02-SUMMARY.md` ao terminar.
</output>

---
phase: 03-sala-transporte-e-protocolo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/protocol/src/roomCode.ts
  - packages/protocol/src/enums.ts
  - packages/protocol/src/signaling.ts
  - packages/protocol/src/version.ts
  - packages/protocol/src/index.ts
  - tests/room-code.test.ts
  - tests/protocol-enums.test.ts
  - tests/protocol-vocabulary.test.ts
  - tests/snapshots/protocol-enums.json
autonomous: true
requirements: [SALA-01, SALA-05, SYNC-04]

must_haves:
  truths:
    - "Um código digitado como `abc-123` vira `ABC123`, e um código com `U` é recusado antes de sair da máquina (SALA-01)"
    - "`MSG_KIND` tem dez entradas, com `ping` no índice 8 e `pong` no índice 9, e o golden concorda (D3-13)"
    - "Toda tabela de entidade que o codec vai indexar existe congelada e bate, na ordem, com a fonte do sim (SYNC-04)"
    - "O vocabulário do signaling é uma tabela congelada, e os corpos das mensagens são tipos sem runtime (D3-09, D3-14)"
    - "`PROTOCOL_VERSION` subiu, então uma aba velha e uma nova recusam-se sem bypass (D-08)"
  artifacts:
    - path: "packages/protocol/src/roomCode.ts"
      provides: "Alfabeto, comprimento, normalização e validação do código de sala"
      exports: ["ROOM_CODE_ALPHABET", "ROOM_CODE_LENGTH", "normalizeRoomCode", "isRoomCode"]
    - path: "packages/protocol/src/signaling.ts"
      provides: "Os tipos dos doze corpos de mensagem do signaling, sem runtime"
    - path: "packages/protocol/src/enums.ts"
      provides: "MSG_KIND com ping/pong e as quinze tabelas congeladas"
      contains: "SNAPSHOT_PART"
    - path: "tests/snapshots/protocol-enums.json"
      provides: "O ouro das tabelas, atualizado no mesmo commit de cada append"
      contains: "ICE_CANDIDATE_TYPE"
  key_links:
    - from: "packages/protocol/src/enums.ts"
      to: "tests/protocol-enums.test.ts"
      via: "registro no objeto TABLES"
      pattern: "TABLES[\\s\\S]*ENEMY_TYPE"
    - from: "packages/protocol/src/index.ts"
      to: "packages/protocol/src/roomCode.ts"
      via: "re-export do barrel"
      pattern: "export \\* from './roomCode'"
    - from: "packages/protocol/src/enums.ts"
      to: "packages/sim/src/defs/enemies.ts"
      via: "teste de pinagem contra ENEMY_DEFS, na ordem"
      pattern: "Object\\.keys\\(ENEMY_DEFS\\)"
---

<objective>
Congelar o vocabulário do fio desta fase antes que qualquer código escreva um byte com ele:
o código de sala (alfabeto, comprimento, normalização), as quinze tabelas de enum que o codec
e o signaling vão indexar, o append de `ping`/`pong` à `MSG_KIND`, os tipos dos corpos de
mensagem do signaling e o bump de `PROTOCOL_VERSION`.

Purpose: o índice de um nome numa tabela congelada **é** o valor que viaja. Congelar depois
de já existir tráfego é reinterpretar em silêncio toda mensagem gravada — e esta é a última
janela em que a edição custa horas em vez de migração.

Output: `packages/protocol` fechado para esta fase, com o golden atualizado no mesmo commit
de cada append, e um código de sala que o jogador consegue ditar por voz sem ambiguidade.
</objective>

## Phase Goal

**As a** jogador que quer jogar com amigos, **I want to** criar uma sala, receber um código
curto e ver meus amigos entrarem e escolherem classe no lobby antes de eu iniciar a run,
**so that** nós quatro comecemos a mesma run no mesmo mundo, sobre um fio cujo formato já
está medido e congelado.

> Derivado do `**Goal:**` da fase 3 no ROADMAP e dos critérios 1, 2 e 5. O ROADMAP não traz a
> forma de user story; ver a nota do planejador no retorno da orquestração.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sala-transporte-e-protocolo/03-CONTEXT.md
@.planning/phases/03-sala-transporte-e-protocolo/03-PATTERNS.md
@.planning/phases/03-sala-transporte-e-protocolo/03-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
Contratos que já existem e que este plano estende — leia-os no arquivo, não presuma a forma.

De `packages/protocol/src/enums.ts`:
- `MSG_KIND` (8 entradas hoje: hello, welcome, reject, lobbyState, startRun, input, snapshot, ack)
- `REJECT_REASON` (simVersion, protocolVersion, roomFull, roomClosed, badCode)
- `CHANNEL_CLASS` (reliable, unreliable)
- `OBJECTIVE_KIND` (none, defend, hunt, purge, fetch, extract) — o molde de toda tabela nova

De `packages/protocol/src/version.ts`:
- `PROTOCOL_VERSION: string` (hoje `'1'`), `Versions`, `VersionMismatch`, `checkVersions`

De `packages/sim` (fontes de pinagem, todas em runtime):
- `ENEMY_DEFS` (Record, 11 chaves), `ELITE_TYPES` (Record, 3 chaves), `CLASS_KEYS` (array de 7),
  `MUTATORS` (Record, 5 chaves)
- Tipos de união sem contraparte em runtime: `ClassKey`, `AttackKind`, `MutatorKey`, `Phase`,
  `GameMode`, `PlayerSlot`, `Chest['state']`, `Obstacle['kind']`, `Enemy['bossState']` (que é
  `string`, não união — ver a tarefa 3)

**Colisão de nomes já existente, medida no código — regra que a tarefa 3 obedece.**
`packages/protocol/src/runEnvelope.ts:56` **já exporta** um tipo chamado `PlayerSlot`, e é um
`{ id, cls, name }`, não o id do slot; `packages/sim/src/types.ts:243` exporta outro
`PlayerSlot`, que é `'p0' | 'p1' | 'p2' | 'p3'`. O comentário do sim já registra a colisão e diz
que os dois nunca entram no mesmo arquivo. O mesmo vale para `ClassKey`, `Phase`, `GameMode`,
`AttackKind` e `MutatorKey`, que já existem em `@dg2/sim`. **Consequência:** as onze tabelas que
espelham o sim exportam **só o array**, nunca um `export type` derivado — o tipo já existe do
lado do sim, e um segundo com o mesmo nome no barrel de `@dg2/protocol` seria exatamente a
segunda grafia que as tabelas existem para impedir. Só as quatro tabelas sem contraparte
(`SNAPSHOT_PART`, `SIGNAL_KIND`, `ICE_ROUTE`, `ICE_CANDIDATE_TYPE`) exportam tipo derivado.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: O código de sala como módulo do protocolo</name>
  <files>packages/protocol/src/roomCode.ts, packages/protocol/src/index.ts, tests/room-code.test.ts</files>

  <read_first>
    - `src/app/ulid.ts` (o alfabeto Crockford já existe na linha 20; leia o comentário que o acompanha)
    - `tests/ulid.test.ts` (o analog do teste: vetores conhecidos, injeção de dependência)
    - `packages/protocol/src/inputCodec.ts` linhas 1-40 (a forma do cabeçalho que carrega a decisão)
    - `packages/protocol/src/index.ts` (o barrel e o comentário que diz o que este pacote é)
    - `packages/protocol/tsconfig.json` (`lib: ["ES2022"]`, `types: []` — o que está e o que não está disponível)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-RESEARCH.md` § "Recommendations for Claude's Discretion" #1
  </read_first>

  <behavior>
    - `normalizeRoomCode('abc-123')` devolve `'ABC123'` (minúscula sobe, hífen e espaço somem)
    - `normalizeRoomCode('OIL123')` devolve `'011123'` (Crockford: `O`→`0`, `I`→`1`, `L`→`1`)
    - `normalizeRoomCode('ABCDEU')` devolve `null` (`U` é recusado, nunca mapeado)
    - `normalizeRoomCode('ABC12')` devolve `null` (comprimento diferente de 6)
    - `normalizeRoomCode('ABC1234')` devolve `null`
    - `normalizeRoomCode('')` devolve `null`
    - `isRoomCode('ABC123')` é `true`; `isRoomCode('abc123')` é `false` (a forma canônica é maiúscula e já normalizada)
    - Todo caractere de `ROOM_CODE_ALPHABET` sobrevive a `normalizeRoomCode` de um código de 6 formado por ele
    - `ROOM_CODE_ALPHABET.length` é 32 e não contém `I`, `L`, `O` nem `U`
  </behavior>

  <action>
    Crie `packages/protocol/src/roomCode.ts` exportando exatamente quatro nomes:
    `ROOM_CODE_ALPHABET = '0123456789ABCDEFGHJKMNPQRSTVWXYZ'`, `ROOM_CODE_LENGTH = 6`,
    `normalizeRoomCode(raw: string): string | null` e `isRoomCode(value: string): boolean`.

    `normalizeRoomCode` remove espaços em branco e hífens, sobe para maiúscula, aplica o mapa de
    decodificação Crockford (`I`→`1`, `L`→`1`, `O`→`0`), recusa `U` sem mapeá-lo, e devolve
    `null` se o resultado não tiver exatamente `ROOM_CODE_LENGTH` caracteres, todos em
    `ROOM_CODE_ALPHABET`. `isRoomCode` é a checagem estrita da forma canônica: comprimento exato
    e todo caractere no alfabeto, sem normalizar nada.

    O cabeçalho do arquivo, em inglês, tem de dizer três coisas com ênfase: (a) o código é a
    **única** credencial da sala nesta fase (D3-09), e é por isso que ele tem 32^6 = 1.073.741.824
    valores e depende do rate limit do `join` para não ser varrível (T-3-01); (b) a geração mora
    no servidor, com `crypto`, e **nunca** aqui — este módulo só normaliza e valida, e é o que
    permite ao pacote manter `dependencies: {}` e `types: []`; (c) este alfabeto é uma **segunda
    cópia independente** do que existe em `src/app/ulid.ts:20`, deliberadamente não compartilhada,
    porque acoplá-las faria uma mudança de protocolo mexer no formato do id do ledger.

    Acrescente `export * from './roomCode';` a `packages/protocol/src/index.ts`, mantendo a lista
    em ordem alfabética por especificador, como o arquivo já faz.

    Escreva `tests/room-code.test.ts` no estilo de `tests/ulid.test.ts`: `describe` e `it` em
    português com o identificador do requisito entre parênteses (`'código de sala (SALA-01)'`),
    tabela de casos, mensagens de erro em português. Cobre todos os comportamentos listados em
    `<behavior>`.
  </action>

  <verify>
    <automated>npx vitest run tests/room-code.test.ts tests/protocol-vocabulary.test.ts &amp;&amp; npm run typecheck:protocol &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `packages/protocol/src/roomCode.ts` contém a string literal `'0123456789ABCDEFGHJKMNPQRSTVWXYZ'`
    - `packages/protocol/src/roomCode.ts` contém `export const ROOM_CODE_LENGTH = 6`
    - `packages/protocol/src/index.ts` contém a linha `export * from './roomCode';`
    - `npx vitest run tests/room-code.test.ts` sai com código 0 e reporta pelo menos 8 testes
    - `npx vitest run tests/protocol-vocabulary.test.ts` sai com código 0 (FORM-12 continua verde)
    - `npm run typecheck:protocol` sai com código 0
    - `npx vitest run tests/purity.test.ts -t 'dependencies'` sai com código 0 (`packages/protocol` continua `dependencies: {}`)
    - `grep -n "randomBytes\|randomInt\|crypto" packages/protocol/src/roomCode.ts` não retorna nada
  </acceptance_criteria>

  <done>
    O jogador pode digitar o código como quiser — minúsculo, com hífen, trocando `O` por zero — e
    o cliente devolve a forma canônica ou recusa antes de gastar uma ida ao servidor.
  </done>
</task>

<task type="auto">
  <name>Task 2: `ping`/`pong`, as quatro tabelas de fio novas, os tipos do signaling e o bump de versão</name>
  <files>packages/protocol/src/enums.ts, packages/protocol/src/signaling.ts, packages/protocol/src/version.ts, packages/protocol/src/index.ts, tests/protocol-enums.test.ts, tests/protocol-vocabulary.test.ts, tests/snapshots/protocol-enums.json</files>

  <read_first>
    - `packages/protocol/src/enums.ts` **inteiro** (o cabeçalho de doutrina das linhas 1-28 não se reescreve, só se estende; `OBJECTIVE_KIND` nas linhas 81-102 é o molde)
    - `packages/protocol/src/version.ts` (o comentário do bump, linhas 20-29)
    - `tests/protocol-enums.test.ts` **inteiro** (o objeto `TABLES`, o texto `HOWTO`, os testes de pinagem por tabela)
    - `tests/snapshots/protocol-enums.json` (formato exato: array por tabela, indentação 2, ordem de declaração)
    - `tests/protocol-vocabulary.test.ts` **inteiro** (o regex `FORBIDDEN`, o guarda anti-vacuidade, o teste que testa o detector, e o comentário das linhas 70-82 que esta fase torna falso)
    - `packages/protocol/src/runEnvelope.ts` linhas 47-60 (a forma de um tipo de fio)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-RESEARCH.md` § Discretion #3, #9, #11 e #12
  </read_first>

  <action>
    Tudo desta tarefa entra num commit só — a tabela e o ouro nunca se separam.

    **(a) Append à `MSG_KIND`.** Acrescente `'ping'` e `'pong'` **no fim**, virando os índices 8 e
    9; a tabela passa a ter 10 entradas. Estenda o bloco de doc da tabela dizendo que `ping`/`pong`
    são mensagens **do jogo**, no DataChannel `unreliable`, e que **não** são os frames de
    controle `ping`/`pong` do protocolo WebSocket usados pelo heartbeat do signaling — são duas
    coisas com o mesmo nome em duas camadas (D3-13).

    **(b) Quatro tabelas novas em `enums.ts`**, cada uma com bloco de doc no molde de
    `OBJECTIVE_KIND` (o que espelha, por que existe, e por que o índice 0 é o que é):
    - `SNAPSHOT_PART = ['actors', 'projectiles', 'pickups']` — as três partes de D3-19. O doc diz
      que a partição é por classe de entidade, é **auto-contida** (perder uma parte não invalida
      as outras) e **não é** interest management.
    - `SIGNAL_KIND = ['create', 'created', 'join', 'joined', 'peers', 'offer', 'answer',
      'candidate', 'leave', 'closed', 'iceOutcome', 'error']` — doze entradas. O doc diz que esta
      é a perna do **servidor** (WebSocket) e `MSG_KIND` é a perna do **peer** (DataChannel), que
      são duas tabelas porque são dois fios, e que o servidor relaya `offer`/`answer`/`candidate`
      como bytes opacos sem nunca parsear SDP.
    - `ICE_ROUTE = ['unknown', 'direct', 'relay']` — `unknown` no índice 0, com o motivo repetido
      (não linkado): um campo zerado decodifica para "não sei", nunca para "direto".
    - `ICE_CANDIDATE_TYPE = ['host', 'srflx', 'prflx', 'relay']` — os nomes do W3C / RFC 8445.

    Estas quatro **exportam** o tipo derivado — `export type SnapshotPart = typeof
    SNAPSHOT_PART[number]`, e o mesmo para `SignalKind`, `IceRoute` e `IceCandidateType` —
    porque nenhum desses quatro nomes existe hoje em `@dg2/sim` nem em `@dg2/protocol`. As onze
    tabelas da tarefa 3 **não** exportam tipo derivado; ver a nota de colisão em `<interfaces>`.

    **(c) A exceção nomeada de FORM-12.** A entrada `'host'` de `ICE_CANDIDATE_TYPE` casa o regex
    de `tests/protocol-vocabulary.test.ts` e quebraria o portão. A resolução, que **não** é
    afrouxar o regex: a linha que contém `'host'` em `enums.ts` leva o marcador literal
    `FORM-12-EXEMPT` num comentário de fim de linha, com a justificativa "RFC 8445 candidate type,
    not this project's topology"; e `tests/protocol-vocabulary.test.ts` passa a ignorar **apenas**
    linhas que contenham esse marcador, mais dois testes novos: (1) todo arquivo com o marcador
    está numa lista literal declarada no teste, que nesta tarefa é exatamente
    `['enums.ts']`; (2) toda linha com o marcador contém também a substring `RFC 8445`. Um `host`
    sem marcador continua falhando, e o teste que testa o detector ganha esse caso.

    **(d) `packages/protocol/src/signaling.ts`** — só `type`, zero runtime, zero import de valor.
    Um tipo por entrada de `SIGNAL_KIND`, mais `TurnCredential` (`username`, `credential`, `ttl`)
    e `IceConfig` (a lista de `iceServers` como dados puros, sem tocar em `RTCIceServer`, que é um
    tipo de DOM e não existe neste `tsconfig`). Duas decisões que precisam ficar escritas no
    arquivo: (1) `Created` e `Joined` carregam **`authorityPeerId` explícito** — a autoridade
    **nunca** é derivada do slot, porque FORM-12 diz que o protocolo não codifica a topologia, e é
    isso que faz mover a autoridade para um servidor dedicado ser troca de construtor; (2)
    `IceOutcome` carrega `id` (ULID gerado no cliente, idempotência) e **não carrega** `address`
    nem `port` — são IPs de jogador, e D3-14 limita o dado pessoal ao ULID. Acrescente
    `export * from './signaling';` ao barrel.

    **(e) `PROTOCOL_VERSION` sobe de `'1'` para `'2'`.** No comentário existente, acrescente a
    frase que diz o que mudou nesta fase: duas entradas novas no fim de `MSG_KIND`, o
    enquadramento binário do snapshot e o vocabulário de signaling. Um bump só cobre a fase
    inteira; não suba de novo nos planos seguintes.

    **(f) Ouro e registro.** Acrescente `SNAPSHOT_PART`, `SIGNAL_KIND`, `ICE_ROUTE` e
    `ICE_CANDIDATE_TYPE` ao objeto `TABLES` de `tests/protocol-enums.test.ts` **e** a
    `tests/snapshots/protocol-enums.json`, e atualize `MSG_KIND` no ouro com as duas entradas
    novas. Acrescente os testes de pinagem no estilo dos existentes: `MSG_KIND[8]` é `'ping'`,
    `MSG_KIND[9]` é `'pong'`, `MSG_KIND.length` é 10, `SNAPSHOT_PART[0]` é `'actors'`,
    `ICE_ROUTE[0]` é `'unknown'`, `SIGNAL_KIND.length` é 12.
  </action>

  <verify>
    <automated>npx vitest run tests/protocol-enums.test.ts tests/protocol-vocabulary.test.ts &amp;&amp; npm run typecheck:protocol &amp;&amp; npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `node -e "import('@dg2/protocol').then(m=>console.log(m.MSG_KIND.length, m.MSG_KIND[8], m.MSG_KIND[9]))"` não é exigido; em vez disso: `npx vitest run tests/protocol-enums.test.ts` sai 0 e os testes `MSG_KIND[8]`/`MSG_KIND[9]` aparecem verdes na saída
    - `grep -c "SNAPSHOT_PART\|SIGNAL_KIND\|ICE_ROUTE\|ICE_CANDIDATE_TYPE" tests/snapshots/protocol-enums.json` retorna 4 ou mais
    - `tests/snapshots/protocol-enums.json` contém as strings `"ping"` e `"pong"` dentro do array `"MSG_KIND"`, nessa ordem, como últimas entradas
    - `packages/protocol/src/version.ts` contém `export const PROTOCOL_VERSION: string = '2';`
    - `packages/protocol/src/signaling.ts` contém `authorityPeerId` e **não** contém as substrings `address` nem `port`
    - `grep -c "FORM-12-EXEMPT" packages/protocol/src/enums.ts` retorna 1
    - `grep -v '^\s*//' packages/protocol/src/signaling.ts | grep -c "^import type" ` retorna pelo menos 1 e `grep -v '^\s*//' packages/protocol/src/signaling.ts | grep -c "^import {"` retorna 0 (nenhum import de valor)
    - `npx vitest run tests/protocol-vocabulary.test.ts` sai 0, e a saída inclui um teste novo cujo nome menciona `FORM-12-EXEMPT`
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>

  <done>
    Uma mensagem `ping` tem um número de fio congelado, o servidor tem doze verbos nomeados, e
    duas abas de builds diferentes se recusam mutuamente pelo `PROTOCOL_VERSION` novo.
  </done>
</task>

<task type="auto">
  <name>Task 3: As onze tabelas que espelham o sim, pinadas na ordem</name>
  <files>packages/protocol/src/enums.ts, tests/protocol-enums.test.ts, tests/snapshots/protocol-enums.json</files>

  <read_first>
    - `packages/protocol/src/enums.ts` (já editado na tarefa 2 — leia o estado atual)
    - `packages/sim/src/defs/enemies.ts` linhas 1-45 (`ENEMY_DEFS` e `ELITE_TYPES`, na ordem literal)
    - `packages/sim/src/defs/classes.ts` linha 6 (`CLASS_KEYS`, que já é um array em runtime)
    - `packages/sim/src/defs/mutators.ts` linhas 1-12 (`MUTATORS`)
    - `packages/sim/src/types.ts` linhas 1-15 e 86-150 (as uniões `ClassKey`, `AttackKind`, `MutatorKey`, `Phase`, `GameMode`, `PlayerSlot`; e `Enemy.bossState`, que é `string`)
    - `packages/sim/src/boss.ts` linhas 60-130 (os quatro literais de `bossState` que existem de fato)
    - `packages/sim/src/types.ts` § `Chest` e § `Obstacle` (os campos `state` e `kind`)
    - `tests/protocol-enums.test.ts` (o `TABLES` já estendido na tarefa 2)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-RESEARCH.md` § Discretion #9 (a tabela de cardinalidades medidas)
  </read_first>

  <action>
    Acrescente onze tabelas a `enums.ts`, todas append-only, cada uma com bloco de doc no molde de
    `OBJECTIVE_KIND` dizendo o que espelha e o que quebra se a ordem mudar. Valores e ordem
    exatos:

    - `ENEMY_TYPE`: skeleton, goblin, demon, brute, mimic, necromancer, swampy, zombie_king,
      ogre_warlord, goblin_chief, necro_lord (11)
    - `ELITE_TYPE`: none, swift, brutish, vampiric (4) — `'none'` no índice 0, com o motivo repetido
    - `BOSS_STATE`: chase, telegraph, charging, recover (4)
    - `CHEST_STATE`: closed, opening, looted (3)
    - `OBSTACLE_KIND`: column, crate (2)
    - `ATTACK_KIND`: melee, bolt, arrow, bullet, fireball (5)
    - `CLASS_KEY`: mage, archer, warrior, ninja, priestess, witch, coprobo (7)
    - `MUTATOR_KEY`: none, swarm, frenzy, bounty, elite, fog (6) — `'none'` no índice 0, motivo repetido
    - `PHASE`: playing, levelup, shop, gameover, victory (5)
    - `GAME_MODE`: campaign, endless (2)
    - `PLAYER_SLOT`: p0, p1, p2, p3 (4)

    **Nenhuma destas onze exporta `export type`.** O tipo já existe em `@dg2/sim`
    (`ClassKey`, `Phase`, `GameMode`, `AttackKind`, `MutatorKey`, `PlayerSlot`), e
    `packages/protocol/src/runEnvelope.ts:56` já ocupa o nome `PlayerSlot` com outra coisa. Um
    segundo `export type ClassKey` saindo do barrel de `@dg2/protocol` seria a segunda grafia
    que estas tabelas existem para impedir. Escreva esse motivo uma vez, no doc de `CLASS_KEY`,
    e cite-o nas outras dez.

    Duas notas obrigatórias nos docs: (1) `ENEMY_TYPE` congela dentro de **todo replay guardado a
    partir da fase 4** — inserir um inimigo no meio da lista invalida replay armazenado; (2)
    `OBSTACLE_KIND` nasce sem consumidor de propósito, porque D3-17 tira `obstacles` do fio, e
    existe para o dia em que a arena deixar de ser derivada da seed — escreva que é um julgamento
    e que a alternativa (não congelar o que nada escreve) era defensável.

    **Não** crie tabela para `anim`, `eliteName` nem `eliteTint`: os três são deriváveis de `type`
    e `elite`, que já viajam. Registre a ausência como decisão no doc de `ENEMY_TYPE`.

    Registre as onze em `TABLES` e no ouro `tests/snapshots/protocol-enums.json` nesta mesma
    tarefa. Acrescente os testes de pinagem contra o sim, e a pinagem é **na ordem**, nunca como
    conjunto:
    - runtime: `ENEMY_TYPE` contra `Object.keys(ENEMY_DEFS)`; `ELITE_TYPE.slice(1)` contra
      `Object.keys(ELITE_TYPES)`; `CLASS_KEY` contra `CLASS_KEYS`; `MUTATOR_KEY.slice(1)` contra
      `Object.keys(MUTATORS)`
    - tipo, com os helpers `Equal`/`Expect` declarados no próprio arquivo de teste (o mesmo par
      que `03-RESEARCH.md` § Discretion #3 propõe para o zod): `ClassKey`, `AttackKind`,
      `MutatorKey`, `Phase`, `GameMode` e `PlayerSlot` contra o `typeof TABELA[number]`
      correspondente. `MUTATOR_KEY` e `ELITE_TYPE` comparam a fatia sem o `'none'`.
    - `BOSS_STATE` **não** tem contraparte de tipo: `Enemy.bossState` é `string` em
      `packages/sim/src/types.ts`. Escreva isso no comentário do teste e pine só por valor, com a
      nota de que a união do lado do sim é dívida de um commit futuro que já mova `SIM_VERSION`.

    Nenhuma linha de `packages/sim` muda neste plano — a fronteira da fase proíbe mover
    `SIM_VERSION`.
  </action>

  <verify>
    <automated>npx vitest run tests/protocol-enums.test.ts tests/protocol-vocabulary.test.ts &amp;&amp; npm run typecheck:protocol &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/protocol-enums.test.ts` sai 0
    - `grep -c "^  \"[A-Z_]*\": \[" tests/snapshots/protocol-enums.json` retorna 19 (as 4 originais + 4 da tarefa 2 + 11 desta)
    - `packages/protocol/src/enums.ts` contém `export const ENEMY_TYPE`, `export const CLASS_KEY`, `export const PLAYER_SLOT` e `export const MUTATOR_KEY`
    - `packages/protocol/src/enums.ts` **não** contém `export const ANIM` nem `export const ELITE_NAME`
    - `grep -c "^export type" packages/protocol/src/enums.ts` retorna 8 (as 4 tabelas originais + as 4 da tarefa 2; nenhuma das onze desta tarefa acrescenta uma nona)
    - `tests/protocol-enums.test.ts` contém `Object.keys(ENEMY_DEFS)` e `CLASS_KEYS`
    - `git diff --name-only` no commit desta tarefa **não** lista nenhum arquivo sob `packages/sim/`
    - `npx tsc --noEmit` sai 0 (é este comando que executa as asserções `Equal`/`Expect`)
    - `npm test` sai 0
  </acceptance_criteria>

  <done>
    Todo enum que o codec da onda 2 vai indexar tem um número de fio congelado, e um `git diff` no
    sim que reordene inimigos ou classes quebra o build antes de quebrar um replay.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jogador → cliente | O código de sala digitado é entrada não confiável; normalizar e recusar antes de sair da máquina |
| peer → peer (DataChannel) | Todo índice de enum recebido é um inteiro de origem remota e precisa ser tratado como fora de faixa até prova em contrário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | `packages/protocol/src/roomCode.ts` | mitigate | Alfabeto de 32 e comprimento 6 fixados aqui como constantes exportadas (32^6 = 1.073.741.824); o rate limit que fecha a mitigação é do plano 03-04 e está citado no cabeçalho do módulo |
| T-3-07 | Tampering | `CLASS_KEY`, `PLAYER_SLOT`, `MUTATOR_KEY` | mitigate | Tabelas fechadas dão ao cliente um domínio finito para validar `lobbyState` vindo de um peer, em vez de aceitar qualquer string |
| T-3-08 | Denial of Service | `SNAPSHOT_PART` | mitigate | O índice de parte é um dos três valores da tabela; o decodificador do plano 03-02 recusa qualquer outro antes de alocar |
| T-3-09 | Information Disclosure | `packages/protocol/src/signaling.ts` (`IceOutcome`) | mitigate | O tipo do corpo **não declara** `address` nem `port`, então o compilador recusa quem tentar enviá-los (D3-14) |
| T-3-12 | Tampering | autoridade emitindo `lobbyState`/`startRun` | accept | Fora do escopo por desenho: a autoridade é fonte da verdade da **sessão**, não da **conta**; nada durável passa por ela nesta fase |
| T-3-SC | Tampering | instalação de pacotes | mitigate | **Nenhum pacote é instalado neste plano.** `packages/protocol` mantém `dependencies: {}`, asserido por `tests/purity.test.ts` e `tests/workspaces.test.ts` |
</threat_model>

<verification>
- `npm run lint` sai 0
- `npm test` sai 0
- `npx tsc --noEmit` sai 0
- `npm run typecheck:protocol` sai 0
- `npx vitest run tests/protocol-enums.test.ts tests/protocol-vocabulary.test.ts tests/room-code.test.ts tests/purity.test.ts tests/workspaces.test.ts` sai 0
- `tests/snapshots/protocol-enums.json` mudou no mesmo commit de cada tabela nova
</verification>

<success_criteria>
- `MSG_KIND` tem 10 entradas com `ping` em 8 e `pong` em 9
- 19 tabelas registradas em `TABLES` e no ouro, batendo valor a valor na ordem
- `normalizeRoomCode` cobre os nove casos de `<behavior>` da tarefa 1
- `PROTOCOL_VERSION` é `'2'`
- Nenhum arquivo de `packages/sim` foi modificado
- `packages/protocol/package.json` continua com `"dependencies": {}`
</success_criteria>

<output>
Crie `.planning/phases/03-sala-transporte-e-protocolo/03-01-SUMMARY.md` ao terminar.
</output>

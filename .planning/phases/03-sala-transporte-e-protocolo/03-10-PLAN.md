---
phase: 03-sala-transporte-e-protocolo
plan: 10
type: execute
wave: 6
depends_on: ["03-02", "03-09"]
files_modified:
  - src/app/forge.ts
  - src/main.ts
  - src/net/lobby.ts
  - src/ui/room.ts
  - tests/run-config-lobby.test.ts
  - tests/net/room.spec.ts
  - tests/net/e2e-helpers.ts
  - tests/net/tsconfig.json
  - tests/pwa/room-url.spec.ts
  - playwright.config.ts
  - tsconfig.json
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [SALA-02, SALA-03]

must_haves:
  truths:
    - "Quem criou a sala aperta `▶ INICIAR` e todo mundo entra numa run montada do mesmo `RunConfig` — mesma seed, mesmos slots, mesmas classes (SALA-03, D3-05)"
    - "Cada máquina roda `createWorld` + `generateArena` localmente e manda o `hashWorld` do tick 0; divergência vira erro na tela com os dois hashes (D3-05)"
    - "`LOCAL_SLOT` deixou de ser constante: o slot vem do `startRun` recebido (D3-05, ADR 0001)"
    - "`buildRunConfig` monta os quatro jogadores e a seed vem da autoridade, não de `Math.random()` local"
    - "Dois contextos de navegador fecham uma sala por loopback e concordam no hash do tick 0 (SALA-02)"
    - "Um PWA instalado a partir de `/?sala=X` não fixa uma sala no `start_url` (D3-07)"
  artifacts:
    - path: "tests/net/room.spec.ts"
      provides: "A prova de ponta a ponta: dois browserContext, uma sala, hash do tick 0 igual"
    - path: "tests/run-config-lobby.test.ts"
      provides: "RunConfig montado pelo lobby produz o mesmo hashWorld nos dois lados"
    - path: "tests/pwa/room-url.spec.ts"
      provides: "O start_url resolvido não carrega ?sala="
  key_links:
    - from: "src/main.ts"
      to: "src/net/lobby.ts"
      via: "beginRun recebe o RunConfig e o slot do startRun em vez de montá-los"
      pattern: "beginRun\\("
    - from: "src/net/lobby.ts"
      to: "packages/sim/src/serialize.ts"
      via: "hashWorld do tick 0 trocado pelo canal reliable"
      pattern: "hashWorld"
    - from: "playwright.config.ts"
      to: "tests/net/room.spec.ts"
      via: "projeto novo com testDir tests/net"
      pattern: "tests/net"
---

<objective>
Fechar o laço: o lobby deixa de ser uma tela bonita e passa a iniciar a run. `startRun` leva só o
`RunConfig`, cada máquina constrói o mundo a partir da seed, e as duas comparam o `hashWorld` do
tick 0 antes de qualquer outra coisa.

Purpose: em vez de "confiar que a config chegou", cada cliente prova que chegou. É barato, é o
mesmo `hashWorld` do portão de três motores, e pega **no lobby** o que senão apareceria na fase 4
como "dessincronizou em 40 segundos". A run começa **local e sem sincronia** nesta fase — isso é
por desenho (D3-05, D3-18), e é exatamente onde a fase 4 encaixa.

Output: **os critérios de sucesso 1, 2 e a parte local do 4**, provados por dois navegadores de
verdade fechando uma sala por loopback.
</objective>

## Phase Goal

**As a** jogador que quer jogar com amigos, **I want to** criar uma sala, receber um código
curto e ver meus amigos entrarem e escolherem classe no lobby antes de eu iniciar a run,
**so that** nós quatro comecemos a mesma run no mesmo mundo, sobre um fio cujo formato já
está medido e congelado.

Esta é a última fatia local: depois dela a frase inteira é verdade, exceto pelo caminho de relay
contra a caixa, que é o plano 03-11.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sala-transporte-e-protocolo/03-CONTEXT.md
@.planning/phases/03-sala-transporte-e-protocolo/03-PATTERNS.md
@.planning/phases/03-sala-transporte-e-protocolo/03-UI-SPEC.md
@.planning/phases/03-sala-transporte-e-protocolo/03-VALIDATION.md
@.planning/phases/03-sala-transporte-e-protocolo/03-09-SUMMARY.md
@docs/adr/0001-identidade-em-tres-espacos.md
@CLAUDE.md
</context>

<interfaces>
Do plano 03-02: `extractSnapshot`/`encodeSnapshot` (não usados aqui — a run começa local e sem
sincronia nesta fase, D3-18). Do plano 03-03: `createLobby`. Do plano 03-08:
`createRtcTransport`, `createSignalingClient`. Do plano 03-09: `src/ui/room.ts` e o
`#desync-screen`.

De `packages/sim`, o caminho canônico de início de run, que `startRun` tem de reproduzir bit a
bit (é o mesmo do replay, D-11): `createWorld(config)` → `generateArena` → `createPlayer` para
cada slot de `config.players` → `startRun(world)`. `tests/cross-engine.test.ts:49-55` já o
canoniza numa função `buildWorld()` — copie a forma.

`hashWorld` de `packages/sim/src/serialize.ts` é a comparação do tick 0 e **já existe**: nenhum
código novo é necessário para a prova.

`RunConfig` (`packages/sim/src/types.ts:262-278`): `seed`, `mode`, e `players: RunPlayer[]` cuja
**ordem do array é a ordem canônica** (FORM-02/D-13) — `step()` itera esse array, não
`Object.keys(players)`, para que quem tira qual número de `world.rng` seja decidido pelo manifesto
da run e não pela ordem em que as pessoas entraram.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `buildRunConfig` de um jogador para quatro, e `LOCAL_SLOT` deixando de ser constante</name>
  <files>src/app/forge.ts, src/main.ts, tests/run-config-lobby.test.ts</files>

  <read_first>
    - `src/app/forge.ts` **inteiro**, com atenção às linhas 19-59: o doc-comment já descreve esta mudança ("quando a fase 4 fizer uma sala, a autoridade monta este array"), `slot` já é parâmetro por decisão, e a linha 37 tem um comentário que **viola FORM-12**
    - `src/main.ts` linhas 170-215 (o doc de `LOCAL_SLOT`, que já anuncia que esta é "a única linha que deixa de ser constante", e os quatro consumidores em `frame()`) e 230-260 (`beginRun`, com a regra "leia de volta do manifesto, não dos argumentos")
    - `tests/cross-engine.test.ts` linhas 45-70 (`buildWorld()` — a sequência canônica de início de run, e o molde deste teste)
    - `packages/sim/src/types.ts` linhas 243-300 (`PlayerSlot`, `RunPlayer`, `RunConfig` e o comentário sobre a ordem canônica)
    - `packages/sim/src/serialize.ts` (`hashWorld`)
    - `src/net/lobby.ts` (do plano 03-03 — `startRoom()` já monta o `RunConfig`)
  </read_first>

  <behavior>
    - `buildRunConfig` aceita a seed como argumento e **não** chama `Math.random()`
    - `buildRunConfig` monta `players` com um `RunPlayer` por ocupante, na ordem de slot `p0..p3`
    - Dois `RunConfig` idênticos produzem `World` com o mesmo `hashWorld` no tick 0
    - Dois `RunConfig` que diferem só na seed produzem `hashWorld` diferentes no tick 0
    - Dois `RunConfig` que diferem só na **ordem** de `players` produzem `hashWorld` diferentes — o que prova que a ordem do array é carga, não estilo
    - `beginRun` lê classe, nome e forge **do manifesto recebido**, nunca dos argumentos da chamada
    - `frame()` e `beginRun` usam o slot recebido; nenhum literal `'p0'` sobra fora do valor padrão do modo solo
  </behavior>

  <action>
    **`src/app/forge.ts`.** `buildRunConfig` passa a receber a **seed** e a lista de ocupantes, e
    devolve o `RunConfig` com os quatro `RunPlayer` na ordem de slot. O `Math.random()` da linha 41
    **sai**: a autoridade emite a seed e ela chega no `startRun`. Mantenha o modo solo funcionando
    — ele passa a ser uma sala de um jogador com uma seed gerada uma vez no ponto de entrada, e
    isso vai escrito no doc-comment, porque **solo passa a ser um caso de multiplayer** (D3-04) e é
    o que faz single-player e co-op compartilharem `beginRun`.

    **Corrija a linha 37 no mesmo commit** (FORM-12): o comentário `In Marco 1 the host picks it
    and sends it to every client` passa a dizer **authority**. `src/app/` não está no glob de
    `tests/protocol-vocabulary.test.ts` hoje, e é justamente por isso que a correção precisa ser
    deliberada — a linha está sendo editada de qualquer forma.

    **`src/main.ts`.** `LOCAL_SLOT` deixa de ser `const ... = 'p0'` e passa a ser uma variável
    definida quando o `startRun` chega, com `'p0'` como valor do caminho solo. Os quatro
    consumidores em `frame()` (`updateHud`, `syncScreens`, `render`) e o `beginRun` passam a
    **receber** o slot em vez de fechar sobre a constante. `beginRun` continua obedecendo a regra
    já escrita: `config.players[0]` vira `config.players.find(p => p.id === mySlot)`, porque uma
    run que começa de uma classe diferente da que grava é uma divergência que ninguém veria até o
    replay. Preserve o doc-comment de `LOCAL_SLOT` atualizando-o para dizer o que aconteceu, em vez
    de apagá-lo.

    **`tests/run-config-lobby.test.ts`** cobre os itens de `<behavior>`, no molde de
    `tests/cross-engine.test.ts:45-70`: monte o `RunConfig` pelo caminho do lobby, construa o mundo
    pela sequência canônica, e compare `hashWorld`. `describe`/`it` em português com o requisito
    entre parênteses.
  </action>

  <verify>
    <automated>npx vitest run tests/run-config-lobby.test.ts tests/golden.test.ts tests/determinism.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Math.random" src/app/forge.ts` retorna 0
    - `grep -ci "host" src/app/forge.ts` retorna 0
    - `grep -c "const LOCAL_SLOT" src/main.ts` retorna 0
    - `npx vitest run tests/run-config-lobby.test.ts` sai 0 e executa pelo menos 7 testes
    - A saída inclui um teste cujo nome menciona `ordem` (a ordem do array de `players` é carga)
    - `npx vitest run tests/golden.test.ts tests/determinism.test.ts` sai 0 — a run solo continua produzindo o mesmo ouro
    - `npm run build` sai 0
  </acceptance_criteria>

  <done>
    A linha que o Marco 0 anunciou como "a única que deixa de ser constante" deixou de ser
    constante, e a seed passou a vir de quem manda em vez de cada máquina inventar a sua.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `startRun` pelo lobby e a prova do hash do tick 0</name>
  <files>src/net/lobby.ts, src/ui/room.ts, tests/run-config-lobby.test.ts</files>

  <read_first>
    - `src/net/lobby.ts` (do plano 03-03 — `startRoom()`, a emissão de `startRun` e o canal `reliable`)
    - `src/ui/room.ts` (do plano 03-09 — o `#desync-screen`, `#desync-ours`, `#desync-theirs`, `#btn-desync-close`)
    - `src/ui/screens.ts` linha 326 (`deps.onQuit` — o mesmo caminho de saída da tela de pause, que o botão da divergência reusa)
    - `packages/sim/src/serialize.ts` (`hashWorld` e o cabeçalho sobre `-0`)
    - `packages/protocol/src/enums.ts` (`MSG_KIND`, `CHANNEL_CLASS`)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-UI-SPEC.md` § "Screens & States" #3 (a tela de divergência, a copy literal e o `user-select: text` dos hashes)
  </read_first>

  <behavior>
    - `startRoom()` na autoridade emite `startRun` com o `RunConfig` pelo canal `reliable`, e nada mais
    - Cada cliente que recebe `startRun` constrói o mundo localmente e devolve o `hashWorld` do tick 0 pelo canal `reliable`
    - Hashes iguais: a run começa nos dois lados e nenhuma tela de erro aparece
    - Hashes diferentes: a autoridade abre `#desync-screen` com os dois valores, e o convidado divergente também
    - O `startRun` **não** carrega `obstacles`, `traps`, `play` nem `config` derivado — só o `RunConfig` (D3-17)
    - Um `startRun` que chegue de um `peerId` que não é a autoridade é descartado
    - Fechar a tela de divergência usa o mesmo caminho de saída da tela de pause: a run divergente **não** fica rodando atrás do modal
  </behavior>

  <action>
    Ligue `startRoom()` ao início real da run. A autoridade emite `startRun { config }` no canal
    `reliable`; cada cliente (**a autoridade inclusive**, pelo mesmo caminho) roda a sequência
    canônica — `createWorld`, `generateArena`, `createPlayer` para cada slot, `startRun(world)` —
    e devolve `hashWorld(world)` do tick 0 pelo `reliable`. A autoridade compara.

    **A camada estática nunca viaja** (D3-17): `startRun` leva só o `RunConfig`. É o mesmo caminho
    do replay (D-11): **um** jeito de construir o mundo inicial, e o hash do tick 0 é a prova de
    que ele fecha. Escreva isso no comentário, porque é a decisão que o codec da fase 4 depende.

    **Divergência.** Quem detecta abre `#desync-screen` com os dois hashes, usando a copy literal
    da UI-SPEC (`MUNDOS DIFERENTES`, o corpo, e os rótulos `o seu:` / `o da sala:` — nunca "o do
    host"). Os dois valores são selecionáveis (`user-select: text`, já no CSS do plano 03-09), ou a
    frase "copie os dois códigos" é mentira. `#btn-desync-close` usa o **mesmo `deps.onQuit`** da
    tela de pause: deixar uma run divergente rodando atrás de um modal não é estado que esta fase
    suporta.

    **A run começa local e sem sincronia** (D3-05, D3-18): os outros personagens ficam parados, e
    isso é por desenho — **não** implemente streaming de snapshot aqui; é a fase 4. Escreva a frase
    no arquivo, ou alguém a lê como bug.

    Estenda `tests/run-config-lobby.test.ts` com os itens de `<behavior>` sobre o par de
    `Transport` em processo de `tests/net/helpers.ts`, forçando divergência com um `RunConfig`
    adulterado num dos lados.
  </action>

  <verify>
    <automated>npx vitest run tests/run-config-lobby.test.ts tests/lobby.test.ts tests/net-vocabulary.test.ts &amp;&amp; npm run build &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/run-config-lobby.test.ts` sai 0 e executa pelo menos 12 testes
    - A saída inclui um teste cujo nome menciona `divergência` ou `tick 0`
    - `src/net/lobby.ts` contém `hashWorld`
    - `grep -vE "^\s*[/*]" src/net/lobby.ts | grep -cE "obstacles|traps"` retorna 0
    - `grep -c "desync" src/ui/room.ts` retorna 3 ou mais
    - `grep -ci "fase 4" src/net/lobby.ts` retorna 1 ou mais (a frase sobre a run começar sem sincronia existe)
    - `npx vitest run tests/net-vocabulary.test.ts` sai 0
    - `npm test` sai 0
  </acceptance_criteria>

  <done>
    Apertar `▶ INICIAR` põe todo mundo numa run construída da mesma seed, e se dois mundos
    divergirem isso aparece no lobby, com os dois hashes na tela, em vez de virar
    "dessincronizou em 40 segundos" na fase 4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dois navegadores fechando uma sala, e o PWA que não guarda a sala no `start_url`</name>
  <files>playwright.config.ts, tests/net/room.spec.ts, tests/net/e2e-helpers.ts, tests/net/tsconfig.json, tests/pwa/room-url.spec.ts, tsconfig.json, package.json, .github/workflows/ci.yml</files>

  <read_first>
    - `playwright.config.ts` **inteiro** (o cabeçalho que diz por que um browser só, por que nenhum servidor gerenciado pelo runner, e o `fullyParallel: false` / `workers: 1` com o motivo — que vale igual para as specs de rede: uma origem, um servidor, salas em memória compartilhadas)
    - `tests/pwa/helpers.ts` linhas 1-60 (`serveDir`, o `close()` que mata sockets abertos de verdade, e o cabeçalho que explica cada "porquê")
    - `tests/pwa/install.spec.ts` linhas 20-60 (a forma de uma spec: `afterEach` tolerante, `expect.soft` para nomear todas as falhas de uma vez)
    - `tests/pwa/tsconfig.json` (o molde do tsconfig de um canto de `tests/` que precisa de tipos de Node)
    - `tsconfig.json` linhas 25-40 (o `exclude` e o comentário que explica por que cada arquivo excluído está lá)
    - `apps/server/src/signaling/index.ts` e `apps/server/src/app.ts` (o que o helper de e2e tem de instanciar)
    - `public/manifest.json` linhas 1-10 e `public/sw.js` linhas 110-130
    - `.github/workflows/ci.yml` job `pwa` (linhas ~213-260)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-VALIDATION.md` § "Wave 0 Requirements" (o item que deixa esta decisão para o planejador)
  </read_first>

  <action>
    **A decisão de configuração, que a `03-VALIDATION.md` deixou aberta:** `playwright.config.ts`
    passa a ter **dois projetos**, cada um com o seu `testDir` — `pwa` com `testDir: 'tests/pwa'` e
    `net` com `testDir: 'tests/net'`, os dois sobre `devices['Desktop Chrome']`. `fullyParallel:
    false` e `workers: 1` continuam no topo e passam a valer para os dois, **e o motivo é o mesmo
    escrito de outra forma**: as specs de rede compartilham uma origem, um servidor e salas em
    memória, então rodá-las em paralelo faria uma ver o estado da outra. Acrescente esse parágrafo
    ao cabeçalho do arquivo.

    Scripts: `"test:pwa": "playwright test --project=pwa"` (o comando que a `03-VALIDATION.md`
    referencia continua válido) e `"test:e2e": "playwright test"` (os dois projetos). O job `pwa`
    do CI passa a rodar `npm run test:e2e`, com o comentário no estilo do arquivo dizendo que ele
    agora cobre também a sala entre dois contextos.

    **`tests/net/e2e-helpers.ts`.** Um servidor único que serve `dist/` **e** atende o `upgrade` de
    `/ws` na **mesma origem** — é a topologia do Caddy colapsada num processo, e é obrigatório
    porque a checagem de `Origin` do plano 03-04 recusa origens diferentes e D-08 recusa builds
    diferentes sem bypass. Reuse `serveDir` de `tests/pwa/helpers.ts` para a parte estática e
    anexe `attachSignalling` ao mesmo `http.Server`, com `DG2_ORIGIN` apontando para a origem
    efêmera. `close()` mata os sockets de verdade, como o helper do PWA já faz.

    **Registro de tipos**, seguindo o padrão que o repositório já usa duas vezes: `tsconfig.json`
    `exclude` ganha `tests/net/room.spec.ts` e `tests/net/e2e-helpers.ts` — e **não**
    `tests/net/helpers.ts`, que é puro e continua no programa da raiz. Crie
    `tests/net/tsconfig.json` no molde de `tests/pwa/tsconfig.json`, cobrindo os dois arquivos
    excluídos, e o script `"typecheck:net"`. Acrescente `npm run typecheck:net` ao job `test` do
    CI, ao lado dos outros `typecheck:*`.

    **`tests/net/room.spec.ts`.** Dois `browserContext` no **mesmo** Chromium — contextos
    compartilham o processo do navegador mas não o estado de sessão, que é exatamente a fronteira
    certa para dois jogadores, e o WebRTC entre eles fecha por loopback sem STUN. **Não** é preciso
    `--use-fake-device-for-media-stream`: DataChannel não toca `getUserMedia`. A spec prova, com
    `expect.soft` para nomear todas as falhas de uma vez:
    1. o contexto A cria a sala e o código de 6 caracteres aparece em `#lobby-code`;
    2. o contexto B entra por esse código e os dois passam a ver dois slots preenchidos;
    3. cada slot mostra um ping numérico e a palavra `direto`;
    4. B escolhe uma classe e o card correspondente muda **nos dois** lados;
    5. A aperta `▶ INICIAR` e **nenhum** `#desync-screen` aparece em nenhum dos dois — o hash do
       tick 0 conferiu (D3-05);
    6. fechar o contexto B esvazia o slot em A **sem** derrubar a sala (D3-08);
    7. fechar o contexto A faz B ver `Quem criou a sala saiu. A sala acabou.` (D3-02).

    **`tests/pwa/room-url.spec.ts`.** Instala o PWA a partir de `/?sala=ABC123` e confere que o
    `start_url` resolvido **não** contém `sala=` e que a query não virou uma entrada de cache
    distinta da raiz. É a resposta à pergunta em aberto da pesquisa sobre `start_url: "."`
    (D3-07). Molde: `tests/pwa/install.spec.ts`.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/net/room.spec.ts &amp;&amp; npx playwright test tests/pwa/room-url.spec.ts &amp;&amp; npm run typecheck:net &amp;&amp; npm run typecheck:pwa &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx playwright test tests/net/room.spec.ts` sai 0 e executa pelo menos 1 spec com 7 asserções nomeadas
    - `npx playwright test tests/pwa/room-url.spec.ts` sai 0
    - `npx playwright test --project=pwa --list` lista as specs de `tests/pwa` e **nenhuma** de
      `tests/net` (a fiação dos dois projetos se confere sem rodar a suíte)
    - `npx playwright test --list` lista specs dos **dois** projetos, `pwa` e `net` — a execução
      completa (`npm run test:e2e`) é o portão de fase, em `<verification>` e no CI, e não o
      verify desta tarefa
    - `playwright.config.ts` contém `testDir: 'tests/net'` e `testDir: 'tests/pwa'`
    - `tsconfig.json` `exclude` contém `tests/net/room.spec.ts` e `tests/net/e2e-helpers.ts` e **não** contém `tests/net/helpers.ts`
    - `node -p "JSON.parse(require('fs').readFileSync('package.json')).scripts['typecheck:net']"` imprime um comando `tsc -p tests/net/tsconfig.json --noEmit`
    - `grep -c "typecheck:net" .github/workflows/ci.yml` retorna 1
    - `grep -c "test:e2e" .github/workflows/ci.yml` retorna 1
    - `npx tsc --noEmit` sai 0 e `npm test` sai 0
  </acceptance_criteria>

  <done>
    **Os critérios 1, 2 e a metade local do 4 estão fechados**, provados por dois navegadores de
    verdade que abrem uma sala, se veem, escolhem classe, começam a run com o mesmo mundo e
    sobrevivem à saída de um dos dois.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autoridade → peers (`startRun`) | O `RunConfig` chega de uma máquina remota e vira o manifesto da run local |
| peer → autoridade (hash do tick 0) | O hash é auto-declarado; ele prova concordância, não honestidade |
| URL → PWA | `/?sala=X` no momento da instalação pode gravar um `start_url` com sala fixa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-35 | Tampering | `startRun` forjado por um convidado | mitigate | Um `startRun` que chegue de um `peerId` diferente do `authorityPeerId` é descartado; teste dedicado |
| T-3-36 | Tampering | `RunConfig` com valores fora de domínio | mitigate | `cls` é validado contra `CLASS_KEY` e `id` contra `PLAYER_SLOT` pela guarda estreita do plano 03-03 antes de virar `RunConfig`; a seed é um `uint32` |
| T-3-12 | Tampering | autoridade emitindo config vantajosa para si | accept | Fora do escopo por desenho (ARCHITECTURE Padrão 3): a autoridade é fonte da verdade da **sessão**, não da **conta**; nada durável passa por ela nesta fase, e a verificação de run é a fase 9 |
| T-3-32 | Information Disclosure | `?sala=` fixado no `start_url` do PWA | mitigate | `tests/pwa/room-url.spec.ts` instala a partir de `/?sala=X` e assere que o `start_url` resolvido não contém `sala=`; a query é removida com `replaceState` no boot (D3-07) |
| T-3-37 | Denial of Service | run divergente rodando atrás de um modal | mitigate | `#btn-desync-close` usa o mesmo `deps.onQuit` da tela de pause; a run é encerrada, não escondida |
| T-3-SC | Tampering | dependência de teste nova | accept | Nenhum pacote é instalado: Playwright 1.62.1, Vitest e o browser mode já estão no repositório, e a versão do Playwright está fixada de propósito |
</threat_model>

<verification>
- `npm run build` sai 0
- `npm test` sai 0
- `npm run test:e2e` sai 0 (projetos `pwa` e `net`)
- `npm run test:browser` sai 0
- `npm run bench:snapshot` sai 0
- `npm run lint`, `npx tsc --noEmit`, `npm run typecheck:net`, `npm run typecheck:pwa`, `npm run typecheck:sim`, `npm run typecheck:protocol` e `npm run typecheck:server` saem 0
- `npx vitest run tests/golden.test.ts tests/determinism.test.ts` sai 0 (a run solo não regrediu)
</verification>

<success_criteria>
- **Critério 1:** um jogador cria uma sala, recebe um código de 6+ caracteres sem caractere ambíguo, e outro entra por ele — provado por dois `browserContext`
- **Critério 2:** cada jogador escolhe a classe no lobby, quem criou inicia, e os slots `p0..p3` são atribuídos quando a sala fecha e não mudam
- **Critério 4 (metade local):** a tela mostra ping e rota, e o desfecho ICE é gravado
- O hash do tick 0 confere entre os dois lados na spec de ponta a ponta
- Um PWA instalado a partir de `/?sala=X` não abre numa sala morta
- `LOCAL_SLOT` não é mais uma constante e `buildRunConfig` não chama `Math.random()`
</success_criteria>

<output>
Crie `.planning/phases/03-sala-transporte-e-protocolo/03-10-SUMMARY.md` ao terminar.
</output>

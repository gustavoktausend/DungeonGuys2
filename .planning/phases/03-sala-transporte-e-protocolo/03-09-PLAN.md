---
phase: 03-sala-transporte-e-protocolo
plan: 09
type: execute
wave: 5
depends_on: ["03-03", "03-08"]
files_modified:
  - index.html
  - src/ui/dom.ts
  - src/style.css
  - src/ui/room.ts
  - src/render/sprites.ts
  - src/main.ts
  - tests/net-vocabulary.test.ts
  - tests/room-ui.test.ts
autonomous: false
requirements: [SALA-01, SALA-02, SALA-03, SALA-05]

must_haves:
  truths:
    - "Um jogador clica em `◆ JOGAR COM AMIGOS`, cria uma sala e vê o código de seis caracteres na tela (SALA-01)"
    - "Um amigo cola o código, entra, e os dois se veem no lobby com nome, classe e cor (SALA-02, D3-06)"
    - "Cada slot mostra `{n} ms · direto|relay`, e o indicador da run mostra o mesmo (SALA-05, D3-15)"
    - "O card de classe selecionado pinta na hora e o `lobbyState` seguinte é a verdade (SALA-03)"
    - "`▶ INICIAR` **não existe no DOM** para quem não criou a sala — ausência, não `disabled` (D3-04)"
    - "`https://<domínio>/?sala=CODIGO` abre no fluxo de entrada com o código preenchido, e a query some da URL (D3-07)"
    - "Quatro slots em quatro cores não sobrescrevem a folha que a run inteira desenha"
  artifacts:
    - path: "src/ui/room.ts"
      provides: "A view das telas de sala, lobby, divergência e do badge de rede — dirigida por estado de rede"
      min_lines: 200
    - path: "index.html"
      provides: "Quatro telas novas, o badge e o botão de entrada, no padrão .screen > .screen-inner.compact"
      contains: "lobby-screen"
    - path: "src/ui/dom.ts"
      provides: "Os 27 ids novos e as três entradas novas em dom.screens"
    - path: "src/render/sprites.ts"
      provides: "O helper puro de recolor que DEVOLVE o canvas, com recolorPlayerSheet virando uma linha em cima dele"
  key_links:
    - from: "src/ui/room.ts"
      to: "src/net/lobby.ts"
      via: "a view assina o estado da máquina; a máquina não conhece o DOM"
      pattern: "createLobby|onLobbyState"
    - from: "index.html"
      to: "src/ui/dom.ts"
      via: "todo id novo tem entrada no mesmo commit (tests/dom-ids.test.ts)"
      pattern: "getElementById\\('lobby-slots'\\)"
    - from: "src/ui/room.ts"
      to: "src/render/sprites.ts"
      via: "cache de um canvas por slot, chaveado por classe e cor"
      pattern: "recolorSheet"
---

<objective>
Dar tela ao que já funciona: as quatro telas novas (sala, lobby, divergência, mais o indicador
de rede), os 27 ids, o CSS que reusa o skin existente, o avatar por slot na cor de cada jogador,
e o fluxo de entrada por código e por link.

Purpose: até aqui a sala existe em teste. Este plano é o que faz um humano conseguir usá-la — e
o `03-UI-SPEC.md` já fechou copy, cores, tipografia, espaçamento e contrato de DOM, então este
plano executa um contrato em vez de inventar um.

Output: **os critérios de sucesso 1 e 2 da fase, e a metade visível do critério 4.**
</objective>

## Phase Goal

**As a** jogador que quer jogar com amigos, **I want to** criar uma sala, receber um código
curto e ver meus amigos entrarem e escolherem classe no lobby antes de eu iniciar a run,
**so that** nós quatro comecemos a mesma run no mesmo mundo, sobre um fio cujo formato já
está medido e congelado.

Esta é a fatia em que a frase inteira vira algo que um humano faz com o dedo.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sala-transporte-e-protocolo/03-CONTEXT.md
@.planning/phases/03-sala-transporte-e-protocolo/03-UI-SPEC.md
@.planning/phases/03-sala-transporte-e-protocolo/03-PATTERNS.md
@.planning/phases/03-sala-transporte-e-protocolo/03-VALIDATION.md
@.planning/phases/03-sala-transporte-e-protocolo/03-03-SUMMARY.md
@.planning/phases/03-sala-transporte-e-protocolo/03-08-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
**`03-UI-SPEC.md` é contrato, não sugestão.** Ele fixa: a tabela completa de copy (§ Copywriting
Contract), os 27 ids (§ DOM Contract), as cores (só tokens do skin craftpix, nenhum novo), a
tipografia (quatro papéis, dois pesos, todos os `clamp()` já existentes), a escala de espaçamento
(múltiplos de 4) e o contrato de acessibilidade. Onde este plano parecer inventar algo, ele está
citando aquele documento.

As três coisas que a UI-SPEC deixou **abertas para o planejador**, decididas aqui:
1. **Quando `#net-badge` aparece:** assim que existe sessão de sala — junto com o lobby —
   mostrando `—` até o primeiro `pong`. Um elemento, um ciclo de vida, e a fase 4 o herda como
   está.
2. **Onde mora o cache de canvas dos avatares:** o helper **puro** de recolor é exportado de
   `src/render/sprites.ts`; o **cache por slot** mora em `src/ui/room.ts`. A separação é a que já
   existe no repositório entre "quem sabe desenhar" e "quem sabe o estado da tela".
3. **`#room-screen` e `#lobby-screen` compartilham um módulo:** sim, `src/ui/room.ts`, porque são
   um fluxo só (entrar → lobby → sair). A máquina de estado continua em `src/net/lobby.ts` e
   **não** conhece o DOM; `src/ui/room.ts` assina os eventos dela.

Do plano 03-03: `createLobby(deps)` e `createPinger(...)`. Do plano 03-08:
`createSignalingClient`, `createRtcTransport`, `routeOf`, `readRelayFlag`, `clearRelayFlag`.

Armadilhas medidas no código, que a UI-SPEC nomeia e que este plano tem de respeitar:
- `src/ui/settings.ts:55-57` faz `document.querySelectorAll('.class-card')` **global** — por isso
  os componentes novos são `.lobby-slot` e `.lobby-class-card`, com nome próprio, e as regras de
  `style.css` ganham o seletor novo **na lista**, nunca uma cópia da declaração.
- `src/ui/settings.ts:32-35` dá `blur()` em **todo** botão clicado — por isso cada tela nova é
  obrigada a recolocar o foco ao abrir, ou o teclado perde o lugar a cada ação.
- `src/ui/events.ts:12-14` (`mouseOnly()`): os botões destas telas **não** o usam, porque Espaço é
  a tecla de ataque **durante a run** e aqui não há run. A exceção única é `#btn-relay-flag`, que
  fica visível durante a run e **usa**.
- `src/ui/events.ts:19-22` (`isTextInput()`): `#join-code` **precisa** ser `type="text"`, ou
  digitar o código dispara atalhos do jogo.
- `#net-badge` vai **depois** de `#touch-ui` no markup, e a regra usa o combinador `~` e não `+`
  — `style.css:981` já mostra o erro simétrico que `+` causa quando um elemento entra no meio.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: O markup das quatro telas, os 27 ids e o CSS que reusa o skin</name>
  <files>index.html, src/ui/dom.ts, src/style.css</files>

  <read_first>
    - `.planning/phases/03-sala-transporte-e-protocolo/03-UI-SPEC.md` **inteiro** — é o contrato
    - `index.html` linhas 110-235 (a forma de uma tela: `.screen` > `.screen-inner.compact` > placa de título + `.btn-pixel`; e onde `#btn-coop` entra, logo depois de `#btn-start` e antes de `#btn-update`)
    - `src/ui/dom.ts` linhas 19-32 (o registro `dom.screens`) e 85-93 (o molde de comentário por bloco, citando a decisão)
    - `tests/dom-ids.test.ts` linhas 40-58 (o guarda que obriga todo id novo a ter entrada em `dom.ts` no mesmo commit; `MIN_IDS = 80` continua válido com 112 ids)
    - `src/style.css` linhas 51-58 (os tokens do skin craftpix), 90-142 (a casca de tela), 189-232 (`.class-cards` e `.class-card`), 296-315 (`#hero-name`), 541-554 (`.key`), 975-990 (o combinador que já falha por causa de um elemento no meio), 1055-1080 (`#btn-touch-pause` 44×44 e o toast), 1258-1400 (as escalas de fonte e as cores de status)
    - `tests/build-base.test.ts` (que lê `src/style.css` como texto cru — confira se algum caso novo é afetado)
  </read_first>

  <action>
    **`index.html`.** Quatro `<div class="screen">` novos dentro do `#ui-overlay`
    (`#room-screen`, `#lobby-screen`, `#desync-screen`) mais o `#net-badge` **fora** dele, depois
    de `#touch-ui`. E `#btn-coop` (`◆ JOGAR COM AMIGOS`, `.btn-pixel.secondary`) logo depois de
    `#btn-start`. A estrutura de cada tela copia `#pause-screen`: `.screen` > `.screen-inner
    compact` > placa `.shop-title` > conteúdo > botões. Os 27 ids são exatamente os da tabela §
    DOM Contract da UI-SPEC, com os elementos e os atributos que ela especifica —
    `#join-code` com `type="text" maxlength="6" autocapitalize="characters" autocomplete="off"
    spellcheck="false"`; `#room-error` com `role="alert"`; `#room-status` e `#lobby-status` com
    `role="status" aria-live="polite"`; `#lobby-link` com `readonly`.

    Os quatro cards de slot e os cards de classe **não têm id próprio**: são filhos construídos
    por JS dentro de `#lobby-slots` e `#lobby-class`, no mesmo padrão de `#shop-slots`,
    `#forge-list` e `#levelup-choices`.

    **Toda a copy vem da tabela da UI-SPEC, literal.** Botões e rótulos em CAIXA ALTA; frases em
    caixa de frase com ponto final; português do Brasil com acento. As telas legadas seguem em
    inglês e traduzi-las está fora do escopo.

    **`src/ui/dom.ts`.** As 27 entradas novas, agrupadas por tela, cada bloco com um comentário
    curto citando a decisão que o justifica (o molde é o comentário de `btnUpdate` nas linhas
    89-91). `dom.screens` ganha `room`, `lobby` e `desync`.

    **`src/style.css`.** Zero token novo, zero `@media` nova, zero família de fonte nova. Os
    componentes novos entram **acrescentando o seletor à lista** das regras existentes onde a base
    visual é a mesma (`.lobby-slot` ao lado de `.class-card`, `.lobby-chip` ao lado de `.key`,
    `#join-code` ao lado de `#hero-name`), nunca copiando declarações. Regras genuinamente novas,
    e só estas:
    - `.btn-pixel:disabled, .btn-pixel.secondary:disabled { opacity: 0.5; cursor: not-allowed; filter: none; transform: none; }` — o único estado novo da fase
    - `:focus-visible { outline: 2px solid var(--cp-ink-light); outline-offset: 2px; }` nos controles das telas novas — hoje não existe estilo de foco global, e sem ele o teclado navega às cegas
    - `min-height: 44px` (e `min-width: 44px` em botão só de ícone) em todo controle novo
    - o layout dos slots: `display: flex; flex-wrap: wrap; gap: 12px`
    - o avatar do slot: canvas de **48×56**, exatamente o `#color-preview`, para reusar a escala `s = 1.7` de `settings.ts:113-124` sem recalibrar
    - o nome do slot: `overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: 12ch`
    - `#net-badge`: posição fixa em `left: 12px; bottom: 12px`, `z-index: 11`, `pointer-events: none` no container e `auto` só em `#btn-relay-flag`; e `#touch-ui.enabled ~ #net-badge` movendo para o rodapé central — **combinador `~`, não `+`**
    - `#desync-ours` e `#desync-theirs` com `user-select: text; cursor: text`, porque
      `html, body { user-select: none }` (`style.css:69`) tornaria a copy "copie os dois códigos"
      uma mentira

    As faixas de ping reusam `.fx-pos` e `.fx-neg` já declaradas; a mitigação de contraste é a que
    a UI-SPEC registrou (**a cor nunca é o único sinal**: o texto diz o número ou diz "sem
    resposta", e a rota vem por extenso).
  </action>

  <verify>
    <automated>npx vitest run tests/dom-ids.test.ts tests/build-base.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "id=\"lobby-screen\"\|id=\"room-screen\"\|id=\"desync-screen\"\|id=\"net-badge\"\|id=\"btn-coop\"" index.html` retorna 5
    - `grep -o "id=\"[a-z-]*\"" index.html | sort -u | wc -l` retorna 112
    - `npx vitest run tests/dom-ids.test.ts` sai 0
    - `src/ui/dom.ts` contém `room:`, `lobby:` e `desync:` dentro do objeto `screens`
    - `grep -c "@media" src/style.css` retorna 0
    - `grep -c "btn-pixel:disabled" src/style.css` retorna 1
    - `grep -c "focus-visible" src/style.css` retorna 1 ou mais
    - `grep -c "#touch-ui.enabled ~ #net-badge" src/style.css` retorna 1 e `grep -c "#touch-ui.enabled + #net-badge" src/style.css` retorna 0
    - `grep -c "type=\"text\"" index.html` inclui a linha de `#join-code`: `grep -c 'id="join-code"[^>]*type="text"\|type="text"[^>]*id="join-code"' index.html` retorna 1
    - `npm run build` sai 0
  </acceptance_criteria>

  <done>
    As quatro telas existem no markup e no registro, com os 27 ids conferidos por teste, e nenhum
    token, fonte, media query ou dependência nova entrou no projeto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O helper puro de recolor, o cache por slot e a pintura do lobby</name>
  <files>src/render/sprites.ts, src/ui/room.ts, tests/room-ui.test.ts, tests/net-vocabulary.test.ts</files>

  <read_first>
    - `src/render/sprites.ts` linhas 105-180 (`playerSheet` como módulo-global e o corpo de `recolorPlayerSheet` — é dele que o helper puro sai)
    - `src/ui/settings.ts` linhas 100-145 e 193-203 (`#color-preview`, a escala `s = 1.7`, o `setInterval` que **não** deve ser copiado, e `heroName()`)
    - `src/app/save.ts` linha 27 (`colors` como `Record<string, [number, number, number]>`)
    - `src/ui/screens.ts` linhas 29-38, 48-73, 149-163, 180-196 (`showScreen`, o `span()` construído por nós com o comentário que nomeia **esta fase** como a razão, o "remendar no lugar" que evita piscar, a delegação de clique, e o `announce()` com o motivo de ele ter um só uso aqui)
    - `src/net/lobby.ts` (do plano 03-03 — os eventos que esta view assina)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-UI-SPEC.md` § "Screens & States" #2 e § Accessibility
  </read_first>

  <behavior>
    - `recolorSheet(cls, rgb)` **devolve** um canvas e **não** escreve em `playerSheet`
    - `recolorPlayerSheet(cls, rgb)` continua existindo e passa a ser uma linha sobre `recolorSheet`, com o mesmo efeito de antes
    - Chamar `recolorSheet` quatro vezes com quatro cores devolve quatro canvases distintos, e `playerSheet` continua igual ao que era
    - O cache de avatar é chaveado por `` `${cls}|${r},${g},${b}` `` e só repinta quando a chave muda
    - Pintar um `lobbyState` com o nome `<img src=x onerror=alert(1)>` produz um nó de texto com esse conteúdo literal e **nenhum** elemento `img` no DOM
    - Um nome com mais de 12 pontos de código é cortado em 12 na exibição
    - Um slot vazio mostra `VAZIO` e nenhum avatar
    - Um slot com ping acima de 150 ms recebe a classe de status negativa **e** o texto do número; um "sem resposta" mostra o texto, nunca só a cor
    - `▶ INICIAR` não existe no DOM quando o jogador local não é a autoridade
  </behavior>

  <action>
    **`src/render/sprites.ts`.** Extraia o corpo de `recolorPlayerSheet` num helper **puro**
    `recolorSheet(cls, rgb)` que **devolve** o canvas recolorido em vez de atribuir ao
    módulo-global, e reduza `recolorPlayerSheet` a uma linha sobre ele. O comentário explica o
    defeito que a extração previne: quatro slots em quatro cores chamando a função antiga
    sobrescreveriam um ao outro e a run começaria com a cor do último. Preserve o
    `try`/`catch` que devolve a folha original quando o canvas está contaminado (abertura por
    `file://`).

    **`src/ui/room.ts`.** A view das telas. O cabeçalho é obrigatório e diz que **estas são as
    primeiras telas do jogo dirigidas por estado de REDE e não por `world`** — todo o resto de
    `src/ui/screens.ts` lê do `World` uma vez por quadro pelo `syncScreens`; aqui não existe
    `World` até `startRun`. Sem essa frase, o próximo a mexer procura o `world` e não acha.

    A view assina os eventos de `createLobby` e pinta. Regras que não são estilo:
    - **`textContent` sempre, `innerHTML` nunca.** Nome e classe vêm de um peer, e o comentário
      de `screens.ts:56-71` já diz que é **esta fase** que abre esse buraco: a partir da fase 6 a
      origem carrega o cookie de sessão, e um `innerHTML` concatenado seria XSS armazenado.
    - Os quatro cards e os cards de classe são construídos **uma vez** ao abrir o lobby e
      **remendados no lugar** a 1 Hz (`textContent`/`classList`), nunca recriados — recriar quatro
      canvases por segundo é lixo de GC e pisca. O molde é o gate de `screens.ts:149-152`.
    - Um canvas por slot em cache, chave `` `${cls}|${r},${g},${b}` ``, quadro **idle 0 parado**.
      **Sem `setInterval`**, ao contrário de `#color-preview`: são quatro canvases e o lobby não
      precisa de animação para comunicar nada.
    - Clique por **delegação** no container, no molde de `screens.ts:157-163`, mas **sem** a
      guarda `detail === 0` — ela impediria a ativação por teclado, e a UI-SPEC exige Enter e
      Espaço nestas telas.
    - **Foco ao abrir cada tela**, obrigatório: sala → `#btn-create-room` (ou `#btn-join-room`
      quando veio de `?sala=`); lobby → `#btn-copy-link`. É o contrapeso do `blur()` global de
      `settings.ts:32-35`.
    - Mudanças de estado vão para `#lobby-status` (`{NOME} entrou.`, `{NOME} saiu.`,
      `Link copiado.`, limpando em 3 s). **`announce()` não é usado no lobby** — é um toast de
      2600 ms em 44 px no centro exato da tela e cobriria os slots. Fica reservado a
      `SALA ENCERRADA`, quando a sala morre e a tela troca embaixo do jogador (D3-02).
    - **`▶ INICIAR` é ausência para o convidado, não `disabled`**: um botão desabilitado convida a
      clicar e a perguntar por quê, e a assimetria é permanente (D3-02).
    - Classes repetidas não recebem aviso, marca nem cor diferente (D3-03). Sem estado de "pronto",
      sem timer (D3-04). O seletor de classe pinta o card local **na hora** (eco otimista) e o
      `lobbyState` seguinte é a verdade.
    - `#net-badge` aparece assim que existe sessão de sala, com `—` até o primeiro `pong`; para o
      convidado mostra `{n} ms · direto|relay`, e para a autoridade o **pior** entre os slots
      conectados, ou nada quando está sozinha. Escreve `textContent`/`style` uma vez por quadro,
      no mesmo `frame()` do HUD, e **não toca no `world`**.

    **`tests/net-vocabulary.test.ts`** ganha `src/ui/room.ts` no glob, com um comentário dizendo
    por que só este arquivo de `src/ui/` entra: é o único que fala de sala, peers e slots, e a
    regra FORM-12 vale para ele exatamente como vale para `src/net/`. A copy da tela diz "quem
    criou a sala" ao jogador; o código diz "autoridade".

    **`tests/room-ui.test.ts`** cobre os itens de `<behavior>` que não precisam de DOM real por
    inspeção de funções puras (o corte de nome, a chave do cache, a escolha da faixa de ping) e
    por asserção **estrutural** sobre o texto de `src/ui/room.ts` para os que precisam (ausência
    de `innerHTML`, ausência de `setInterval`, presença de `focus()` na abertura de cada tela,
    ausência de `detail === 0`). O runner é Node sem jsdom, e o cabeçalho do teste diz isso e diz
    que a prova de comportamento com DOM real é a spec de Playwright do plano 03-10.
  </action>

  <verify>
    <automated>npx vitest run tests/room-ui.test.ts tests/net-vocabulary.test.ts tests/dom-ids.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `src/render/sprites.ts` contém `export function recolorSheet` e `recolorPlayerSheet` tem no máximo 4 linhas de corpo
    - `grep -c "playerSheet =" src/render/sprites.ts` retorna 3 ou menos, e nenhuma dessas atribuições está dentro de `recolorSheet`
    - `grep -c "innerHTML" src/ui/room.ts` retorna 0
    - `grep -c "setInterval" src/ui/room.ts` retorna 0
    - `grep -c "detail === 0" src/ui/room.ts` retorna 0
    - `grep -c "announce(" src/ui/room.ts` retorna 1 (só o caso de sala encerrada)
    - `grep -c "\.focus()" src/ui/room.ts` retorna 2 ou mais
    - `npx vitest run tests/room-ui.test.ts` sai 0 e executa pelo menos 9 testes
    - `npx vitest run tests/net-vocabulary.test.ts` sai 0 e o glob inclui `src/ui/room.ts`
    - `npm run build` sai 0
  </acceptance_criteria>

  <done>
    Quatro bonecos em quatro cores aparecem no lobby sem que a folha da run seja sobrescrita, e um
    nome de peer com markup dentro vira texto literal na tela em vez de um elemento.
  </done>
</task>

<task type="auto">
  <name>Task 3: O fluxo — `#btn-coop`, `?sala=`, copiar link, e a verificação humana das telas</name>
  <files>src/main.ts, src/ui/room.ts, index.html</files>

  <read_first>
    - `src/main.ts` linhas 30-60 (o registro do service worker por `import.meta.env.BASE_URL`) e 170-260 (`LOCAL_SLOT`, `frame()`, `beginRun`)
    - `public/sw.js` linhas 110-130 (`/api/` e `/ws` já passam direto; a allowlist de precache é por caminho, então `/?sala=ABC` casa `/` e é servido do cache — que é o comportamento certo)
    - `public/manifest.json` linhas 1-10 (`start_url` e `scope` em `"."`)
    - `tools/sw/emit.mjs` e `package.json` (o Vite **não** reescreve `public/`; qualquer mudança lá passa pelo passo `sw:emit` e pelo portão `sw:verify`)
    - `src/ui/room.ts` e `src/net/ice.ts` (`readRelayFlag`, `clearRelayFlag`)
    - `.planning/phases/03-sala-transporte-e-protocolo/03-UI-SPEC.md` § "Screens & States" #1 e #5
  </read_first>

  <action>
    **`src/main.ts`.** Ligue `#btn-coop` à abertura de `#room-screen`. No boot, leia a query uma
    vez: `?sala=CODIGO` abre `#room-screen`, preenche `#join-code` com o código **normalizado** e
    põe o foco em `#btn-join-room` — **sem disparar o join sozinho**; o jogador confirma. Depois de
    consumida, a query sai da URL com `history.replaceState`, pela mesma razão que a `?ice=` sai:
    recarregar não deve tentar entrar de novo numa sala que já morreu. Faça a leitura de `?ice=` na
    mesma passagem, chamando `readRelayFlag` de `src/net/ice.ts`.

    **"COPIAR LINK" monta o link a partir do código da sala, nunca de `location.href`** — é o que
    impede a flag de debug de viajar junto quando alguém compartilha. Use `navigator.clipboard`
    com o caminho de falha da UI-SPEC (`Não consegui copiar. Selecione o link e copie à mão.`),
    porque `clipboard` recusa em contexto não seguro e em alguns navegadores sem gesto do usuário.

    **`public/` não muda.** Confira e registre no SUMMARY: `start_url` e `scope` continuam `"."`,
    e `sw.js` já deixa `/api/` e `/ws` passarem. Se **alguma** mudança em `public/` se mostrar
    necessária, ela passa por `npm run sw:emit` e pelo portão `npm run sw:verify` no mesmo commit —
    o Vite não reescreve `public/`. A prova de que um PWA instalado a partir de `/?sala=X` não fixa
    uma sala no `start_url` é a spec de Playwright do plano 03-10.

    **`#btn-relay-flag`** só é visível quando a flag está ligada, com o texto
    `RELAY FORÇADO (DEBUG) ✕`, e clicar nele desliga a flag e recarrega a página. Ele é o único
    botão destas telas que **usa** `mouseOnly()`, porque fica visível durante a run.

    **Sair da sala como autoridade** é confirmação em dois cliques, sem `confirm()`: o primeiro
    troca o rótulo para `✕ SAIR MESMO? A SALA ACABA` e o segundo confirma; volta sozinho ao rótulo
    original em 3 s.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; npm run sw:verify &amp;&amp; npx vitest run tests/dom-ids.test.ts tests/room-ui.test.ts tests/net-vocabulary.test.ts &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "sala" src/main.ts` retorna 1 ou mais e `grep -c "replaceState" src/main.ts` retorna 1 ou mais
    - `grep -c "location.href" src/ui/room.ts` retorna 0
    - `grep -c "navigator.clipboard" src/ui/room.ts` retorna 1
    - `grep -c "mouseOnly" src/ui/room.ts` retorna 1 (só em `#btn-relay-flag`)
    - `git diff --name-only` do commit desta tarefa **não** inclui `public/manifest.json` nem `public/sw.js`; se incluir, inclui também a saída de `npm run sw:emit`
    - `npm run sw:verify` sai 0
    - `npm run build` sai 0 e `npm test` sai 0
  </acceptance_criteria>

  <done>
    Um link com `?sala=ABC123` abre o jogo já no fluxo de entrada, com o código preenchido, e a
    query some da barra de endereço.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Conferência visual das quatro telas</name>

  <what-built>
    Quatro telas novas no padrão existente — `#room-screen`, `#lobby-screen`, `#desync-screen` e o
    `#net-badge` — mais o botão `◆ JOGAR COM AMIGOS` na tela inicial, o fluxo `?sala=`, o avatar
    por slot na cor de cada jogador e o indicador de ping e rota.

    Tudo o que é medível já está automatizado: os 27 ids por `tests/dom-ids.test.ts`, a ausência de
    `innerHTML` e de `setInterval` por `tests/room-ui.test.ts`, o vocabulário por
    `tests/net-vocabulary.test.ts`, e o build por `npm run build`. O que sobra é leitura visual, e
    a `03-VALIDATION.md` já a registrou como verificação manual.
  </what-built>

  <action>
    Pare aqui e apresente ao usuário o bloco `<how-to-verify>` abaixo, na íntegra. Não execute
    nenhuma tarefa seguinte deste plano nem toque em nenhum arquivo antes de a resposta chegar:
    este é um portão bloqueante e não é auto-aprovável.
  </action>

  <how-to-verify>
    1. `npm run dev` e abra o endereço que o Vite imprimir.
    2. Na tela inicial, confirme que `◆ JOGAR COM AMIGOS` aparece **entre** `▶ START GAME` e o
       botão de recarregar, com o visual de botão secundário (violeta), e que o resto da tela não
       se mexeu.
    3. Clique nele. Na tela `SALA`: o foco começa em `▶ CRIAR SALA`; navegue só de `Tab` até
       `✕ VOLTAR` e confirme que o contorno de foco é visível em cada parada; digite `abc-123` em
       `— CÓDIGO DA SALA —` e confirme que aparece em maiúsculas e espaçado, e que **nenhum
       atalho do jogo dispara** enquanto você digita.
    4. Crie uma sala. No `LOBBY`: o código aparece grande; `COPIAR LINK` responde com
       `Link copiado.`; o seu card mostra o avatar **na sua cor**, o nome, a classe e os chips
       `VOCÊ` e `CRIOU A SALA`; os outros três cards dizem `VAZIO`; a frase
       *"Você está sozinho na sala. Pode iniciar assim mesmo."* aparece; `▶ INICIAR` está
       **habilitado**.
    5. Troque de classe algumas vezes e confirme que o card pinta na hora e o avatar acompanha, sem
       piscar e sem travar.
    6. Abra uma **segunda aba** no **mesmo endereço** (o mesmo `npm run dev`; abas em portas
       diferentes têm origens diferentes e o `upgrade` vai recusar — isso é esperado, não um bug) e
       entre com o código. Confirme: os dois se veem; cada card mostra `{n} ms · direto`; a linha
       de status anuncia `{NOME} entrou.`; e escolher a **mesma classe** nos dois não gera aviso
       nenhum.
    7. Feche a aba do convidado e confirme que o slot esvazia e a sala **não** cai. Depois feche a
       aba de quem criou e confirme, na do convidado, o aviso `Quem criou a sala saiu. A sala acabou.`
    8. Abra `http://<endereço>/?sala=ABC123`: a tela `SALA` abre com o código preenchido, o foco em
       `▶ ENTRAR`, e a query **some da barra de endereço**.
    9. Abra `http://<endereço>/?ice=relay`: o badge `RELAY FORÇADO (DEBUG) ✕` aparece, a query some
       da URL, e recarregar mantém o badge. Clique no `✕` e confirme que ele some.
    10. Reduza a janela para largura de celular e confirme que os quatro cards quebram em duas
        linhas e que a tela rola em vez de cortar.
  </how-to-verify>

  <acceptance_criteria>
    - O usuário confirmou os dez passos, ou listou o que não bateu
    - Nenhum passo exigiu abrir o DevTools para funcionar
  </acceptance_criteria>

  <verify>
    <human-check>A resposta do usuário satisfaz os critérios de aceitação acima; registre-a no SUMMARY.</human-check>
  </verify>

  <done>O usuário respondeu, a resposta ficou registrada no SUMMARY, e o plano pode seguir.</done>

  <resume-signal>Digite "aprovado" ou descreva o que não bateu, passo a passo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer → DOM | Nome e classe chegam de uma máquina remota e viram texto numa tela que, a partir da fase 6, carrega o cookie de sessão |
| URL → aplicação | `?sala=` e `?ice=` são entrada do usuário, e um deles liga o caminho de relay |
| área de transferência | O link copiado não pode carregar a flag de debug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-14 | Tampering | XSS armazenado por nome de peer | mitigate | `textContent` sempre; `grep -c "innerHTML" src/ui/room.ts` igual a 0 é critério de aceitação; corte duro em 12 pontos de código; o comentário de `screens.ts:56-71` já nomeia esta fase como a razão da regra |
| T-3-29 | Tampering | flag de relay viajando num link compartilhado | mitigate | "COPIAR LINK" monta o link a partir do **código da sala**, nunca de `location.href`; a query `?ice=` é removida com `replaceState`; o badge permanente torna o estado visível |
| T-3-32 | Information Disclosure | `?sala=` virando entrada de cache ou `start_url` do PWA | mitigate | `public/` não muda; `start_url` e `scope` continuam `"."`; a allowlist do service worker é por caminho e `/?sala=X` casa `/`; a prova é a spec de Playwright do plano 03-10 (D3-07) |
| T-3-33 | Information Disclosure | código da sala visível em captura de tela ou log | accept | O código é feito para ser dito em voz alta entre amigos; o público é fechado, o TTL é de 30 min, e o rate limit do `join` cobre a varredura |
| T-3-34 | Denial of Service | nome de peer quebrando o layout | mitigate | `max-width: 12ch` com `ellipsis` e corte em 12 pontos de código na exibição |
| T-3-SC | Tampering | biblioteca de UI ou de ícones | mitigate | **Superfície de terceiros zero** (UI-SPEC § Registry Safety): nenhum ícone empacotado, nenhum CSS de CDN, nenhuma fonte remota; `npm run build` e `tests/workspaces.test.ts` provam `dependencies: {}` |
</threat_model>

<verification>
- `npm run build` sai 0
- `npm test` sai 0
- `npm run lint` sai 0
- `npm run sw:verify` sai 0
- `npx vitest run tests/dom-ids.test.ts tests/room-ui.test.ts tests/net-vocabulary.test.ts tests/build-base.test.ts tests/workspaces.test.ts` sai 0
- O checkpoint visual foi aprovado
</verification>

<success_criteria>
- **Critério 1 da fase:** um jogador cria uma sala e vê um código de 6 caracteres sem caractere ambíguo
- **Critério 2 da fase (metade visível):** cada jogador escolhe a classe no lobby, e só quem criou vê `▶ INICIAR`
- **Critério 4 da fase (metade visível):** cada slot mostra ping e `direto`/`relay`, e o badge mostra o mesmo durante a run
- 112 ids em `index.html`, todos com entrada em `src/ui/dom.ts`
- Zero token de cor, fonte, media query ou dependência nova
- `grep -c "innerHTML" src/ui/room.ts` retorna 0
</success_criteria>

<output>
Crie `.planning/phases/03-sala-transporte-e-protocolo/03-09-SUMMARY.md` ao terminar.
</output>

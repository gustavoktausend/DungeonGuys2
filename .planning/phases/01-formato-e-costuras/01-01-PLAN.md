---
phase: 01-formato-e-costuras
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - vitest.browser.config.ts
  - eslint.config.js
  - tools/README.md
  - .github/workflows/ci.yml
autonomous: false
requirements: [FORM-04]

must_haves:
  truths:
    - "O mesmo arquivo de teste roda em Node, Chromium, Firefox e WebKit por um comando só"
    - "O CI executa lint, testes de Node e testes de navegador em Node 24, sem tocar no deploy.yml"
    - "`npm audit` deixa de acusar o high e o critical que o Vite 5.4.21 carrega (T-1-06)"
    - "A versão do Playwright está pinada exata: as builds dos motores são a variável sob teste"
    - "Existe uma convenção escrita para scripts Node em `tools/`, porque o repositório não tinha nenhuma"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Runner de Node; exclui explicitamente o teste cross-engine"
      contains: "exclude"
    - path: "vitest.browser.config.ts"
      provides: "Três instâncias de navegador via @vitest/browser-playwright"
      contains: "instances"
    - path: ".github/workflows/ci.yml"
      provides: "Portão de CI em Node 24 com cache dos navegadores"
      contains: "node-version: '24'"
    - path: "tools/README.md"
      provides: "Convenção de scripts Node (extensão, invocação, código de saída, tsconfig, eslint)"
    - path: "package.json"
      provides: "Toolchain alvo + scripts de teste"
      contains: "test:browser"
  key_links:
    - from: "package.json"
      to: "playwright"
      via: "versão pinada exata, sem circunflexo"
      pattern: "\"playwright\": \"1\\.62\\.1\""
    - from: ".github/workflows/ci.yml"
      to: "vitest.browser.config.ts"
      via: "npm run test:browser"
      pattern: "test:browser"
---

<objective>
Levantar a infraestrutura que todo o resto da fase 1 usa: a toolchain alvo do `CLAUDE.md`
(Vite 7.3.6, TypeScript 6.0.3, Vitest 4.1.11, ESLint 10.9.1, typescript-eslint 8.68.0), o
modo navegador do Vitest com Playwright **pinado exato**, e o primeiro workflow de teste que
este repositório já teve.

Hoje `.github/workflows/` tem **um** arquivo (`deploy.yml`), que roda os testes dentro do job
de deploy e em `node-version: 20`. Não existe `vitest.config.ts`, não existe `tools/`, e
`npm audit` acusa 3 moderate + 1 high + 1 critical — o high/critical é o
GHSA-fx2h-pf6j-xcff do Vite (bypass de `server.fs.deny` no Windows, que é o SO desta
máquina).

Este plano **não** toca em `src/`. Os 244 testes em 21 arquivos precisam continuar verdes
depois do upgrade — essa é a única prova de que a troca de toolchain foi neutra.

Purpose: sem o modo navegador do Vitest, o critério de sucesso 1 da fase (mesmo `hashWorld`
em quatro motores) não tem onde rodar.
Output: toolchain atualizada, dois configs de teste, `tools/README.md` com a convenção de
scripts Node, e `ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-formato-e-costuras/01-CONTEXT.md
@.planning/phases/01-formato-e-costuras/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Portão de legitimidade de pacotes ([SUS] em `vitest`)</name>
  <read_first>
    - .planning/phases/01-formato-e-costuras/01-RESEARCH.md § Package Legitimacy Audit (a tabela dos 13 pacotes e o parágrafo sobre o falso positivo)
  </read_first>
  <what-built>
    Nada ainda — este portão vem **antes** de qualquer `npm i`. O `slopcheck` 0.6.1 auditou
    os 13 pacotes recomendados em 2026-08-31: **12 OK, 1 SUS, 0 SLOP**. O único [SUS] é
    `vitest`, e o motivo dado pela ferramenta é heurística de distância de edição:
    *"Suspiciously close to 'vite'. Could be a typosquat."*

    Três evidências independentes de que é falso positivo:
    1. `vitest@2.1.9` já está instalado neste repositório e roda os 244 testes hoje;
    2. o `repository.url` do pacote aponta para `github.com/vitest-dev/vitest`;
    3. `@vitest/browser-playwright@4.1.11` declara peer `vitest@4.1.11` — os dois saem do
       mesmo monorepo.
  </what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/vitest e confirmar: repositório
       `github.com/vitest-dev/vitest`, downloads semanais na casa dos milhões, última
       publicação recente.
    2. Confirmar que a versão alvo `4.1.11` existe na lista de versões.
    3. Abrir https://www.npmjs.com/package/@vitest/browser-playwright e confirmar que o
       pacote pertence à mesma org e declara peer `vitest`.
    4. Nenhum outro pacote da instalação precisa de conferência: os outros 12 saíram [OK].
  </how-to-verify>
  <files>package.json</files>
  <action>
    Parar antes de qualquer `npm i` e apresentar ao desenvolvedor o veredito do `slopcheck`
    com as tres evidencias acima, mais os dois enderecos de `npmjs.com` a conferir. Nao
    instalar nada, nao editar `package.json` e nao prosseguir para a Task 2 ate receber
    aprovacao explicita. Este portao nao e auto-aprovavel: `auto_advance` e o modo `yolo` nao
    valem para portao de legitimidade de pacote. Registrar a justificativa do falso positivo no
    SUMMARY para que o alerta nao reapareca como novidade a cada re-execucao.
  </action>
  <verify>
    <automated>node -e "const{execSync}=require('child_process');const s=execSync('git status --porcelain package.json package-lock.json',{encoding:'utf8'});if(s.trim()){console.error('houve instalacao antes do portao');process.exit(1)}console.log('nenhuma instalacao antes do portao')"</automated>
  </verify>
  <acceptance_criteria>
    - O revisor humano confirmou por escrito que `vitest` e `@vitest/browser-playwright`
      sao os pacotes oficiais do projeto Vitest.
    - Nenhum `npm i` foi executado antes desta confirmacao: `git status --porcelain package.json package-lock.json` esta vazio ao fim desta task.
    - O SUMMARY registra a justificativa do falso positivo, com as tres evidencias.
  </acceptance_criteria>
  <done>O unico pacote marcado [SUS] pelo `slopcheck` foi conferido por um humano contra o registro npm, e a justificativa ficou registrada.</done>
  <resume-signal>Escreva "aprovado" ou descreva o que encontrou de errado</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Subir a toolchain e criar os dois configs de teste</name>
  <files>package.json, package-lock.json, vitest.config.ts, vitest.browser.config.ts, eslint.config.js, tools/README.md</files>
  <read_first>
    - package.json (estado atual: `dependencies: {}`, Vite 5.4.9, TS 5.6.3, Vitest 2.1.3, 6 scripts de uma linha)
    - eslint.config.js (a regra de pureza de `src/sim/**` e o `ignores: ['dist', 'public', 'node_modules']` da linha 4)
    - tsconfig.json (`"types": ["vite/client"]` é o que faz `import.meta.glob` compilar em tests/purity.test.ts — não mexer)
    - vite.config.ts (o molde de config do projeto: ESM, `defineConfig`, um comentário de uma linha explicando o porquê)
    - .planning/phases/01-formato-e-costuras/01-RESEARCH.md § Standard Stack (a lista de instalação) e § Code Examples 1 (o `vitest.browser.config.ts`)
  </read_first>
  <action>
    Instalar exatamente estas versões, como **devDependencies** (o `dependencies: {}` da raiz
    é invariante e não muda nesta task):

    - `vite@7.3.6`, `typescript@6.0.3`, `vitest@4.1.11`, `typescript-eslint@8.68.0`, `eslint@10.9.1`
    - `@vitest/browser-playwright@4.1.11`
    - `playwright@1.62.1` — instalar com `-E` (pin exato, **sem circunflexo**). As builds dos
      motores são a variável sob teste; um minor do Playwright troca essas builds e
      reintroduz exatamente a não-determinismo que o portão existe para pegar.
    - `@stdlib/math-base-special-sin@0.3.1`, `@stdlib/math-base-special-cos@0.3.1`,
      `@stdlib/math-base-special-atan2@0.3.1` — oráculo do port fdlibm do plano 01-09. Puxam
      153 pacotes transitivos: **devDependency apenas, jamais runtime**.
    - `ajv@8.20.0` — validador de manifesto de assets do plano 01-11, roda em `tools/`.

    Depois: `npx playwright install chromium firefox webkit`.

    Não subir para TypeScript 7: `typescript-eslint@8.68.0` declara peer
    `typescript >=4.8.4 <6.1.0` e o TS 7 quebra o portão de lint.

    Criar `vitest.config.ts` na raiz (hoje o Vitest roda com defaults sobre `vite.config.ts`):
    `include` de `tests/**/*.test.ts` e `exclude` explícito de `tests/cross-engine.test.ts`
    — esse arquivo nasce no plano 01-04 e só roda pelo config de navegador, senão ele
    passaria em Node e daria falsa segurança.

    Criar `vitest.browser.config.ts` na raiz: `test.include` apenas
    `['tests/cross-engine.test.ts']`, `test.browser` com `enabled: true`,
    `provider: playwright()` importado de `@vitest/browser-playwright`, `headless: true` e
    `instances: [{ browser: 'chromium' }, { browser: 'firefox' }, { browser: 'webkit' }]`.
    Usar `browser.instances`, não `test.workspace` nem projetos separados — o nome do motor
    passa a vir do nome do projeto do Vitest, o que faz a falha dizer
    `webkit > determinismo entre motores` sem encanamento extra.

    Acrescentar aos `scripts` do `package.json`, na forma de uma linha que o projeto já usa:
    `"test:browser": "vitest run --config vitest.browser.config.ts"`.

    Em `eslint.config.js`, acrescentar `'tools'` ao array `ignores` da linha 4, ao lado de
    `dist`, `public` e `node_modules`.

    Criar `tools/README.md` fixando a convenção de scripts Node — o repositório **não tem
    nenhum** `.mjs`/`.cjs` hoje, então as cinco perguntas abaixo não têm resposta no código e
    ficam decididas aqui, em português, para todos os planos seguintes:
    1. **Extensão `.mjs` explícita**, mesmo com `"type": "module"` na raiz — os scripts vivem
       fora dos workspaces e a extensão remove a ambiguidade de qual `package.json` vale.
    2. **Invocação sempre por script de `package.json`**, uma linha cada; o `ci.yml` chama
       `npm run <script>`, nunca o caminho do arquivo.
    3. **Falha**: `console.error` com prefixo `arquivo:ponteiro: mensagem` e
       `process.exit(1)`. Sucesso: uma linha em `stdout` e saída 0. Sem `throw` não tratado.
    4. **`tsconfig.json` `include` não muda**: `tools/` é JavaScript puro e fica fora da
       checagem de tipos.
    5. **`tools/` fica no `ignores` do ESLint**; a cobertura desses scripts vem de rodá-los
       no CI, não de lint.

    Rodar a suíte inteira depois do upgrade. Se `npx tsc --noEmit` acusar erros novos do TS 6,
    corrigir os erros no código — **não** afrouxar opção nenhuma do `tsconfig.json`
    (`strict`, `noUnusedLocals`, `noUnusedParameters` ficam como estão). Se aparecerem mais
    de 10 erros, parar e relatar em vez de continuar.
  </action>
  <verify>
    <automated>npx vitest run && npm run lint && npx tsc --noEmit && npm audit --audit-level=high</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` reporta **244 testes em 21 arquivos, todos verdes** — o mesmo número de antes.
    - `npm run lint` sai com código 0.
    - `npx tsc --noEmit` sai com código 0.
    - `npm audit --audit-level=high` não reporta nenhuma vulnerabilidade high ou critical (T-1-06).
    - `grep '"playwright"' package.json` mostra a versão `1.62.1` **sem** `^` nem `~`.
    - `grep '"dependencies"' -A1 package.json` mostra `{}` — o objeto continua vazio.
    - `vitest.browser.config.ts` contém as três instâncias `chromium`, `firefox` e `webkit`.
    - `vitest.config.ts` exclui `tests/cross-engine.test.ts`.
    - `tools/README.md` existe e responde às cinco perguntas numeradas.
    - `node -e "console.log(require('./package.json').scripts['test:browser'])"` imprime o comando do config de navegador.
  </acceptance_criteria>
  <done>Toolchain alvo instalada, suíte verde, audit limpo de high/critical, e os dois configs de teste existem.</done>
</task>

<task type="auto">
  <name>Task 3: `ci.yml` — o primeiro workflow de teste do repositório</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (o único workflow existente, 42 linhas; é o esqueleto de job a copiar)
    - package.json (os nomes de script que o workflow vai chamar)
    - tools/README.md (regra 2: o CI chama `npm run <script>`, nunca o caminho do arquivo)
  </read_first>
  <action>
    Criar `.github/workflows/ci.yml`, disparado em `pull_request` e em `push`. **Não tocar em
    `deploy.yml`** — quem mata o GitHub Pages é INFRA-01, na fase 2.

    Um job `test`, `runs-on: ubuntu-latest`, com `actions/checkout@v4` e
    `actions/setup-node@v4` em `node-version: '24'` e `cache: npm` — o `deploy.yml` usa
    `node-version: 20`, e tanto a máquina de dev quanto o `CLAUDE.md` estão em Node 24 LTS.

    Passos, nesta ordem: `npm ci` → `npm run lint` → `npm test`.

    Depois, o cache dos navegadores do Playwright com `actions/cache@v4` sobre
    `~/.cache/ms-playwright`, com a chave contendo a **versão exata** do Playwright lida do
    `package-lock.json` (a versão é a variável do experimento: um cache compartilhado entre
    versões diferentes invalida o portão). Em seguida
    `npx playwright install --with-deps chromium firefox webkit` e `npm run test:browser`.

    O passo `npm run test:browser` nasce com `continue-on-error: true` e um comentário de uma
    linha, em português, dizendo: *"vermelho de propósito até o plano 01-12 trocar a
    trigonometria; a remoção deste `continue-on-error` é critério de aceitação daquele
    plano"*. Isso é deliberado — o portão precisa provar que falha contra o código de hoje
    antes de provar que passa.

    Por último, `npm run build`.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const need=['node-version','24','npm ci','npm run lint','npm test','ms-playwright','test:browser','continue-on-error']; const miss=need.filter(s=>!y.includes(s)); if(miss.length){console.error('faltando: '+miss.join(', '));process.exit(1)} console.log('ci.yml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` existe e dispara em `pull_request` e `push`.
    - O workflow usa `node-version: '24'`; `grep -c "node-version: 20" .github/workflows/ci.yml` retorna 0.
    - Contém um passo de cache de `~/.cache/ms-playwright` cuja chave inclui a versão do Playwright.
    - Contém `npx playwright install --with-deps chromium firefox webkit`.
    - O passo `npm run test:browser` tem `continue-on-error: true` e o comentário citando o plano 01-12.
    - `git diff --name-only` **não** inclui `.github/workflows/deploy.yml`.
  </acceptance_criteria>
  <done>CI de teste existe, roda em Node 24, instala e cacheia os três navegadores, e o portão de navegador está marcado como vermelho-de-propósito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro npm → árvore de dependências local | Código de terceiros entra na máquina de dev e no CI |
| dev server do Vite → sistema de arquivos | O Vite 5.4.21 permite bypass de `server.fs.deny` no Windows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Tampering | devDependencies novas (13 pacotes) | mitigate | `slopcheck` 0.6.1 já executado (12 OK, 1 SUS, 0 SLOP); checkpoint humano bloqueante na Task 1 para o [SUS] `vitest`; versões pinadas no `package-lock.json`; `playwright` pinado exato com `-E` |
| T-1-06 | Information Disclosure | dev server do Vite (GHSA-fx2h-pf6j-xcff) | mitigate | Upgrade para `vite@7.3.6` na Task 2; `npm audit --audit-level=high` sem achados é critério de aceitação |
| T-1-01 | Spoofing | `SIM_VERSION` | accept (nesta fase) | Ainda não existe valor de `SIM_VERSION`; o controle nasce nos planos 01-06 e 01-07 |
</threat_model>

<verification>
- `npx vitest run` — 244 testes verdes, mesmo número de antes do upgrade
- `npm run lint` e `npx tsc --noEmit` — saída 0
- `npm audit --audit-level=high` — sem high nem critical
- `npm run test:browser` — sobe os três motores; pode falhar por ausência de
  `tests/cross-engine.test.ts` (ele nasce em 01-04), o que é esperado neste plano
</verification>

<success_criteria>
- Toolchain do `CLAUDE.md` instalada, com `playwright` pinado exato
- Dois configs de teste na raiz, com o cross-engine isolado no config de navegador
- `tools/README.md` responde às cinco perguntas de convenção que o repositório não respondia
- `ci.yml` existe, roda em Node 24 e cacheia os navegadores; `deploy.yml` intocado
- `dependencies: {}` na raiz continua vazio
</success_criteria>

<output>
Create `.planning/phases/01-formato-e-costuras/01-01-SUMMARY.md` when done
</output>

---
phase: 01-formato-e-costuras
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ulid.ts
  - src/app/ledger.ts
  - src/app/forge.ts
  - src/app/save.ts
  - src/ui/settings.ts
  - tests/ulid.test.ts
  - tests/ledger.test.ts
autonomous: true
requirements: [FORM-05]

must_haves:
  truths:
    - "O saldo de soul gold é a soma dos eventos do ledger, não um contador mutável"
    - "Aplicar o mesmo evento duas vezes não muda o saldo — o `eventId` é um ULID gerado no cliente no momento do evento, e é a chave de idempotência que o servidor da fase 6 deduplica por `UNIQUE(id)` (D-27)"
    - "Gastar no forge grava um evento negativo com id próprio no mesmo ledger, e o nível do forge vira estado derivado gravado na mesma transação que o gasto (D-28)"
    - "Um ULID gerado neste cliente tem 26 caracteres do alfabeto Crockford e é monotônico dentro do mesmo milissegundo"
    - "O `progress.soulGold` que existia em `dungeonguys2_save_v1` foi descartado, sem código de migração"
  artifacts:
    - path: "src/app/ulid.ts"
      provides: "ULID escrito à mão sobre `crypto.getRandomValues` — sem dependência de runtime"
      exports: ["ulid", "createUlidFactory"]
    - path: "src/app/ledger.ts"
      provides: "Ledger append-only de soul gold, núcleo puro separado do localStorage"
      exports: ["balance", "appendEvent", "compact", "Ledger"]
      contains: "dungeonguys2_ledger_v1"
    - path: "tests/ledger.test.ts"
      provides: "FORM-05 — saldo, idempotência, gasto negativo, compactação"
    - path: "tests/ulid.test.ts"
      provides: "FORM-05 — 26 chars, alfabeto, monotonicidade, ordem lexicográfica = temporal"
  key_links:
    - from: "src/app/forge.ts"
      to: "src/app/ledger.ts"
      via: "buyForge grava evento negativo; finishRun grava evento positivo"
      pattern: "appendEvent"
    - from: "src/app/ledger.ts"
      to: "src/app/ulid.ts"
      via: "id do evento"
      pattern: "ulid"
---

<objective>
Trocar o contador mutável `progress.soulGold` por um **ledger append-only** cujo saldo é a
soma dos eventos, com um ULID gerado no cliente como chave de idempotência (D-27), o gasto
como evento negativo no mesmo ledger (D-28), chave própria `dungeonguys2_ledger_v1` (D-29) e
regra de compactação decidida agora, mesmo que o servidor só exista na fase 6.

Isso é formato, não feature: last-write-wins numa moeda **perde ou duplica dinheiro**. Com
ledger e `UNIQUE(id)`, sincronizar duas vezes é no-op e dois aparelhos offline simplesmente
somam. É o que faz o critério 3 da fase 6 ("restaurar um save antigo não ressuscita saldo já
gasto") funcionar sem caso especial.

O `progress.soulGold` de hoje é **descartado** (D-26): o ledger nasce vazio, sem código de
migração e sem tipo de evento `legacy`. O jogo nunca foi publicado sob domínio próprio e o
único save real é o do desenvolvedor.

Nenhum arquivo de `src/sim/` é tocado — o ledger é estado de conta, não de mundo, e `sim/`
não pode ver `localStorage` nem `Date`.

Purpose: FORM-05 e o critério 3 da fase 6.
Output: `app/ulid.ts`, `app/ledger.ts`, `app/forge.ts` religado ao ledger, e dois arquivos de
teste novos — os primeiros do repositório a importar de `src/app/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-formato-e-costuras/01-CONTEXT.md
@.planning/phases/01-formato-e-costuras/01-VALIDATION.md
@src/app/save.ts
@src/app/forge.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ULID escrito à mão, com os vetores da spec como teste</name>
  <files>src/app/ulid.ts, tests/ulid.test.ts</files>
  <read_first>
    - src/sim/rng.ts (`:1-9` e `:12-18` — o precedente do projeto para aritmética de bits determinística: cabeçalho `// nome.ts — o que é`, `Math.imul`, `>>>`, funções curtas com JSDoc de uma linha)
    - tests/rng.test.ts (o molde de teste de módulo puro: import direto, `describe` com o nome do módulo, `it` como frase declarativa em português, corpus determinístico em laço)
    - package.json (confirmar que `dependencies` continua `{}` — é por isso que este módulo é escrito à mão em vez de usar `ulid@3.0.2`)
  </read_first>
  <behavior>
    - Um id tem exatamente 26 caracteres.
    - Todo caractere pertence ao alfabeto Crockford Base32 `0123456789ABCDEFGHJKMNPQRSTVWXYZ` (sem I, L, O, U).
    - Os 10 primeiros caracteres codificam o timestamp em ms (48 bits); os 16 seguintes, os 80 bits de aleatoriedade.
    - Dois ids gerados no **mesmo** milissegundo são estritamente crescentes na ordem lexicográfica: o componente aleatório é incrementado em 1 no bit menos significativo, com carry.
    - Ids gerados em milissegundos diferentes ordenam lexicograficamente na mesma ordem que os timestamps.
    - Com o mesmo relógio e a mesma fonte de bytes injetados, a saída é reproduzível (é isso que torna o módulo testável).
  </behavior>
  <action>
    Criar `src/app/ulid.ts` transcrevendo a spec do ULID (github.com/ulid/spec): 128 bits =
    48 bits de timestamp em ms + 80 bits de aleatoriedade; string canônica de 26 caracteres
    (10 de tempo + 16 de aleatoriedade) em Crockford Base32
    `0123456789ABCDEFGHJKMNPQRSTVWXYZ`; ordenação lexicográfica igual à ordenação temporal;
    dentro do mesmo milissegundo, o componente aleatório é **incrementado em 1 no bit menos
    significativo, com carry**.

    Escrever à mão é decisão registrada: `ulid@3.0.2` e `ulidx@2.4.1` são legítimos, mas o
    ledger roda no jogo publicado e `dependencies: {}` é invariante do `CLAUDE.md`. A spec
    cabe em cerca de 40 linhas.

    Superfície pública, desenhada para ser testável sem relógio de parede e sem mockar
    global: exportar `createUlidFactory(deps)` onde `deps` tem `now(): number` e
    `randomBytes(n: number): Uint8Array`, e exportar `ulid` como a instância padrão ligada a
    `Date.now` e `crypto.getRandomValues`. Nunca `Math.random` — o id é chave de dedupe e
    precisa de aleatoriedade criptográfica (T-1-02). Este módulo vive em `app/`; `sim/` jamais
    o importa.

    Cabeçalho do arquivo em inglês, na forma do projeto: `// ulid.ts — ...` seguido da fonte
    da spec e da razão de não usar a dependência.

    Escrever `tests/ulid.test.ts` **antes** da implementação, cobrindo os seis
    comportamentos do bloco acima. Proibido `toBeCloseTo` em qualquer teste desta fase.
    Nomes de teste em português, como o resto da suíte.
  </action>
  <verify>
    <automated>npx vitest run tests/ulid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/ulid.test.ts` passa.
    - Um teste assere `length === 26` e outro assere que `/^[0-9A-HJKMNP-TV-Z]{26}$/` casa com 1.000 ids gerados.
    - Um teste gera pelo menos 100 ids com `now()` fixo e assere que a lista é estritamente crescente quando ordenada lexicograficamente e comparada à ordem de geração.
    - Um teste assere que ids gerados com `now()` crescente ordenam lexicograficamente na mesma ordem.
    - `grep -c "Math.random" src/app/ulid.ts` retorna 0.
    - `grep -c "toBeCloseTo" tests/ulid.test.ts` retorna 0.
    - `node -e "const p=require('./package.json'); if(Object.keys(p.dependencies).length) process.exit(1)"` sai 0.
  </acceptance_criteria>
  <done>ULID gerado no cliente, testado contra os quatro invariantes da spec, sem dependência de runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O ledger append-only, com o núcleo puro separado do `localStorage`</name>
  <files>src/app/ledger.ts, tests/ledger.test.ts</files>
  <read_first>
    - src/app/save.ts (`:56-129` — o único módulo de persistência do projeto; copiar o padrão: `const KEY` versionada, módulo-IIFE com `export const`, `persist()`/`load()` com `try/catch` silencioso, getter `get data()`)
    - tests/camera.test.ts (o único precedente de testar módulo não-`sim/` em Node: `camera.ts` é testável porque recebe `w`/`h` por argumento e não toca DOM — o ledger precisa nascer com a mesma propriedade)
    - src/app/ulid.ts (a fábrica criada na Task 1)
    - docs/adr/0010-soul-gold-ledger-append-only.md (se o plano 01-02 já tiver rodado; se não, usar D-26 a D-29 do 01-CONTEXT.md)
  </read_first>
  <behavior>
    - `balance([])` é 0.
    - `balance` de uma lista de eventos é a soma aritmética dos `amount`, positivos e negativos.
    - Aplicar duas vezes o **mesmo** evento (mesmo `id`) resulta no mesmo saldo: a segunda aplicação é no-op.
    - Dois eventos com `amount` diferente e ids diferentes somam; a ordem de aplicação não muda o saldo.
    - Um gasto é um evento com `amount` negativo e id próprio — não é uma subtração num campo.
    - `compact` colapsa todos os eventos que carregam marca d'água de confirmação num único evento de saldo consolidado, preserva os pendentes individualmente, e **não muda o saldo**.
    - O núcleo puro (`balance`, `appendEvent`, `compact`) funciona sobre arrays, sem tocar `localStorage`.
  </behavior>
  <action>
    Criar `src/app/ledger.ts` com duas metades explicitamente separadas.

    **Núcleo puro** (testável em Node, sem DOM): o tipo `LedgerEvent` com os campos `id`
    (ULID gerado no cliente, chave de dedupe do servidor na fase 6), `accountId` (carimbado na
    criação, mesmo enquanto a conta é local e não-reivindicada — D-31), `amount` (positivo =
    concessão, negativo = gasto), `reason` (`'run' | 'mission' | 'season' | 'forge'`), `at`
    (ms de época, **só para exibição** — quem carrega a ordenação é o ULID) e `confirmed`
    opcional (marca d'água do servidor). Mais as funções `balance(events)`,
    `appendEvent(events, event)` — que ignora o evento se o `id` já estiver na lista, é a
    idempotência de T-1-02 — e `compact(events)`, que colapsa os confirmados num único evento
    consolidado com a marca d'água mais alta e mantém os pendentes individualmente (D-29).

    **Metade persistente**: um `export const Ledger` no formato módulo-IIFE que
    `src/app/save.ts` já usa, com `const KEY = 'dungeonguys2_ledger_v1'` — chave **própria**,
    separada de `dungeonguys2_save_v1` (D-29) — `load()`/`persist()` com `try/catch`
    silencioso, getter `get events()`, e os métodos `grant(amount, reason)` e
    `spend(amount, reason)` que geram o ULID, montam o evento e persistem. O ledger nasce
    vazio: **nenhum** código de migração e **nenhum** tipo de evento `legacy` (D-26).

    Cabeçalho do arquivo em inglês explicando por que é ledger e não contador — a frase que
    importa é que last-write-wins numa moeda perde ou duplica dinheiro.

    Escrever `tests/ledger.test.ts` **antes** da implementação, cobrindo os sete
    comportamentos do bloco acima. Os testes exercitam o núcleo puro por arrays; nada de
    `localStorage` no teste (o projeto não tem jsdom e não vai ter).
  </action>
  <verify>
    <automated>npx vitest run tests/ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/ledger.test.ts` passa.
    - Existe um teste que aplica o mesmo evento duas vezes e assere que o saldo é idêntico ao de uma aplicação.
    - Existe um teste que assere que `compact` preserva o saldo e reduz o número de eventos confirmados a 1.
    - Existe um teste que assere que um gasto entra como `amount` negativo.
    - `grep -c "dungeonguys2_ledger_v1" src/app/ledger.ts` retorna pelo menos 1.
    - `grep -ci "legacy\|migra" src/app/ledger.ts` retorna 0 — não há código de migração (D-26).
    - `grep -c "localStorage" tests/ledger.test.ts` retorna 0.
    - `npx vitest run` continua com toda a suíte verde.
  </acceptance_criteria>
  <done>Ledger append-only com núcleo puro testado, chave própria e regra de compactação escrita em código.</done>
</task>

<task type="auto">
  <name>Task 3: Religar o forge ao ledger e apagar `progress.soulGold`</name>
  <files>src/app/forge.ts, src/app/save.ts, src/ui/settings.ts</files>
  <read_first>
    - src/app/forge.ts (`:60`, `:65`, `:73`, `:92-93`, `:127` — os seis pontos que leem ou escrevem `Save.data.progress.soulGold`; e `buyForge`/`finishRun` inteiros)
    - src/app/save.ts (`:40` a declaração `soulGold: number;` e `:65` o valor em `defaults()`)
    - src/ui/settings.ts (`:303` — a linha `['SOUL GOLD', p.soulGold || 0]` do painel de perfil)
    - src/app/ledger.ts (a superfície criada na Task 2)
  </read_first>
  <action>
    Trocar todas as leituras de `Save.data.progress.soulGold` por
    `balance(Ledger.events)` em `src/app/forge.ts` — são os pontos `:60` (`forgeGold`), `:65`
    (`soulGold` do DOM), `:73` (o `disabled` do botão de compra) e `:92` (a checagem de saldo
    em `buyForge`).

    Em `buyForge` (`:88-99`), substituir `Save.data.progress.soulGold -= cost` por uma
    chamada a `Ledger.spend(cost, 'forge')`, mantendo `Save.data.progress.forge[key] =
    forgeLevel(key) + 1` e `Save.persist()` **na mesma sequência** — é o que D-28 quer dizer
    com "o nível do forge vira estado derivado, gravado na mesma transação que o gasto". O
    nível do forge continua morando no save; o que muda de lugar é a moeda.

    Em `finishRun` (`:127`), substituir `Save.data.progress.soulGold += forged` por
    `Ledger.grant(forged, 'run')`.

    Em `src/app/save.ts`, remover a declaração `soulGold: number;` do tipo `SaveData` (`:40`)
    e o par `soulGold: 0,` de `defaults()` (`:65`). **Não** escrever código de migração e
    **não** ler o valor antigo antes de descartá-lo (D-26): um save que ainda tiver a chave
    simplesmente a perde no primeiro `persist()`, porque `defaults()` deixa de conhecê-la.

    Em `src/ui/settings.ts:303`, trocar `p.soulGold || 0` pelo saldo vindo do ledger.

    Nada em `src/sim/` muda: o `hashWorld` de qualquer run continua idêntico, e a suíte de 244
    testes é a prova disso.
  </action>
  <verify>
    <automated>npx vitest run && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "soulGold" src/app/save.ts` não retorna nada.
    - `grep -rn "progress.soulGold" src/` não retorna nada.
    - `grep -c "Ledger.spend" src/app/forge.ts` retorna 1 e `grep -c "Ledger.grant" src/app/forge.ts` retorna 1.
    - `npx tsc --noEmit` sai com código 0 — o tipo `SaveData` já não declara `soulGold` e nenhum consumidor o lê.
    - `npx vitest run` mantém os 244 testes originais verdes, mais os novos de `ulid` e `ledger`.
    - `npm run lint` sai com código 0.
    - `git diff --stat src/sim/` está vazio — nenhum arquivo de simulação foi tocado.
  </acceptance_criteria>
  <done>A moeda é o ledger; o contador mutável saiu do save; o forge compra gravando um evento negativo e o fim de run grava um positivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → servidor de progressão (fase 6) | O `eventId` é gerado no **cliente** e o servidor confia nele como chave de dedupe |
| `localStorage` → aplicação | Um save restaurado de backup pode reintroduzir eventos já aplicados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Tampering | `eventId` do ledger colidindo ou sendo reusado | mitigate | ULID com 80 bits vindos de `crypto.getRandomValues` (nunca `Math.random`), monotonicidade dentro do mesmo ms testada em `tests/ulid.test.ts`, e `appendEvent` ignorando id repetido — a idempotência local espelha o `UNIQUE(id)` que a fase 6 aplica no servidor |
| T-1-02 | Tampering | restauração de save antigo ressuscitando saldo gasto | mitigate | O gasto é evento negativo no mesmo ledger (D-28): restaurar um estado antigo não remove o gasto, porque o saldo é soma e não um campo sobrescrito |
| T-1-05 | Tampering | dependência de ULID de terceiros | mitigate | Nenhuma: o ULID é escrito à mão e testado contra os vetores da spec, mantendo `dependencies: {}` (asserido no critério de aceitação da Task 1) |
</threat_model>

<verification>
- `npx vitest run tests/ulid.test.ts` e `npx vitest run tests/ledger.test.ts` verdes
- `npx vitest run` — os 244 testes originais continuam verdes
- `npm run lint` e `npx tsc --noEmit` saem com código 0
- `git diff --stat src/sim/` vazio
</verification>

<success_criteria>
- Saldo de soul gold derivado de eventos, nunca de contador
- Idempotência por id provada por teste
- Gasto como evento negativo, com o nível do forge gravado na mesma sequência
- `progress.soulGold` removido do save sem código de migração
- `dependencies: {}` intacto
</success_criteria>

<output>
Create `.planning/phases/01-formato-e-costuras/01-03-SUMMARY.md` when done
</output>

---
phase: 01-formato-e-costuras
plan: 12
type: execute
wave: 6
depends_on: ["01-04", "01-06", "01-07", "01-08", "01-09"]
files_modified:
  - packages/sim/src/arena.ts
  - packages/sim/src/boss.ts
  - packages/sim/src/combat.ts
  - packages/sim/src/enemies.ts
  - packages/sim/src/loot.ts
  - packages/sim/src/run.ts
  - packages/sim/src/special.ts
  - packages/sim/src/constants.ts
  - packages/sim/src/stats.ts
  - packages/sim/src/types.ts
  - packages/sim/src/world.ts
  - src/ui/labels.ts
  - src/ui/screens.ts
  - src/ui/shop.ts
  - src/render/entities.ts
  - src/main.ts
  - eslint.config.js
  - tests/golden/campaign-mage-3000.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [FORM-04, FORM-03, FORM-08]

must_haves:
  truths:
    - "A mesma seed produz `hashWorld` bit-idêntico em Chromium, Firefox, WebKit e Node, comparado contra o hash-ouro versionado"
    - "O teste falha, nomeando o motor divergente, se qualquer um sair da linha"
    - "Nenhum arquivo de `packages/sim` chama `Math.sin`, `Math.cos` ou `Math.atan2`, e o lint impede que volte"
    - "Renomear um rótulo de HUD não fecha mais a temporada: o vocabulário de apresentação saiu do bundle da simulação"
    - "A forma do `World` está congelada: `objectives` presente desde a criação, `nextWaveDelay` morto removido"
  artifacts:
    - path: "src/ui/labels.ts"
      provides: "STAT_LABELS e PCT_STATS fora do SIM_VERSION"
      exports: ["STAT_LABELS", "PCT_STATS"]
    - path: "eslint.config.js"
      provides: "A regra de pureza estendida com a trigonometria"
      contains: "atan2"
    - path: "tests/golden/campaign-mage-3000.json"
      provides: "O ouro re-baselinado — o primeiro e único desta fase"
  key_links:
    - from: "packages/sim/src/enemies.ts"
      to: "packages/sim/src/math.ts"
      via: "import de sin/cos/atan2"
      pattern: "from './math'"
---

<objective>
O momento da fase. Trocar os **27 call sites** de trigonometria (12 `sin`, 12 `cos`, 3
`atan2`, em 7 arquivos) pelo `sim/math.ts` do plano 01-09, fazer a limpeza de `sim/` que fica
cara depois do primeiro placar, e pagar o **único re-baseline** de hashes-ouro da fase.

O portão cross-engine está vermelho desde o plano 01-04, de propósito. Esta é a mudança que o
torna verde nos quatro motores — e o SUMMARY do plano 01-04 registrou os três hashes
divergentes exatamente para que a comparação seja auditável.

Três limpezas viajam no mesmo commit, porque **toda limpeza de `sim/` fica cara depois do
primeiro board** (a partir da fase 9, cada uma delas fecharia uma temporada sozinha):
1. `STAT_LABELS` e `PCT_STATS` (`stats.ts:65-76`) são vocabulário de HUD consumido só por
   `ui/screens.ts` e `ui/shop.ts`, e hoje entram no bundle e, portanto, no `SIM_VERSION` —
   o que contradiz D-06 ("um ajuste de HUD não fecha a temporada"). **Saem para `ui/`.**
2. `SPRITE_SCALE` (`constants.ts:59`) sai de `sim/` como conceito de sprite (D-19). O número
   continua em `sim/` sob outro nome, porque `e.scale` é estado do `World`; o que sai é a
   ideia de "escala de sprite", que passa a viver em `render/`.
3. `world.nextWaveDelay` (`types.ts:270`, `world.ts:37`) é inicializado em `3000` e **nunca
   lido**. Está prestes a entrar num formato congelado. Remover custa uma linha hoje.

E mais duas mudanças de forma que precisam da mesma passagem: `world.objectives` nasce (FORM-08
— campo do `World`, não evento drenável), e o `playerId` local passa de `'p1'` para `'p0'`
(FORM-01/D-30: os slots são `p0..p3`). Ambas mudam a impressão digital do mundo, e o objetivo
é pagar **um** re-baseline, não cinco.

**Disciplina de commit, que é o que torna a fase 9 auditável:** o código vai num commit; o
arquivo de ouro vai em **outro commit, sozinho**. Assim `git log -- tests/golden/` continua
sendo a lista completa e limpa de tudo que já alterou a simulação.

Purpose: critério de sucesso 1 da fase; FORM-04, FORM-03 (fronteira do hash) e FORM-08 (forma
do campo).
Output: `sim/` sem `Math.sin`/`cos`/`atan2`, lint que impede a volta, `World` com a forma
congelada, e o portão verde nos quatro motores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-formato-e-costuras/01-CONTEXT.md
@.planning/phases/01-formato-e-costuras/01-VALIDATION.md
@eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Os 27 call sites e a regra de lint que impede a volta</name>
  <files>packages/sim/src/arena.ts, packages/sim/src/boss.ts, packages/sim/src/combat.ts, packages/sim/src/enemies.ts, packages/sim/src/loot.ts, packages/sim/src/run.ts, packages/sim/src/special.ts, packages/sim/src/constants.ts, eslint.config.js</files>
  <read_first>
    - packages/sim/src/math.ts (o módulo do plano 01-09 — a superfície a importar)
    - packages/sim/src/constants.ts (`:1-20` — o bloco "Floating-point doctrine for sim/", que diz `OPEN ITEM for Marco 2` sobre exatamente o que esta task entrega; ele precisa ser reescrito)
    - packages/sim/src/arena.ts (`:105-111` — a forma típica de call site: a troca é literalmente de import, sem mudança de expressão)
    - packages/sim/src/combat.ts (`:96-99` e `:110-113` — o único lugar onde `atan2` alimenta um laço de normalização de ângulo)
    - packages/sim/src/boss.ts (`:109-123` — o ramo `ring`, agora coberto pelos testes do plano 01-08)
    - eslint.config.js (`:20-23` — o `no-restricted-properties` com `Math.random` e `Date.now`, e a convenção de mensagem: sempre dizer **o que usar em vez** e citar a origem da regra)
    - .planning/phases/01-formato-e-costuras/01-PATTERNS.md (a tabela com as linhas exatas dos 27 call sites, arquivo por arquivo)
  </read_first>
  <action>
    Trocar `Math.sin`, `Math.cos` e `Math.atan2` por `sin`, `cos` e `atan2` importados de
    `./math` nos sete arquivos, nas linhas que a tabela de PATTERNS lista:
    `arena.ts:109`; `boss.ts:116`; `combat.ts:62,63,85,86` (`cos`/`sin`) e `:96,110` (`atan2`);
    `enemies.ts:156,157,170,171,304` (`atan2`), `:307,308,385,386,387,388`; `loot.ts:153,154`;
    `run.ts:239,240`; `special.ts:73,74`. Total: **27**. `constants.ts:4` é comentário e não é
    call site.

    A troca é de import, não de expressão: `Math.cos(a) * 2` vira `cos(a) * 2`. Nenhuma
    reescrita de fórmula, nenhuma reordenação de operação — qualquer uma das duas mudaria os
    bits por conta própria e contaminaria a atribuição da mudança de hash.

    **Nenhum outro `Math.*` sai.** Ficam, todos exatos por spec: `Math.sqrt` (25 usos),
    `Math.PI` (36), `Math.max` (35), `Math.min` (31), `Math.round` (16), `Math.floor` (7),
    `Math.abs` (2), `Math.imul` (2), `Math.trunc` (2).

    Reescrever o bloco de doutrina de `constants.ts:1-20`: o `OPEN ITEM` está fechado. O texto
    novo registra que `sin`, `cos` e `atan2` agora vêm de `sim/math.ts`, que `Math.sqrt`
    continua porque a spec o fixa em IEEE-754, e que a divergência entre motores foi **medida**
    neste repositório — três hashes diferentes em quatro motores, primeira divergência no tick
    361 — e não inferida.

    Estender `no-restricted-properties` em `eslint.config.js` para o glob
    `packages/sim/src/**/*.ts`, acrescentando `sin`, `cos`, `atan2`, `tan`, `pow`, `exp`, `log`
    e `hypot` sobre o objeto `Math`, cada um com mensagem no formato do arquivo — dizendo o que
    usar em vez (`sim/math.ts`) e citando a origem (decisão D-01). As regras existentes
    (`Math.random`, `Date.now`, os oito globais e os seis padrões de import) ficam.

    `packages/sim/src/math.ts` precisa ser **exceção** da própria regra, já que ele é o
    substituto: acrescentar um bloco de override para esse arquivo, ou escrever a regra de modo
    que ele não seja alcançado. Deixar isso explícito e comentado.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck:sim && npx vitest run tests/math-oracle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "Math\.(sin|cos|atan2)\(" packages/sim/src/ --include=*.ts` não retorna nada.
    - `grep -rc "from './math'" packages/sim/src/arena.ts packages/sim/src/boss.ts packages/sim/src/combat.ts packages/sim/src/enemies.ts packages/sim/src/loot.ts packages/sim/src/run.ts packages/sim/src/special.ts` retorna 1 em cada um dos sete.
    - `grep -rc "Math.sqrt" packages/sim/src/ --include=*.ts | awk -F: '{s+=$2} END{print s}'` continua em 25.
    - `npm run lint` sai 0.
    - Sabotagem controlada, desfeita em seguida: reintroduzir `Math.sin(0)` em `packages/sim/src/loot.ts` faz `npm run lint` falhar com a mensagem citando `sim/math.ts` e `D-01`. Registrar no SUMMARY.
    - O bloco de doutrina de `constants.ts` já não contém `OPEN ITEM`.
    - `npx vitest run tests/math-oracle.test.ts` continua verde.
  </acceptance_criteria>
  <done>A simulação inteira usa a trigonometria própria, e a regra de lint impede que `Math.sin` volte por descuido.</done>
</task>

<task type="auto">
  <name>Task 2: Congelar a forma do `World` e tirar o vocabulário de HUD do bundle</name>
  <files>packages/sim/src/stats.ts, packages/sim/src/constants.ts, packages/sim/src/types.ts, packages/sim/src/world.ts, packages/sim/src/enemies.ts, src/ui/labels.ts, src/ui/screens.ts, src/ui/shop.ts, src/render/entities.ts, src/main.ts</files>
  <read_first>
    - packages/sim/src/stats.ts (`:65-76` — `STAT_LABELS` e `PCT_STATS`, o vocabulário a mover)
    - src/ui/screens.ts (`:14` o import e `:46` o uso) e src/ui/shop.ts (`:17` o import; `:34`, `:69` e `:150` os usos) — os dois únicos consumidores
    - packages/sim/src/constants.ts (`:59` — `SPRITE_SCALE = 2`)
    - packages/sim/src/enemies.ts (`:34` o import e `:85` o uso `scale: def.scale || SPRITE_SCALE` — este uso escreve `e.scale`, que é campo do `World`, então o **número** tem de continuar em `sim/`)
    - src/render/entities.ts (`:19` o import e `:40`, `:62`, `:157-158` os usos — é aqui que "escala de sprite" faz sentido)
    - packages/sim/src/types.ts (`:270` `nextWaveDelay: number;`; e o comentário sobre `eliteName`/`eliteTint`, que estabelece a doutrina "uma chave opcional iria e viria entre snapshots")
    - packages/sim/src/world.ts (`:6-52` — `createWorld`, o único lugar onde um campo novo do `World` nasce; `:37` `nextWaveDelay: 3000,`)
    - src/main.ts (`:76`, `:77`, `:123`, `:127`, `:150`, `:154` — os seis literais `'p1'`)
    - docs/adr/0012-objetivos-como-campo-do-world.md (a decisão registrada pelo plano 01-02)
    - packages/protocol/src/enums.ts (a tabela `OBJECTIVE_KIND`, que o tipo do sim precisa espelhar)
  </read_first>
  <action>
    **Vocabulário de HUD para fora do bundle.** Criar `src/ui/labels.ts` e mover para lá
    `STAT_LABELS` e `PCT_STATS`, verbatim. Reapontar os imports de `src/ui/screens.ts:14` e
    `src/ui/shop.ts:17`. Motivo, que vai como comentário no arquivo novo: enquanto eles
    estiverem dentro de `packages/sim`, renomear um rótulo de HUD muda o bundle, muda o
    `SIM_VERSION` e fecha uma temporada — o que contradiz D-06 diretamente.

    **`SPRITE_SCALE`.** O conceito sai de `sim/`, o número fica. Em
    `packages/sim/src/constants.ts`, renomear `SPRITE_SCALE` para `DEFAULT_ENTITY_SCALE`, com
    JSDoc explicando que é o multiplicador de tamanho **da entidade** — campo `scale` do
    `Enemy`, portanto estado do `World` versionado pelo `SIM_VERSION` — e que a escala de
    desenho de sprite deixou de morar aqui (D-19). Atualizar `enemies.ts:34,85`. Em
    `src/render/entities.ts`, declarar um `SPRITE_SCALE = 2` local e usar esse, sem importar
    nada de `sim/` para isso. Registrar no SUMMARY que a fase 7 é quem vai mudar esse número
    para 1 quando a arte de 32×48 entrar, e que essa mudança já não tocará em `sim/`.

    **`nextWaveDelay`.** Remover a declaração de `types.ts:270` e o par `nextWaveDelay: 3000,`
    de `world.ts:37`. `grep` confirma que ninguém lê o campo; se aparecer algum consumidor,
    parar e relatar em vez de improvisar.

    **`world.objectives` (FORM-08).** Acrescentar a `types.ts` o tipo `ObjectiveKind` — a união
    de string literal `'none' | 'defend' | 'hunt' | 'purge' | 'fetch' | 'extract'`, na mesma
    ordem da tabela `OBJECTIVE_KIND` de `packages/protocol` — e o tipo `ObjectiveState` com os
    campos `kind: ObjectiveKind`, `status: 'inactive' | 'active' | 'complete' | 'failed'`,
    `progress: number`, `target: number`, `ticksLeft: number` (`-1` quando não é cronometrado)
    e `marks: number[]` (ids de entidade que o objetivo acompanha; `[]` quando não usado).
    Acrescentar ao `World` o campo `objectives: ObjectiveState[]`, e a `createWorld` o valor
    inicial `objectives: []`.

    Três propriedades que precisam ir escritas como comentário, porque são o motivo de a forma
    ser essa: (a) é **campo do `World`, não evento drenável** — evento drenável não é
    verificável por replay, porque quem drena é `app/` e o que `app/` consumiu não sobrevive ao
    snapshot; (b) é **JSON-safe**, sem `Map`, sem `Set` e sem instância de classe, porque o
    `Rng` é a única instância de classe do `World` e vai continuar sendo; (c) está **sempre
    presente, com a mesma forma**, mesmo numa run de campanha sem missão — uma chave que só
    existe em modo missão é uma divergência de hash esperando o momento errado, e a doutrina
    já está escrita no comentário de `eliteName`/`eliteTint`.

    **`playerId` local.** Declarar em `src/main.ts` uma constante de módulo
    `const LOCAL_SLOT = 'p0'` (tipada como o slot de FORM-01) e trocar os seis literais `'p1'`
    de `:76`, `:77`, `:123`, `:127`, `:150` e `:154` por ela. Os testes existentes continuam
    usando `'p1'` e **não** mudam — eles montam mundos próprios e nada os liga ao slot do
    aplicativo. O ouro do plano 01-04 já usa `p0`, então esta mudança não acrescenta nada ao
    re-baseline.

    Ao fim desta task, `tests/golden.test.ts` está **vermelho** — a forma do `World` mudou e a
    trigonometria também. É o estado esperado; a Task 3 resolve.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck:sim && npx tsc --noEmit && npx vitest run tests/purity.test.ts tests/scc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "STAT_LABELS\|PCT_STATS" packages/sim/src/` não retorna nada, e `grep -c "export const STAT_LABELS" src/ui/labels.ts` retorna 1.
    - `grep -rn "SPRITE_SCALE" packages/sim/src/` não retorna nada; `grep -c "DEFAULT_ENTITY_SCALE" packages/sim/src/constants.ts` retorna pelo menos 1; `grep -c "SPRITE_SCALE" src/render/entities.ts` retorna pelo menos 1.
    - `grep -rn "nextWaveDelay" packages src tests` não retorna nada.
    - `grep -c "objectives" packages/sim/src/types.ts` retorna pelo menos 2 e `grep -c "objectives: \[\]" packages/sim/src/world.ts` retorna 1.
    - `grep -c "'p1'" src/main.ts` retorna 0 e `grep -c "LOCAL_SLOT" src/main.ts` retorna pelo menos 7.
    - `npx tsc --noEmit`, `npm run typecheck:sim` e `npm run lint` saem 0.
    - `npx vitest run tests/purity.test.ts` e `npx vitest run tests/scc.test.ts` continuam verdes.
    - `npx vitest run tests/golden.test.ts` **falha** — estado esperado ao fim desta task, resolvido na Task 3.
  </acceptance_criteria>
  <done>A forma do `World` está congelada e o vocabulário de apresentação saiu do bundle da simulação — as três limpezas caras pagas de uma vez.</done>
</task>

<task type="auto">
  <name>Task 3: O re-baseline, e o portão verde nos quatro motores</name>
  <files>tests/golden/campaign-mage-3000.json, .github/workflows/ci.yml</files>
  <read_first>
    - tools/golden/rebaseline.mjs (o caminho auditável criado no plano 01-04, com a regra de commit escrita no cabeçalho)
    - .planning/phases/01-formato-e-costuras/01-04-SUMMARY.md (os três hashes divergentes registrados como evidência: Node, Chromium, e Firefox/WebKit, com os ticks da primeira divergência)
    - .github/workflows/ci.yml (o passo `npm run test:browser` com `continue-on-error: true` e o comentário citando este plano)
    - .planning/phases/01-formato-e-costuras/01-VALIDATION.md (a linha FORM-04 do mapa: `npx vitest run --config vitest.browser.config.ts`)
  </read_first>
  <action>
    **Commitar primeiro o código das Tasks 1 e 2, sem o arquivo de ouro.** A regra escrita em
    `tools/golden/rebaseline.mjs` é que um commit que muda um hash-ouro não muda mais nada;
    respeitá-la aqui é o que faz `git log -- tests/golden/` valer alguma coisa na fase 9.

    Rodar `npm run golden:rebaseline -- --confirm`. Registrar no SUMMARY o hash antigo e o
    novo, e comparar com os valores medidos pela pesquisa (`9f870f80` em Node antes da troca).
    Commitar **apenas** `tests/golden/campaign-mage-3000.json`, com mensagem em português
    dizendo que a mudança vem da entrada de `sim/math.ts` e das três limpezas de forma do
    `World`.

    Rodar `npm run test:browser` e confirmar que os **três** motores passam. Rodar
    `npx vitest run tests/golden.test.ts` e confirmar a perna Node. Os quatro precisam produzir
    o **mesmo** hash — é o critério de sucesso 1 da fase, e é a primeira vez na história do
    projeto que isso é verdade.

    Se algum motor ainda divergir, **não** re-baselinar de novo: significa que sobrou um
    `Math.sin`/`cos`/`atan2` em algum caminho, ou que uma expressão foi reescrita na Task 1. Os
    checkpoints intermediários do ouro dizem em qual tick a divergência começa; usar isso para
    localizar em vez de tentar de novo.

    Remover o `continue-on-error: true` do passo `npm run test:browser` em
    `.github/workflows/ci.yml`, junto com o comentário que o justificava. A partir daqui o
    portão cross-engine é **bloqueante**: uma divergência entre motores derruba o CI, que é
    exatamente o que ele existe para fazer.

    Rodar a suíte inteira e confirmar que os 244 testes originais mais todos os criados na fase
    continuam verdes. Um teste que dependa de `nextWaveDelay` ou de `STAT_LABELS` importado de
    `sim/` teria falhado antes desta task — se falhar agora, é regressão real e não efeito do
    re-baseline.
  </action>
  <verify>
    <automated>npx vitest run && npm run test:browser</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:browser` sai com código 0: chromium, firefox e webkit **passam**.
    - `npx vitest run tests/golden.test.ts` passa — Node produz o mesmo hash.
    - O SUMMARY registra o hash antigo e o novo, e confirma que os quatro motores produzem o mesmo valor.
    - `grep -c "continue-on-error" .github/workflows/ci.yml` retorna 0.
    - `git log --oneline -1 -- tests/golden/campaign-mage-3000.json` aponta para um commit cujo `git show --stat` lista **um único arquivo**.
    - `npx vitest run` — suíte inteira verde.
    - `npm run sim:version:verify` sai 0 (o `SIM_VERSION` mudou, como esperado, e continua reprodutível).
  </acceptance_criteria>
  <done>Os quatro motores concordam. O portão que nasceu vermelho no plano 01-04 está verde e passou a ser bloqueante.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motor de JavaScript → resultado da simulação | Cada motor pode devolver bits diferentes para o mesmo `Math.sin` |
| mudança de `SIM_VERSION` → temporada de ranking | Qualquer byte do bundle que mude fecha uma temporada a partir da fase 9 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | re-baseline de hash-ouro sem rastro | mitigate | O único caminho é `npm run golden:rebaseline -- --confirm`, só em Node, e o commit do ouro toca **um** arquivo — asserido por `git show --stat` no critério de aceitação |
| T-1-01 | Spoofing | `SIM_VERSION` fechando temporada por mudança de HUD | mitigate | `STAT_LABELS`/`PCT_STATS` saem do bundle nesta task; a partir daqui, renomear rótulo de HUD não muda o hash — que é literalmente o que D-06 promete |
| T-1-03 | Denial of Service | divergência entre motores só descoberta em partida ao vivo | mitigate | O portão cross-engine deixa de ter `continue-on-error` e passa a derrubar o CI; sem ele, o sintoma apareceria na fase 4 dentro de uma sessão de rede, onde o custo de diagnóstico é dez vezes maior |
</threat_model>

<verification>
- `npm run test:browser` — três motores verdes
- `npx vitest run tests/golden.test.ts` — perna Node verde, mesmo hash
- `npx vitest run` — suíte inteira verde
- `npm run lint`, `npm run typecheck:sim`, `npx tsc --noEmit`, `npm run sim:version:verify` — todos 0
- `git show --stat` do commit do ouro lista um único arquivo
</verification>

<success_criteria>
- Critério de sucesso 1 da fase atingido: mesmo `hashWorld` em Node, Chromium, Firefox e WebKit
- Zero `Math.sin`/`cos`/`atan2` em `packages/sim`, com lint bloqueando a volta
- `STAT_LABELS`, `PCT_STATS` e o conceito de `SPRITE_SCALE` fora do `SIM_VERSION`
- `World` com forma congelada: `objectives` presente, `nextWaveDelay` removido, slot local `p0`
- Um único re-baseline, num commit isolado
</success_criteria>

<output>
Create `.planning/phases/01-formato-e-costuras/01-12-SUMMARY.md` when done
</output>

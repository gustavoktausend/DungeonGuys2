---
phase: 01-formato-e-costuras
plan: 10
type: execute
wave: 5
depends_on: ["01-04", "01-06"]
files_modified:
  - packages/protocol/src/inputCodec.ts
  - packages/protocol/src/runEnvelope.ts
  - packages/protocol/src/index.ts
  - src/app/input.ts
  - tests/inputLog.ts
  - tests/input-codec.test.ts
autonomous: true
requirements: [FORM-06]

must_haves:
  truths:
    - "O `InputState` que o sim vê já está quantizado: o número é o produto exato de um inteiro por um passo literal, não a saída de `Math.atan2` do motor"
    - "`decode(encode(x))` devolve exatamente `x` — a quantização é idempotente, que é a propriedade de que o replay precisa"
    - "Um tick sem input de um jogador repete o último input conhecido daquele jogador, e isso é parte do formato"
    - "O envelope de run carrega um teto de ticks desde já, mesmo que quem o aplique seja a fase 9"
    - "`Math.hypot` e `Math.atan2` continuam em `app/input.ts` e isso é seguro, porque o `InputState` trafega em vez de ser recalculado"
  artifacts:
    - path: "packages/protocol/src/inputCodec.ts"
      provides: "Quantização, pacote de 6 bytes, delta+RLE e preenchimento de buracos"
      exports: ["AIM_STEP", "quantize", "packTick", "unpackTick", "encodeLog", "decodeLog", "decodeInputRecords"]
    - path: "packages/protocol/src/runEnvelope.ts"
      provides: "O envelope de run de D-10 com o teto de ticks de T-1-03"
      exports: ["RunEnvelope", "MAX_RUN_TICKS", "RUN_FORMAT_VERSION"]
    - path: "tests/input-codec.test.ts"
      provides: "FORM-06 — idempotência, 6 bytes, `-0` normalizado, buraco = último input"
  key_links:
    - from: "src/app/input.ts"
      to: "packages/protocol/src/inputCodec.ts"
      via: "collect() quantiza antes de montar o InputState"
      pattern: "quantize"
---

<objective>
Fechar FORM-06: o log de inputs é quantizado **na captura**, antes de `sim/` ver o valor, e é
gravado como a tabela **resolvida pela autoridade** — incluindo a política de preenchimento de
buracos — e não como o tráfego que chegou.

A quantização acontece em `app/input.ts` (D-02, D-03): `aim` arredonda ao passo de `2π/65536`
(0,005493°, erro máximo de arredondamento 0,002747°) e `move.x`/`move.y` viram `int8` em
`[-127, 127]` com valor efetivo `n/127`. Isso tira o resultado de `Math.hypot` —
implementation-defined — do caminho do dado gravado: o que o sim vê é o inteiro, não o float
do motor. O custo, medido e aceito: o `move` diagonal do teclado (`1/√2`) vira `int8 90`, que
decodifica para `0,708661`, e a magnitude do vetor passa de `1,000000` para `1,002199`
(+0,22%).

Duas armadilhas medidas que o plano fecha por escrito:

**Faixa do `aim`.** `round(π/STEP) = 32768` e `round(−π/STEP) = −32768` — são 65.537 valores
distintos para 16 bits. O fio grava `q & 0xFFFF` e a decodificação é **`int16`**
(`(q << 16) >> 16`), o que devolve `aim` à faixa `[−π, π)`, a mesma que `Math.atan2` produz
hoje. Decodificar como `uint16` seria matematicamente correto e moveria a faixa para
`[0, 2π)`, mudando a contagem de iterações dos laços `while (diff > Math.PI) diff -= Math.PI*2`
de `combat.ts:97-98,111-112` e, com ela, os bits no limite do arco de melee. **O fio continua
carregando 16 bits; só a interpretação muda.**

**`-0`.** `Math.round(-0.4)` devolve `-0` (verificado), e `-0/127` também é `-0`. Sem o `| 0`,
um joystick levemente negativo injeta `-0` em `move.x`, que atravessa o `World` e é **perdido**
por `JSON.parse(JSON.stringify(...))` sem que o `hashWorld` consiga detectar a perda — porque
o hash passa pelo mesmo caminho lossy. O formato **canoniza** `-0` para `+0` na captura, e
isso está registrado no ADR 0011.

Purpose: FORM-06, e o formato que a fase 4 vai gravar e a fase 9 vai verificar.
Output: o codec no protocolo, o envelope com o teto de ticks, e `app/input.ts` quantizando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-formato-e-costuras/01-CONTEXT.md
@.planning/phases/01-formato-e-costuras/01-VALIDATION.md
@src/app/input.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `inputCodec.ts` — quantização, 6 bytes e preenchimento de buracos</name>
  <files>packages/protocol/src/inputCodec.ts, tests/input-codec.test.ts</files>
  <read_first>
    - packages/sim/src/types.ts (`:237-243` — o `InputState` que o sim consome; `aim` continua `number` em radianos e nenhum consumidor dentro de `sim/` muda de unidade)
    - packages/sim/src/combat.ts (`:96-99` e `:110-113` — os dois laços de normalização de ângulo cuja contagem de iterações depende da faixa de `aim`; é o motivo de a decodificação ser `int16`)
    - tests/inputLog.ts (o decodificador provisório criado no plano 01-04, com o mesmo layout de 6 bytes; este plano o substitui por um adaptador)
    - packages/sim/src/rng.ts (`:12-18` — o vocabulário de aritmética de bits aceito no projeto)
    - .planning/phases/01-formato-e-costuras/01-RESEARCH.md § Architecture Patterns — Padrão 2 (a tabela do pacote de 6 bytes) e § Code Examples 2
  </read_first>
  <behavior>
    - `quantize` sobre um input cru devolve um `InputState` cujos `move.x`, `move.y` e `aim` são reprodutíveis a partir de inteiros.
    - `quantize(quantize(x))` é igual a `quantize(x)` — idempotência, verificada sobre pelo menos 200.000 amostras de um corpus determinístico.
    - `unpackTick(packTick(s))` devolve um `InputState` idêntico a `s` já quantizado, campo a campo, comparado com `Object.is`.
    - Um tick empacotado ocupa exatamente **6 bytes**: `int8` `move.x`, `int8` `move.y`, `uint16 LE` `aim`, `uint8` de flags (bit0 `attack`, bit1 `special`, bit2 `sprint`, bits 3-7 zerados) e `uint8` de índice de jogador.
    - `aim` decodifica como `int16`: um `q` de `0xFFFF` volta como `-AIM_STEP`, não como `65535 * AIM_STEP`.
    - `move.x` de `-0.001` codifica e decodifica para `+0`, e `Object.is(resultado, -0)` é `false`.
    - `encodeLog`/`decodeLog` fazem round-trip de uma tabela de vários jogadores por vários ticks, gravando **só o que mudou** (delta + RLE) e mantendo a ordem canônica dentro do tick.
    - Um jogador ausente num tick recebe, na decodificação, **o último input conhecido dele** — e um jogador que nunca apareceu recebe o input neutro.
    - `decodeLog` recusa um log que declare mais ticks que `MAX_RUN_TICKS`.
  </behavior>
  <action>
    Criar `packages/protocol/src/inputCodec.ts` exportando:
    `AIM_STEP = (Math.PI * 2) / 65536`; `quantize(raw)` que produz um `InputState` já
    quantizado; `packTick(idx, state, view, offset)` e `unpackTick(view, offset)` sobre o
    layout de 6 bytes; `encodeLog(table)` e `decodeLog(base64, players)` para o blob do
    envelope; e `decodeInputRecords(records, players)`, que consome a forma legível em
    inteiros que `tests/golden/campaign-mage-3000.json` usa.

    Detalhes que não podem ficar a critério do executor:
    - `move`: `Math.max(-127, Math.min(127, Math.round(raw.move.x * 127))) | 0`. O `| 0` é
      obrigatório e existe por causa do `-0`; escrever o porquê como comentário em inglês.
      Decodifica por `n / 127`.
    - `aim`: `Math.round(raw.aim / AIM_STEP) & 0xffff` na codificação;
      `((q << 16) >> 16) * AIM_STEP` na decodificação. Comentário em inglês registrando por que
      é `int16` e não `uint16`, citando `combat.ts:97-98,111-112`.
    - Multiplicação e divisão IEEE-754 são exatamente arredondadas por spec, então o valor volta
      bit-idêntico em qualquer motor — é essa propriedade que faz o log ser dado e não cálculo.
    - Buracos (D-04): a decodificação repete o **último input conhecido** daquele jogador. A
      política é parte do formato, não do código de rede, e vai escrita como comentário.
    - Ordem dentro do tick: a ordem canônica de `RunConfig.players` (D-12). O byte 5 carrega o
      índice nessa ordem. Enquanto `RunConfig.players` não existir — ele nasce no plano 01-13 —
      o índice 0 designa o slot local; deixar isso escrito, não implícito.

    O codec importa `InputState` de `@dg2/sim` com `import type`, que some na compilação: o
    `dependencies: {}` de `packages/protocol` continua vazio.

    Escrever `tests/input-codec.test.ts` **antes**, cobrindo os nove comportamentos acima. O
    corpus vem de um `Rng` semeado, nunca de `Math.random`. `toBeCloseTo` é proibido; usar
    `Object.is`, que é o único que distingue `-0` de `+0` — e essa distinção é o ponto de dois
    dos testes.
  </action>
  <verify>
    <automated>npx vitest run tests/input-codec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/input-codec.test.ts` passa, cobrindo os nove comportamentos.
    - Um teste assere que 200.000 amostras satisfazem `quantize(quantize(x)) === quantize(x)`.
    - Um teste assere que o buffer de um tick tem exatamente 6 bytes.
    - Um teste assere que `q = 0xFFFF` decodifica para `-AIM_STEP` (interpretação `int16`).
    - Um teste assere `Object.is(decode(encode(-0.001)).move.x, -0) === false`.
    - Um teste assere que um jogador ausente num tick recebe o último input conhecido dele.
    - `grep -c "toBeCloseTo" tests/input-codec.test.ts` retorna 0.
    - `node -e "const p=require('./packages/protocol/package.json'); if(Object.keys(p.dependencies||{}).length)process.exit(1)"` sai 0.
  </acceptance_criteria>
  <done>O codec existe no protocolo, é idempotente, cabe em 6 bytes por tick e a política de buraco está no formato.</done>
</task>

<task type="auto">
  <name>Task 2: O envelope de run, com o teto de ticks que a fase 9 vai aplicar</name>
  <files>packages/protocol/src/runEnvelope.ts, packages/protocol/src/index.ts, tests/inputLog.ts</files>
  <read_first>
    - docs/adr/0011-formato-de-replay.md (a decisão registrada pelo plano 01-02: envelope JSON legível, replay parte da seed, sem checkpoints periódicos, teto de ticks, `-0` canonizado)
    - packages/protocol/src/version.ts (o `Versions` do plano 01-06, que o envelope referencia)
    - tests/inputLog.ts (o decodificador provisório do plano 01-04, que vira adaptador)
    - tests/golden/campaign-mage-3000.json (a forma que `decodeInputRecords` consome)
  </read_first>
  <action>
    Criar `packages/protocol/src/runEnvelope.ts` com o tipo `RunEnvelope` de D-10: um envelope
    **JSON legível** — `runFormatVersion`, `seed`, `config`, `simVersion`, `protocolVersion`,
    `players` (a ordem canônica), `ticks`, `maxTicks`, `score` alegado, `finalHash` — com o log
    de inputs como **blob binário em base64** dentro dele. Dá para abrir um replay num editor
    sem ferramenta, e o log, que é 99% dos bytes, fica compacto.

    Exportar `MAX_RUN_TICKS = 60 * 3600 * 3` (648.000) e `RUN_FORMAT_VERSION = 1`. O teto é o
    controle de T-1-03: a **verificação** é da fase 9, mas o **campo tem de existir no formato
    agora** — acrescentá-lo depois é migração. Comentário em inglês dizendo exatamente isso, e
    dizendo também que um log alegando dez horas de run é 2,16 milhões de ticks e que dez
    submissões em paralelo são um DoS trivial sem teto estrutural.

    Registrar como comentário, a partir de D-11: o envelope **não** carrega snapshot inicial e
    **não** carrega checkpoints periódicos de hash. O verificador roda `createWorld(config)` e
    depois aplica o log; se o setup divergir, a verificação falha no tick 0 em vez de mascarar o
    problema. Os `checkpoints` que existem em `tests/golden/` são **dado de teste** e não fazem
    parte deste tipo — deixar a distinção escrita, porque é onde a recusa de D-11 tende a vazar
    de volta.

    Acrescentar `export * from './inputCodec'` e `export * from './runEnvelope'` ao barrel
    `packages/protocol/src/index.ts`.

    Reduzir `tests/inputLog.ts` a um adaptador fino que re-exporta `decodeInputRecords` e
    `AIM_STEP` de `@dg2/protocol`, apagando a implementação duplicada que o plano 01-04 deixou
    lá provisoriamente. `tests/golden.test.ts` e `tests/cross-engine.test.ts` continuam
    importando de `tests/inputLog.ts` e não mudam.

    **O hash-ouro não pode mudar.** A promoção é de código, não de dado: o arquivo
    `tests/golden/campaign-mage-3000.json` não é tocado.
  </action>
  <verify>
    <automated>npx vitest run tests/golden.test.ts && git diff --quiet tests/golden/ && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MAX_RUN_TICKS" packages/protocol/src/runEnvelope.ts` retorna pelo menos 1, e o valor é `60 * 3600 * 3`.
    - O tipo `RunEnvelope` declara `maxTicks`, `simVersion`, `protocolVersion`, `finalHash` e `log`.
    - `grep -ci "checkpoint" packages/protocol/src/runEnvelope.ts` retorna pelo menos 1, e a ocorrência é o comentário que **recusa** checkpoints no formato.
    - `wc -l tests/inputLog.ts` mostra um arquivo de no máximo 15 linhas — só re-export.
    - `npx vitest run tests/golden.test.ts` passa e `git diff --quiet tests/golden/` sai 0.
    - `npx tsc --noEmit` e `npx tsc -p packages/protocol/tsconfig.json --noEmit` saem 0.
  </acceptance_criteria>
  <done>O envelope de run está tipado, com o teto de ticks no formato desde já, e o codec provisório dos testes virou um adaptador.</done>
</task>

<task type="auto">
  <name>Task 3: Quantizar na captura, em `app/input.ts`</name>
  <files>src/app/input.ts</files>
  <read_first>
    - src/app/input.ts (o arquivo inteiro; `:102-128` é `collect()`, o ponto único de entrada; `:110-118` é onde a quantização entra; `:64`, `:79`, `:82`, `:111` e `:113` são os `Math.hypot`/`Math.atan2` que **ficam**)
    - packages/protocol/src/inputCodec.ts (o `quantize` criado na Task 1)
    - packages/sim/src/types.ts (`:237-243` — o `InputState`, que não muda de forma nem de unidade)
    - docs/adr/0011-formato-de-replay.md (a regra escrita do protocolo, D-05)
  </read_first>
  <action>
    Em `src/app/input.ts`, dentro de `collect(tick)`, inserir a quantização **entre** a
    montagem de `x`, `y` e `aim` e a construção do objeto `input`: o `InputState` devolvido já
    sai quantizado. Nenhum consumidor dentro de `sim/` muda de unidade — `aim` continua
    `number` em radianos e `move` continua com componentes em `[-1, 1]`.

    Os `Math.hypot` de `:111` e `:113` e o `Math.atan2` de `:79`/`:82` **ficam onde estão**
    (D-05). Isso é seguro por uma razão específica que precisa ir escrita como comentário em
    inglês no arquivo: **o `InputState` quantizado é o que atravessa a rede e o que entra no
    log; nenhum par recalcula mira ou movimento a partir de estado do mundo**. Enquanto essa
    regra valer, o resultado implementation-defined de `Math.hypot` morre na captura e nunca
    entra em dado gravado. Auto-aim (`nearestEnemy`, `aimAngle`) continua fora de `sim/` pela
    mesma razão.

    A magnitude parcial do joystick analógico continua preservada — é a razão de `move` ser
    `int8` com valor efetivo `n/127` e não um bit de direção.

    A suíte inteira precisa continuar verde e o hash-ouro precisa continuar idêntico: o ouro é
    dado gravado e não passa por `app/input.ts`. Se o ouro mudar, alguma coisa foi alterada
    fora do escopo desta task.
  </action>
  <verify>
    <automated>npx vitest run && git diff --quiet tests/golden/ && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "quantize" src/app/input.ts` retorna pelo menos 1, e a chamada está dentro de `collect`.
    - `grep -c "Math.hypot" src/app/input.ts` retorna 2 e `grep -c "Math.atan2" src/app/input.ts` retorna pelo menos 1 — eles **ficam**.
    - O arquivo contém um comentário em inglês registrando a regra de D-05 (o `InputState` trafega, ninguém recalcula).
    - `npx vitest run` mantém a suíte inteira verde.
    - `git diff --quiet tests/golden/` sai 0.
    - `npm run lint` e `npx tsc --noEmit` saem 0.
    - `git diff --stat packages/sim/` está vazio.
  </acceptance_criteria>
  <done>O que o sim vê já é o inteiro quantizado, e o float do motor morre na captura.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispositivo de entrada → `InputState` gravado | O float do motor não pode entrar em dado que será re-executado |
| envelope de run submetido → verificador (fase 9) | Um log é entrada não confiável com custo de CPU proporcional ao que ele declara |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Denial of Service | envelope de run como amplificador de CPU | mitigate | `MAX_RUN_TICKS = 60 * 3600 * 3` existe no formato desde já e `decodeLog` recusa um log que declare mais que isso; a aplicação completa do teto (bytes e tempo de parede) é da fase 9, mas o campo não pode nascer depois — seria migração |
| T-1-01 | Spoofing | envelope declarando `simVersion` do agrado do cliente | mitigate | O tipo `RunEnvelope` carrega `simVersion` e `protocolVersion`, e a comparação é feita por `checkVersions` do plano 01-06 contra o valor **próprio** de quem verifica |
| T-1-02 | Tampering | log adulterado com input impossível | mitigate | O clamp de `move` e o domínio de `aim` são aplicados na decodificação por construção (o `int8` e o `int16` não têm valor fora da faixa), e a validação final mora dentro de `step()`/`updatePlayer` — nunca num validador à parte, que derivaria do sim com o tempo |
</threat_model>

<verification>
- `npx vitest run tests/input-codec.test.ts` verde
- `npx vitest run` — suíte inteira verde, incluindo `tests/golden.test.ts`
- `git diff --quiet tests/golden/` sai 0 — este plano não muda dado de ouro
- `npx tsc --noEmit`, `npx tsc -p packages/protocol/tsconfig.json --noEmit` e `npm run lint` saem 0
</verification>

<success_criteria>
- Quantização na captura, com `aim` em `int16` e `move` em `int8`, e `-0` canonizado
- Pacote de 6 bytes por tick, idempotente e round-trip exato
- Delta + RLE com preenchimento de buracos pelo último input conhecido
- Envelope de run tipado, com teto de ticks e sem checkpoints
- `Math.hypot`/`Math.atan2` seguros em `app/`, com a razão escrita no código
</success_criteria>

<output>
Create `.planning/phases/01-formato-e-costuras/01-10-SUMMARY.md` when done
</output>
